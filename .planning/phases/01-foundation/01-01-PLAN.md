---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/bluefish-design-system/SKILL.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "SKILL.md description field uses noun-phrase knowledge domains only — no action verbs (explore, prototype, spec, build)"
    - "SKILL.md does NOT contain the '## Screen Workflow' section or any of its four sub-modes"
    - "SKILL.md retains '## Breaking Changes' and '## Known Gaps' sections intact"
    - "SKILL.md retains '## Code Output' section intact (lines 115–168)"
    - "Known Gaps item for Code Connect no longer references 'Screen Workflow'"
  artifacts:
    - path: "~/.claude/skills/bluefish-design-system/SKILL.md"
      provides: "Refactored foundation skill — shared context only, no workflow logic"
      contains: "## Code Output"
      does_not_contain: "## Screen Workflow"
  key_links:
    - from: "SKILL.md description:"
      to: "Claude skill router"
      via: "frontmatter description field"
      pattern: "description field contains only knowledge domain nouns"
    - from: "SKILL.md body"
      to: "Phase 2/3 command skills"
      via: "@include directive"
      pattern: "foundation file exists at canonical path with no workflow logic"
---

<objective>
Refactor `~/.claude/skills/bluefish-design-system/SKILL.md` into a clean shared context
layer by: (1) rewriting the `description:` frontmatter field to trigger on knowledge
questions only, (2) deleting the `## Screen Workflow` section and all four sub-modes
(lines 169–261), and (3) fixing the stale cross-reference in `## Known Gaps` line 278
that will become a dead pointer after Screen Workflow is removed.

Purpose: Phase 2/3 command skills will load this file via @include. It must contain only
shared context (token rules, a11y, Figma reading, component conventions, code output
standards, known gaps) — no workflow routing, no task verbs in the description.

Output: SKILL.md at the same path, refactored. No new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key decisions governing this task. Extracted from 01-CONTEXT.md. -->

D-01: Command skills load the foundation via @include. The foundation path
      (~/.claude/skills/bluefish-design-system/SKILL.md) must remain stable.

D-03: Foundation description: field must focus on design system context (noun phrases:
      token lookups, accessibility rules, Figma variable reading, component conventions,
      code output standards). Must NOT include action verbs: explore, prototype, spec, build.
      Foundation is a context fallback for general Bluefish design system questions.

Discretion: ## Code Output section (lines 115-168) STAYS in the foundation.
             Command skills may reference its checklist items without duplication prohibition.

<!-- Key boundaries from 01-RESEARCH.md (Focus Area 1). -->

STAY in foundation:
  Lines 1-7:     Frontmatter (name, description) — rewrite description only
  Lines 8-12:    ## About Bluefish
  Lines 14-20:   ## Figma MCP Setup
  Lines 22-42:   ## Live Token Grounding
  Lines 44-81:   ## Token Convention — Critical
  Lines 83-101:  ## Accessibility (WCAG AA)
  Lines 104-113: ## Component Map
  Lines 115-168: ## Code Output  ← KEEP per Claude's Discretion
  Lines 262-279: ## Breaking Changes + ## Known Gaps

REMOVE from foundation (lines 169-261):
  Line 169:  ## Screen Workflow  (through end of section)
  Lines 207-218: ### Spec Output (spec this)
  Lines 219-223: ### Build Output (build this)
  Lines 224-249: ### Prototype Output (prototype this)
  Lines 250-261: ### Explore Output (explore this)
  Lines 262+ continue with ## Breaking Changes (keep from here onward)

STALE CROSS-REFERENCE TO FIX (line 278 after renumbering):
  Current text: "...get_design_context will return a Code Connect interruption prompt.
                 See Screen Workflow for handling instructions."
  Required text: "...get_design_context will return a Code Connect interruption prompt.
                 Command skills invoked for this frame will handle the Code Connect
                 interruption — see the relevant command skill's workflow instructions."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite description field, remove Screen Workflow, fix stale cross-reference</name>

  <read_first>
    - ~/.claude/skills/bluefish-design-system/SKILL.md  (READ ENTIRE FILE before making any edits — understand current state)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-CONTEXT.md  (D-03 description field decision)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-RESEARCH.md  (Focus Area 1 section boundaries, Focus Area 3 description trigger design)
  </read_first>

  <files>~/.claude/skills/bluefish-design-system/SKILL.md</files>

  <action>
    Make three targeted edits to SKILL.md. Read the file first to confirm current line numbers before editing.

    EDIT 1 — Rewrite the description: field in the frontmatter (lines 1-4).

    Current frontmatter:
    ```yaml
    ---
    name: bluefish-design-system
    description: Use when designing, prototyping, generating code, or speccing screens for the Bluefish product. Also use when reading Bluefish Figma components via the Figma MCP.
    ---
    ```

    Replace description with EXACTLY this text (per D-03 — noun-phrase knowledge domains,
    no action verbs for workflow tasks):
    ```yaml
    ---
    name: bluefish-design-system
    description: >
      Bluefish design system reference. Use for token lookups, accessibility rules,
      Figma variable reading, component conventions, and code output standards.
      Activates for any question about the Bluefish design system that is not a
      specific explore, prototype, spec, or build task.
    ---
    ```

    EDIT 2 — Remove the ## Screen Workflow section entirely.

    Delete from the line containing "## Screen Workflow" through the line containing
    "---" that closes the ### Explore Output sub-mode (the horizontal rule before
    ## Breaking Changes). Verify after deletion that the file reads:
      ...end of ## Code Output content...
      ---
      ## Breaking Changes
      ...

    The exact content to remove is the block starting with:
    ```
    ## Screen Workflow

    When the user says "spec this", "build this", "explore this", or "component spec"...
    ```
    ...and ending with (inclusive):
    ```
    - Flag unresolved values: `/* ⚠️ [value] — no token found */`

    ---
    ```
    (the horizontal rule after "Flag unresolved values" that precedes ## Breaking Changes)

    Do NOT remove: the "---" separator that precedes ## Breaking Changes.

    EDIT 3 — Fix the stale cross-reference in ## Known Gaps.

    Find this text in the Known Gaps section:
    ```
    - [ ] Code Connect not configured — eng required to set up `.figma.tsx` mapping files
          and run `figma connect publish`. Until complete, `get_design_context` will return
          a Code Connect interruption prompt. See Screen Workflow for handling instructions.
    ```

    Replace "See Screen Workflow for handling instructions." with:
    "Command skills invoked for this frame will handle the Code Connect interruption — see
    the relevant command skill's workflow instructions."

    Full replacement line:
    ```
    - [ ] Code Connect not configured — eng required to set up `.figma.tsx` mapping files and run `figma connect publish`. Until complete, `get_design_context` will return a Code Connect interruption prompt. Command skills invoked for this frame will handle the Code Connect interruption — see the relevant command skill's workflow instructions.
    ```
  </action>

  <verify>
    Run these checks after editing — ALL must pass:

    1. Description rewritten (no old text):
       grep -c "designing, prototyping, generating code" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 0

    2. Description contains knowledge domain noun phrases (per D-03):
       grep -c "token lookups, accessibility rules" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 1

    3. Screen Workflow section removed:
       grep -c "## Screen Workflow" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 0

    4. Sub-modes removed:
       grep -c "### Spec Output" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 0
       grep -c "### Explore Output" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 0

    5. Code Output section retained:
       grep -c "## Code Output" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 1

    6. Breaking Changes retained:
       grep -c "## Breaking Changes" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 1

    7. Known Gaps retained:
       grep -c "## Known Gaps" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 1

    8. Stale cross-reference removed:
       grep -c "See Screen Workflow for handling" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 0

    9. Replacement text present:
       grep -c "Command skills invoked for this frame" ~/.claude/skills/bluefish-design-system/SKILL.md
       # Expected: 1
  </verify>

  <done>
    - SKILL.md description: field is the exact noun-phrase text from EDIT 1 above (per D-03)
    - SKILL.md contains no "## Screen Workflow" heading or sub-mode headings
    - SKILL.md retains ## About Bluefish, ## Figma MCP Setup, ## Live Token Grounding, ## Token Convention — Critical, ## Accessibility, ## Component Map, ## Code Output, ## Breaking Changes, ## Known Gaps
    - Known Gaps Code Connect item no longer references "Screen Workflow"
    - All 9 grep checks above pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → SKILL.md | File is read and written by the executor; no external input crosses this boundary |
| SKILL.md → Claude skill router | Description field content influences Claude's probabilistic skill selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SKILL.md edit | mitigate | Read the full file before any edit; use targeted Edit tool operations, not full file rewrites, to minimize accidental content loss |
| T-01-02 | Denial of Service | ## Code Output deletion | mitigate | Explicit acceptance_criteria grep checks for ## Code Output after editing; if missing, restore from git |
| T-01-03 | Information Disclosure | Description field | accept | Description is intentionally public (Claude reads it for skill routing); no sensitive content present |
| T-01-04 | Repudiation | Section boundary (line 261 vs 262) | mitigate | Read-first instruction requires confirming line numbers against live file before deleting; do not rely on plan line numbers alone |
</threat_model>

<verification>
After task completes, confirm SKILL.md structure by reading it end-to-end:
- Section order: About Bluefish → Figma MCP Setup → Live Token Grounding → Token Convention — Critical → Accessibility → Component Map → Code Output → Breaking Changes → Known Gaps
- No orphaned sub-mode headings (### Spec Output, ### Build Output, ### Prototype Output, ### Explore Output) remain
- No references to "Screen Workflow" remain anywhere in the file
- The file ends at the last Known Gaps bullet item (no trailing blank sections)
</verification>

<success_criteria>
1. SKILL.md description: field contains noun-phrase knowledge domains, not workflow action verbs (FOUND-02)
2. SKILL.md contains only shared context — any team member reading it understands the design system without seeing command-specific workflow steps (FOUND-01)
3. Code Connect Known Gaps item is self-contained — no dangling pointer to a removed section
4. Foundation path unchanged: ~/.claude/skills/bluefish-design-system/SKILL.md — Phase 2/3 @include directives will resolve correctly
</success_criteria>

<output>
After completion, create `/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-01-SUMMARY.md`
using the summary template.
</output>
