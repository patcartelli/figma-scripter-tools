---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/bluefish-design-system/tokens.md
  - ~/.claude/skills/bluefish-design-system/type-styles.md
  - ~/.claude/skills/bluefish-design-system/tokens-dataviz.md
  - ~/.claude/skills/bluefish-design-system/figma-reading-guide.md
  - ~/.claude/skills/bluefish-design-system/spec-template.md
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "All five support files have been read and confirmed against the outline rename audit"
    - "Each file carries an audit stamp recording the verification date and outcome"
    - "Drift items are flagged inline in the relevant files with a comment note, not corrected"
    - "No token path renames are applied (audit confirmed rename already applied to live tables)"
  artifacts:
    - path: "~/.claude/skills/bluefish-design-system/tokens.md"
      provides: "Token reference with audit stamp confirming outline rename verified"
      contains: "AUDIT"
    - path: "~/.claude/skills/bluefish-design-system/type-styles.md"
      provides: "Typography reference with audit stamp and Page Title drift flag"
      contains: "AUDIT"
    - path: "~/.claude/skills/bluefish-design-system/tokens-dataviz.md"
      provides: "Dataviz palette reference with audit stamp"
      contains: "AUDIT"
    - path: "~/.claude/skills/bluefish-design-system/figma-reading-guide.md"
      provides: "Figma reading guide with audit stamp"
      contains: "AUDIT"
    - path: "~/.claude/skills/bluefish-design-system/spec-template.md"
      provides: "Spec template with audit stamp"
      contains: "AUDIT"
  key_links:
    - from: "tokens.md outline table (lines 38-39)"
      to: "SKILL.md ## Token Convention — Critical"
      via: "on-demand Read instruction in SKILL.md"
      pattern: "outline/outline-variant and outline/default present in table"
---

<objective>
Audit all five support files for the outline token rename (D-04) and attach an audit stamp
to each file recording the verification outcome. The research phase already confirmed the
rename was applied to the live token tables in tokens.md — this plan's job is to: (1) verify
that finding by reading each file, (2) flag any drift items discovered inline, and
(3) attach a dated audit stamp so future readers know the audit was done.

Purpose: FOUND-03 requires support files verified complete and consistent. The stamp
proves the audit happened and makes drift visible without correcting it (D-04: flag only
in Phase 1, correct in a future pass).

Output: All five support files updated with audit stamps. No token path values changed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key decisions governing this task. Extracted from 01-CONTEXT.md. -->

D-02: Support files are NOT @included upfront. The foundation keeps the on-demand Read
      instruction pattern — Claude reads them when the task requires it.

D-04: Apply the outline token rename across all five support files:
      outline/subtle → outline/default
      outline/default → outline/outline-variant
      Any other obvious drift found while reading should be FLAGGED inline
      (comment or note) but NOT corrected in Phase 1 — deferred to a future pass.

<!-- Known findings from 01-RESEARCH.md (Focus Area 2). -->

Audit results from research:
  tokens.md:            Outline table (lines 38-39) already correct. Migration note
                        banner (line 12) uses old names intentionally (documents rename
                        direction). No token path edits needed. No other drift.

  figma-reading-guide.md: Outline mapping table (lines 75-76) already correct.
                        `1/color-roles` prefix notation is intentional Figma collection
                        identifier — NOT a token path error. No edits needed.

  type-styles.md:       No outline references. Drift: `Page Title` listed in table
                        without deprecation note (type-styles.md line 46 has a
                        deprecation note, but SKILL.md line 75 lists Page Title without
                        one). Flag drift — do not correct in Phase 1.

  tokens-dataviz.md:    No outline references. `1/color-roles/dataviz/[NN]/[property]`
                        notation on line 8 is intentional Figma collection notation —
                        NOT an error. No edits needed.

  spec-template.md:     No outline references. No other drift found.

DO NOT change any token path values in any file. The rename is already applied.
DO NOT remove the migration note banner from tokens.md (line 12) — it is intentional
history documentation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read, verify, and stamp all five support files</name>

  <read_first>
    Read each support file in full before editing any of them. Confirm the research
    findings below against the live file state before attaching stamps.
    - ~/.claude/skills/bluefish-design-system/tokens.md
    - ~/.claude/skills/bluefish-design-system/type-styles.md
    - ~/.claude/skills/bluefish-design-system/tokens-dataviz.md
    - ~/.claude/skills/bluefish-design-system/figma-reading-guide.md
    - ~/.claude/skills/bluefish-design-system/spec-template.md
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-CONTEXT.md  (D-04 scope)
  </read_first>

  <files>
    ~/.claude/skills/bluefish-design-system/tokens.md
    ~/.claude/skills/bluefish-design-system/type-styles.md
    ~/.claude/skills/bluefish-design-system/tokens-dataviz.md
    ~/.claude/skills/bluefish-design-system/figma-reading-guide.md
    ~/.claude/skills/bluefish-design-system/spec-template.md
  </files>

  <action>
    For each of the five support files, perform the following steps IN ORDER:

    STEP A — Verify the research findings (check these before stamping):
    1. tokens.md: Confirm lines containing "outline/outline-variant" and "outline/default"
       appear in the color-roles table (Divider + Outline section). Confirm the migration
       note banner on line 12 uses the old names in "X → Y" format (intentional).
       Confirm no stale `outline/subtle` or old `outline/default` appears as a table row.
    2. figma-reading-guide.md: Confirm the M3 mapping table contains:
       `md.sys.color.outline` → `color-roles/outline/outline-variant`
       `md.sys.color.outline-variant` → `color-roles/outline/default`
    3. type-styles.md: Confirm `Page Title` appears in the type style table WITHOUT a
       deprecation note inline (the note is on line 46 as a blockquote above the table,
       not beside Page Title in the table row itself). Note this as a drift flag.
    4. tokens-dataviz.md: Confirm no outline token references. Confirm
       `1/color-roles/dataviz` notation is present — note it is intentional, not an error.
    5. spec-template.md: Confirm no outline token references and no obvious drift.

    If any of the research findings are contradicted by the live file (e.g., an old
    token name still present in a table row), FIX that specific value before stamping
    and note the discrepancy in the audit stamp.

    STEP B — Add inline drift flag to type-styles.md (D-04 says flag, don't fix):
    In type-styles.md, find the Type Style Mapping table. The table lists `Page Title`
    as a row. The research found that SKILL.md line 75 still lists `Page Title` without
    a deprecation note, creating an inconsistency with type-styles.md's blockquote note
    above the table.

    Since we cannot edit SKILL.md's Token Convention section in this plan (it's in
    Plan 01's scope), add a note to the type-styles.md blockquote area to make the
    cross-file drift visible. Add this line immediately after the existing deprecation
    note blockquote (line 46):

    ```
    > **Drift flag (Phase 1 audit, 2026-05-08):** SKILL.md § Token Convention — Critical
    > still lists `Page Title` as an authoritative style without a deprecation note.
    > Correct in a future pass — remove `Page Title` from the SKILL.md type styles list
    > and replace with a note pointing to H4.
    ```

    STEP C — Append an audit stamp to the bottom of EACH file.
    Use this exact stamp format, filling in the file-specific outcome:

    For tokens.md:
    ```
    ---

    ## Phase 1 Audit — 2026-05-08

    **Outline rename:** VERIFIED — `outline/outline-variant` and `outline/default` present
    in live table. Migration note banner (line 12) uses old names intentionally
    (documents rename direction) — no change needed.
    **Other drift:** None found.
    **Status:** FOUND-03 complete for this file.
    ```

    For figma-reading-guide.md:
    ```
    ---

    ## Phase 1 Audit — 2026-05-08

    **Outline rename:** VERIFIED — M3 mapping table correctly maps
    `md.sys.color.outline` → `color-roles/outline/outline-variant` and
    `md.sys.color.outline-variant` → `color-roles/outline/default`.
    **Other drift:** `1/color-roles` and `1/scale` prefix notation (line 56) —
    intentional Figma variable collection identifier, not a token path error. No change.
    **Status:** FOUND-03 complete for this file.
    ```

    For type-styles.md:
    ```
    ---

    ## Phase 1 Audit — 2026-05-08

    **Outline rename:** N/A — no outline token references in this file.
    **Other drift:** `Page Title` listed in type style table; SKILL.md § Token Convention
    still lists it as authoritative without a deprecation note. Drift flag added above
    the table. Correct in a future pass (remove from SKILL.md type styles list).
    **Status:** FOUND-03 complete (no rename needed). Drift flagged per D-04.
    ```

    For tokens-dataviz.md:
    ```
    ---

    ## Phase 1 Audit — 2026-05-08

    **Outline rename:** N/A — no outline token references in this file.
    **Other drift:** `1/color-roles/dataviz/[NN]/[property]` notation on line 8 is
    intentional Figma collection notation (collection name prefix for Figma Desktop
    variable reading), not a token path error. No change needed.
    **Status:** FOUND-03 complete for this file.
    ```

    For spec-template.md:
    ```
    ---

    ## Phase 1 Audit — 2026-05-08

    **Outline rename:** N/A — no outline token references in this file.
    **Other drift:** None found.
    **Status:** FOUND-03 complete for this file.
    ```
  </action>

  <verify>
    Run these checks after all five files are stamped:

    1. tokens.md has audit stamp:
       grep -c "Phase 1 Audit" ~/.claude/skills/bluefish-design-system/tokens.md
       # Expected: 1

    2. type-styles.md has audit stamp:
       grep -c "Phase 1 Audit" ~/.claude/skills/bluefish-design-system/type-styles.md
       # Expected: 1

    3. tokens-dataviz.md has audit stamp:
       grep -c "Phase 1 Audit" ~/.claude/skills/bluefish-design-system/tokens-dataviz.md
       # Expected: 1

    4. figma-reading-guide.md has audit stamp:
       grep -c "Phase 1 Audit" ~/.claude/skills/bluefish-design-system/figma-reading-guide.md
       # Expected: 1

    5. spec-template.md has audit stamp:
       grep -c "Phase 1 Audit" ~/.claude/skills/bluefish-design-system/spec-template.md
       # Expected: 1

    6. Correct outline table values in tokens.md (post-rename names present):
       grep -c "outline/outline-variant" ~/.claude/skills/bluefish-design-system/tokens.md
       # Expected: >= 1

    7. No stale outline/subtle in tokens.md table rows (migration note banner is allowed):
       grep -v "is now renamed" ~/.claude/skills/bluefish-design-system/tokens.md | grep -c "outline/subtle"
       # Expected: 0

    8. Drift flag added to type-styles.md:
       grep -c "Drift flag" ~/.claude/skills/bluefish-design-system/type-styles.md
       # Expected: 1

    9. No token path values changed — verify figma-reading-guide.md M3 mapping is intact:
       grep -c "color-roles/outline/outline-variant" ~/.claude/skills/bluefish-design-system/figma-reading-guide.md
       # Expected: 1
  </verify>

  <done>
    - All five support files read and research findings confirmed against live state
    - Drift flag added to type-styles.md (inline, above the table) per D-04
    - Audit stamp appended to the bottom of all five files with file-specific outcomes
    - No token path values changed in any file (rename was already applied)
    - All 9 grep checks above pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → Support files | Five reference files read and appended to; content must not be corrupted |
| Support files → SKILL.md on-demand reads | Claude reads these files when task requires it; stale content misleads code generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Token table values | mitigate | Action specifies DO NOT change token path values; verification greps confirm correct post-rename names still present |
| T-02-02 | Tampering | Migration note banner (tokens.md line 12) | mitigate | Action explicitly states banner is intentional and must not be removed |
| T-02-03 | Information Disclosure | Drift flag in type-styles.md | accept | Drift flag is intentional and informational; contains no sensitive data |
| T-02-04 | Spoofing | Audit stamp claiming incorrect status | mitigate | Read-first instruction requires confirming live file state before stamping; executor must not stamp based on plan assumptions alone |
</threat_model>

<verification>
After all five stamps are in place, do a final read of each file's last 10 lines to confirm:
- Audit stamp is present and correctly formatted
- No trailing content from SKILL.md or other files accidentally appended
- File content above the stamp is unchanged (no token table rows modified)
</verification>

<success_criteria>
1. All five support files have been read and verified against the outline rename audit (FOUND-03)
2. Audit stamps attached to all five files with accurate, file-specific outcomes
3. Drift flag added to type-styles.md for Page Title inconsistency (per D-04 — flag, don't fix)
4. Zero token path values changed in any file (rename was already applied to live tables)
5. tokens.md migration note banner (line 12) preserved intact — it is historical documentation
</success_criteria>

<output>
After completion, create `/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/01-foundation/01-02-SUMMARY.md`
using the summary template.
</output>
