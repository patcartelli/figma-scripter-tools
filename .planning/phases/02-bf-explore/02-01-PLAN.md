---
phase: 02-bf-explore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/bf-explore/SKILL.md
autonomous: false
requirements: [EXPL-01, EXPL-02, EXPL-03, EXPL-04]
tags: [skill-authoring, bluefish, layout-exploration, claude-code-skills]

must_haves:
  truths:
    - "User can type /bf-explore in Claude Code and the skill loads"
    - "Skill also auto-fires when user describes a layout exploration need without typing the slash command"
    - "Skill inherits Bluefish foundation context via @include of bluefish-design-system/SKILL.md"
    - "When invoked bare, skill asks exactly one question: 'What screen or component are we exploring?'"
    - "When invoked with screen context, skill proceeds directly to generation (no clarifying questions)"
    - "Skill instructs Claude to produce 2-3 HTML layout variations as fenced ```html code blocks"
    - "Skill instructs Claude to annotate every var() with an inline /* token/path */ comment immediately after"
    - "Skill instructs Claude to flag values without a clean token path using /* ⚠️ no token — [reason] */"
    - "Skill points Claude at explorations/variation-{a,b,c}.html as the layout quality bar"
    - "Skill defines structural distinctness rule: variations must differ at nav model, information architecture, and interaction pattern level"
    - "Skill handles Figma MCP opportunistically (use if available, fall back to conversation context)"
    - "Skill handles Code Connect prompt gracefully by flagging inline and continuing"
  artifacts:
    - path: "~/.claude/skills/bf-explore/SKILL.md"
      provides: "bf-explore skill definition, intake gate, output rules, annotation requirements"
      contains: "name: bf-explore"
      min_lines: 80
  key_links:
    - from: "~/.claude/skills/bf-explore/SKILL.md"
      to: "~/.claude/skills/bluefish-design-system/SKILL.md"
      via: "@include directive at top of skill body"
      pattern: "@~/.claude/skills/bluefish-design-system/SKILL.md"
    - from: "~/.claude/skills/bf-explore/SKILL.md frontmatter"
      to: "Claude Code skill router"
      via: "description: field containing 'HTML layout variations' as discriminating phrase"
      pattern: "HTML layout variations"
    - from: "~/.claude/skills/bf-explore/SKILL.md output rules"
      to: "explorations/variation-a.html, variation-b.html, variation-c.html"
      via: "explicit path reference as quality bar in Style Reference section"
      pattern: "explorations/variation-"
---

## Phase Goal

**As a** designer working on a Bluefish screen, **I want to** type `/bf-explore` (or describe a layout exploration need) and receive 2–3 meaningfully distinct, Bluefish-annotated HTML layout variations, **so that** I can compare layout directions without manually authoring HTML or looking up tokens.

<objective>
Create `~/.claude/skills/bf-explore/SKILL.md` — a focused layout variation generator skill that fires on `/bf-explore` (or auto-trigger via "HTML layout variations" language), inherits the Bluefish foundation context via @include, and instructs Claude to produce 2–3 meaningfully distinct, fully-annotated HTML layout variations.

Purpose: Deliver the first command skill in the skill system. After this plan, a designer can invoke /bf-explore on any Bluefish screen and receive layout explorations that match or exceed the quality bar of `explorations/variation-{a,b,c}.html`, with stricter inline token annotation than the reference files.

Output: One new markdown file at `~/.claude/skills/bf-explore/SKILL.md` (no other files modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bf-explore/02-CONTEXT.md
@.planning/phases/02-bf-explore/02-RESEARCH.md
@.planning/phases/02-bf-explore/02-PATTERNS.md
@~/.claude/skills/bluefish-design-system/SKILL.md

<interfaces>
<!-- Established skill patterns from Phase 1 + reference analogs. Executor uses these directly — no codebase exploration needed. -->

Foundation skill frontmatter (verbatim from ~/.claude/skills/bluefish-design-system/SKILL.md lines 1-8):
```yaml
---
name: bluefish-design-system
description: >
  Bluefish design system reference. Use for token lookups, accessibility rules,
  Figma variable reading, component conventions, and code output standards.
  Activates for any question about the Bluefish design system that is not a
  specific explore, prototype, spec, or build task.
---
```

Required @include path convention (from CONTEXT.md canonical_refs + PATTERNS.md):
- Use home-tilde form: `@~/.claude/skills/bluefish-design-system/SKILL.md`
- Place immediately after frontmatter, before any skill body prose
- No `<execution_context>` XML tag (that pattern is for skills that wrap a separate workflow file; bf-explore IS the workflow)

Reference exploration files (study these for layout quality bar):
- /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-a.html — 56px icon rail + KPI strip + full-width chart
- /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-b.html — 222px text nav + rich card grid with embedded breakdown bars
- /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-c.html — 222px text nav + KPI strip + 300px detail panel

Exact annotation format (verbatim from CONTEXT.md D-05, this format MUST appear in the skill output rules):
```css
background-color: var(--color-roles-primary-main); /* color-roles/primary/main */
```

Gap-flag format (verbatim from CONTEXT.md D-06):
```css
box-shadow: 0 4px 8px rgba(0,0,0,0.08); /* ⚠️ no token — elevation TBD */
```

CSS var naming convention (from PATTERNS.md, verified against variation-a.html):
| Token path | CSS custom property |
|------------|---------------------|
| color-roles/primary/main | --color-roles-primary-main |
| color-roles/background/default | --color-roles-background-default |
| color-roles/text/secondary | --color-roles-text-secondary |
| scale/4 | --scale-4 |
| scale/radius/md | --radius-md (shortened convention used in reference files) |
| scale/radius/full | --radius-full |
| color-roles/divider | --color-roles-divider |

Frontmatter description template (from RESEARCH.md Pattern 1, MUST contain the phrase "HTML layout variations"):
```yaml
---
name: bf-explore
description: >
  Generates 2–3 meaningfully distinct HTML layout variations for a Bluefish screen.
  Use when the user asks for layout explorations, layout variations, or wants to see
  multiple HTML layout options for a Bluefish screen or component. Not for general
  design questions (use bluefish-design-system) or single prototype output (use bf-prototype).
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the complete bf-explore SKILL.md (vertical slice — end-to-end skill in one file)</name>
  <files>~/.claude/skills/bf-explore/SKILL.md</files>

  <read_first>
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/02-bf-explore/02-CONTEXT.md (locked decisions D-01 through D-08, exact annotation format)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/02-bf-explore/02-RESEARCH.md (5 patterns, 5 pitfalls, code examples, known gaps)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/02-bf-explore/02-PATTERNS.md (analog skill structure, frontmatter format, @include placement)
    - ~/.claude/skills/bluefish-design-system/SKILL.md (foundation that bf-explore @includes — confirm it exists at this path before writing the @include line)
    - /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-a.html (reference quality bar — observe :root block structure and CSS var naming, but note this file does NOT have inline annotations; the skill output must exceed it)
    - /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-b.html (reference quality bar — second layout pattern)
    - /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-c.html (reference quality bar — third layout pattern)
  </read_first>

  <action>
Create the directory `~/.claude/skills/bf-explore/` and write `~/.claude/skills/bf-explore/SKILL.md` with the EXACT structure below. Use the Write tool, not heredoc.

The file MUST contain these sections, in this order:

**1. YAML frontmatter** (covers EXPL-04 via the discriminating phrase "HTML layout variations"):

```yaml
---
name: bf-explore
description: >
  Generates 2–3 meaningfully distinct HTML layout variations for a Bluefish screen.
  Use when the user asks for layout explorations, layout variations, or wants to see
  multiple HTML layout options for a Bluefish screen or component. Not for general
  design questions (use bluefish-design-system) or single prototype output (use bf-prototype).
---
```

Do NOT include `argument-hint` (per D-03/D-04, no structured arguments). Do NOT include `allowed-tools` unless Figma MCP tools must be enumerated — defer to Claude Code defaults.

**2. Foundation @include directive** (covers the foundation inheritance link), placed IMMEDIATELY after the closing `---` of the frontmatter, on its own line, with one blank line before and after:

```
@~/.claude/skills/bluefish-design-system/SKILL.md
```

**3. H1 heading and a one-paragraph summary** that names this as the bf-explore command skill:

```markdown
# bf-explore

Generates 2–3 meaningfully distinct HTML layout variations for a Bluefish screen, with every Bluefish token annotated inline. Inherits all Bluefish foundation context (token rules, accessibility standards, Figma MCP setup, code output standards) from `bluefish-design-system` via the `@include` above. This skill owns the layout exploration workflow only.
```

**4. `## Support Files — Read On Demand` section** (per D-02 and PATTERNS.md on-demand pattern). Include verbatim:

```markdown
## Support Files — Read On Demand

Do not read these at skill start. Read them only when the task requires it:

- Read `~/.claude/skills/bluefish-design-system/tokens.md` when generating CSS color, spacing, or radius token values
- Read `~/.claude/skills/bluefish-design-system/type-styles.md` when setting typography
- Read `~/.claude/skills/bluefish-design-system/tokens-dataviz.md` when generating dataviz colors
- Read `~/.claude/skills/bluefish-design-system/figma-reading-guide.md` when interpreting Figma MCP output
```

**5. `## Intake` section** (covers D-03 and D-04). Include verbatim:

```markdown
## Intake

**If the user provided a screen or component description** (in the /bf-explore message or in prior conversation context): proceed immediately to generation. Do not ask any clarifying questions.

**If invoked bare** (`/bf-explore` with no accompanying context): ask exactly one question:

> "What screen or component are we exploring?"

Do not ask additional questions. Do not request constraints, layout rules, or brand emphasis at intake — users can include those in their response or as follow-up. Generate 2–3 variations immediately after receiving the response.
```

**6. `## Figma Context (Opportunistic)` section** (covers D-07 and D-08). Include verbatim:

```markdown
## Figma Context (Opportunistic)

If the user has a Figma screen open in Figma Desktop or provides a Figma URL:

1. Call `get_figma_data` to read component/frame info
2. Use the returned structure to ground layout decisions (component names, hierarchy, content types)
3. If `get_figma_data` triggers a Code Connect prompt: note it inline with `⚠️ Code Connect not configured — proceeding from conversation context` and continue with whatever data was returned before the prompt

If no Figma context is available: generate entirely from conversation description.
```

**7. `## Style Reference` section** (points to the three reference files as the quality bar). Include verbatim:

```markdown
## Style Reference

Study these files before generating — they define the layout quality bar, the `:root` CSS block convention, and the CSS custom property naming convention:

- `/Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-a.html` — 56px icon rail + KPI strip + full-width chart
- `/Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-b.html` — 222px text nav + rich card grid with embedded breakdown bars
- `/Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-c.html` — 222px text nav + KPI strip + 300px detail panel

**Important:** These reference files demonstrate layout quality and CSS var naming, but they do NOT have inline `/* token/path */` annotations on individual `var()` usages. Your output must EXCEED the reference files in annotation density — see the Output Rules below. The annotation requirement is the binding rule; the reference files are not.
```

**8. `## Output Rules` section** (covers EXPL-02 and EXPL-03). Include verbatim:

```markdown
## Output Rules

Generate **2–3 HTML layout variations**. Each variation:

1. **Format:** A complete, self-contained HTML document (`<!DOCTYPE html>`, `<head>` with `<style>` containing a `:root` block and CSS rules, `<body>` with the layout markup). Wrap each variation in a fenced ` ```html ` code block.

2. **Variation heading:** Prefix each fenced block with a heading line: `### Variation A: [short layout pattern name]` (then B, C).

3. **`:root` block:** Include a `:root` block at the top of `<style>` defining the CSS custom properties used, with hex fallbacks (matches the convention in `explorations/variation-a.html`). This is for standalone rendering — it does NOT substitute for inline annotation.

4. **Inline token annotation (EXPL-03, required on every `var()` usage):** Every `var()` call in every CSS rule must be followed immediately by an inline comment containing the canonical token path. Exact format:

   ```css
   .nav-rail {
     background: var(--color-roles-background-default); /* color-roles/background/default */
     border-right: 1px solid var(--color-roles-divider); /* color-roles/divider */
     padding: var(--scale-4); /* scale/4 */
   }
   ```

   A `:root` block does NOT satisfy this requirement. Even though the token path is implied by the var name, the inline comment must appear on every usage.

5. **Gap flagging (D-06):** Any value without a clean token path must be flagged inline. Format:

   ```css
   .chart-panel {
     box-shadow: 0 4px 8px rgba(0,0,0,0.08); /* ⚠️ no token — elevation TBD */
   }
   ```

   Known gaps to flag automatically:
   - All `box-shadow` values → `/* ⚠️ no token — elevation undefined */`
   - Dataviz series hardcoded hex (e.g., `#D77D28`) → `/* ⚠️ dataviz series — use color-roles/dataviz/[NN]/main token when token injection is resolved */`
   - `--page-bg`, `--text-emphasis`, or other reference-file vars that have no token in `tokens.md` → prefer the defined token (`color-roles/background/default`, `color-roles/text/primary`) and flag any deviation.

6. **Structural distinctness (EXPL-02):** Variations must differ at the **nav model**, **information architecture**, and **interaction pattern** level. Cosmetic differences (color swap, padding change, font tweak) do NOT qualify. Aim for the spectrum demonstrated by the three reference files: different nav widths/models, different content layouts (strip vs. grid vs. detail panel), different interaction patterns (tooltip-on-hover vs. inline expand vs. master-detail slide-out).

7. **Accessibility:** Exploration HTML is not exempt from a11y requirements inherited from the foundation. Apply ARIA roles, labels, and `aria-current="page"` patterns demonstrated in `variation-a.html` (e.g., `role="banner"`, `aria-label`, `aria-current`).

8. **CSS variable naming convention:** Use the shortened CSS var names observed in the reference files: `--radius-sm`, `--radius-md`, `--radius-full` (not `--scale-radius-*`). The canonical token path in the inline comment still uses the full `scale/radius/sm` form.
```

**9. `## Anti-Patterns` section** (codifies the pitfalls from RESEARCH.md). Include verbatim:

```markdown
## Anti-Patterns — Do Not Do These

- **Cosmetic-only variations:** Three layouts that differ only in color, padding, or font sizes. Variations must differ at the layout/IA level.
- **`:root` block as annotation substitute:** A well-annotated `:root` block does NOT satisfy EXPL-03. Every `var()` usage in every CSS rule needs its own inline `/* token/path */` comment.
- **Hardcoded hex without `⚠️` flag:** Any hex value (chart series colors, elevation, page background) without a token must carry a `/* ⚠️ no token — [reason] */` comment.
- **Multiple intake questions on bare invocation:** Exactly one question: "What screen or component are we exploring?" Nothing else at intake.
- **Trigger over-broadening:** Do not match general design questions, ideation requests, or single-prototype requests. The discriminating phrase is "HTML layout variations" (plural).
- **Discarding partial MCP results:** If `get_figma_data` returns data before a Code Connect prompt, use what was returned — do not abandon the response.
```

After writing the file, immediately run the verification commands listed in `<verify>` to confirm all required strings are present.
  </action>

  <verify>
    <automated>
test -f ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '^name: bf-explore' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'HTML layout variations' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '^@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Intake' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'What screen or component are we exploring' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Support Files — Read On Demand' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Figma Context' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'Code Connect not configured' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Style Reference' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'explorations/variation-a.html' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'explorations/variation-b.html' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q 'explorations/variation-c.html' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Output Rules' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '2–3 HTML layout variations' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '/\* color-roles/primary/main \*/' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '/\* ⚠️ no token' ~/.claude/skills/bf-explore/SKILL.md \
  && grep -q '## Anti-Patterns' ~/.claude/skills/bf-explore/SKILL.md \
  && [ "$(wc -l < ~/.claude/skills/bf-explore/SKILL.md)" -ge 80 ] \
  && echo "PASS: all required sections and strings present" \
  || echo "FAIL: one or more required elements missing"
    </automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f ~/.claude/skills/bf-explore/SKILL.md` succeeds
    - Frontmatter name: `grep -q '^name: bf-explore' ~/.claude/skills/bf-explore/SKILL.md` succeeds
    - Trigger discriminating phrase: `grep -q 'HTML layout variations' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-02)
    - @include directive on its own line: `grep -q '^@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-explore/SKILL.md` succeeds (Phase 1 D-01 pattern)
    - Intake section header present: `grep -q '## Intake' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-03/D-04)
    - Exact intake question present: `grep -q 'What screen or component are we exploring' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-04)
    - Support files section present: `grep -q '## Support Files — Read On Demand' ~/.claude/skills/bf-explore/SKILL.md` succeeds (Phase 1 D-02 pattern)
    - Figma section header present: `grep -q '## Figma Context' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-07)
    - Code Connect handling present: `grep -q 'Code Connect not configured' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-08)
    - Style Reference section present: `grep -q '## Style Reference' ~/.claude/skills/bf-explore/SKILL.md` succeeds
    - All three reference files cited: `grep -q 'explorations/variation-a.html'`, `variation-b.html`, `variation-c.html` all succeed (EXPL-02 quality bar)
    - Output Rules section present: `grep -q '## Output Rules' ~/.claude/skills/bf-explore/SKILL.md` succeeds (EXPL-02, EXPL-03)
    - Variation quantity specified: `grep -q '2–3 HTML layout variations' ~/.claude/skills/bf-explore/SKILL.md` succeeds (EXPL-02)
    - Exact annotation format example present: `grep -q '/\* color-roles/primary/main \*/' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-05, EXPL-03)
    - Gap flag format present: `grep -q '/\* ⚠️ no token' ~/.claude/skills/bf-explore/SKILL.md` succeeds (D-06)
    - Anti-Patterns section present: `grep -q '## Anti-Patterns' ~/.claude/skills/bf-explore/SKILL.md` succeeds
    - File has substantive content: `wc -l ~/.claude/skills/bf-explore/SKILL.md` returns ≥ 80 lines
  </acceptance_criteria>

  <done>
    All grep assertions in `<acceptance_criteria>` pass on first run. File is at least 80 lines. The skill file is complete and ready for human verification in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — invoke /bf-explore on a real screen and confirm output meets quality bar</name>

  <read_first>
    - ~/.claude/skills/bf-explore/SKILL.md (the file just written — quick scan to confirm it is well-formed)
    - /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-a.html (mental quality bar for layout)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/REQUIREMENTS.md (EXPL-01 through EXPL-04 acceptance criteria)
  </read_first>

  <what-built>
    A single new file: `~/.claude/skills/bf-explore/SKILL.md`. It is a Claude Code command skill that:
    - Fires on `/bf-explore` slash command, or auto-fires when a user describes a need for HTML layout variations on a Bluefish screen
    - Inherits the Bluefish foundation via `@~/.claude/skills/bluefish-design-system/SKILL.md`
    - Asks one clarifying question on bare invocation, or proceeds immediately if context is provided
    - Instructs Claude to produce 2–3 meaningfully distinct HTML layout variations, each with inline token annotations
  </what-built>

  <how-to-verify>
    Run the following verification steps in a fresh Claude Code session:

    **Step 1 — Confirm the skill is discoverable:**
    1. Open a new Claude Code conversation in this repo (`/Users/pcartelli/Desktop/figma-scripter-tools`)
    2. Type `/bf-explore` and press Enter
    3. Expected: Claude responds with the single intake question: "What screen or component are we exploring?" (no other questions, no preamble paragraphs)

    **Step 2 — Confirm context-aware invocation:**
    1. In a fresh conversation, paste a screen description (e.g., "I'm exploring layouts for an AI Insights dashboard with KPIs, trend chart, and a card grid"), then type `/bf-explore`
    2. Expected: Claude proceeds directly to generation without asking the intake question.

    **Step 3 — Confirm auto-trigger (EXPL-04):**
    1. In a fresh conversation, WITHOUT typing the slash command, say: "Show me 3 HTML layout variations for the AI Insights dashboard"
    2. Expected: Claude auto-activates the bf-explore skill (you should see it loading the skill content), not the general bluefish-design-system skill.

    **Step 4 — Confirm output quality (EXPL-02, EXPL-03):**
    Review the generated variations and check:
    - 2 or 3 fenced ` ```html ` blocks, each with a `### Variation [A/B/C]: [name]` heading
    - Each variation is structurally distinct from the others at the nav model, IA, or interaction pattern level — not just cosmetic
    - Every `var()` in CSS has an inline `/* token/path */` comment immediately after on the same line (spot-check 10 random `var()` usages)
    - Any hardcoded hex or shadow value has a `/* ⚠️ no token — ... */` flag
    - Output quality is comparable to or better than `explorations/variation-{a,b,c}.html` (open one to compare)

    **Step 5 — Confirm gap handling:**
    - Look for at least one `/* ⚠️ no token */` flag on `box-shadow` or similar elevation value (proves D-06 gap handling is alive)

    If all five steps pass, respond with "approved". If any step fails, describe which step failed and what was observed — the planner will revise the SKILL.md.
  </how-to-verify>

  <acceptance_criteria>
    - User reports Step 1 PASS: bare `/bf-explore` produces exactly the intake question, nothing else
    - User reports Step 2 PASS: contextual invocation skips intake and generates directly
    - User reports Step 3 PASS: auto-trigger fires bf-explore (not bluefish-design-system or another skill) on a layout exploration phrase
    - User reports Step 4 PASS: 2–3 variations generated, structurally distinct, inline `/* token/path */` annotations on every spot-checked `var()`
    - User reports Step 5 PASS: at least one `/* ⚠️ no token */` flag appears on a gap value
    - User explicitly types "approved" or equivalent confirmation
  </acceptance_criteria>

  <resume-signal>Type "approved" to confirm all five verification steps passed, or describe which step failed and what was observed so the planner can revise the SKILL.md.</resume-signal>

  <done>
    User has typed "approved" (or equivalent) AND all five verification steps demonstrably passed. EXPL-01 through EXPL-04 are satisfied with real end-to-end evidence.
  </done>
</task>

</tasks>

<verification>

## Phase-Level Verification

| Requirement | Verified By |
|-------------|-------------|
| EXPL-01 — `/bf-explore` SKILL.md created at `~/.claude/skills/bf-explore/SKILL.md` with focused layout variation workflow | Task 1 acceptance criteria (file exists, all required sections present); Task 2 Step 1 (skill is discoverable and fires) |
| EXPL-02 — 2–3 meaningfully distinct HTML layout variations | Task 1 acceptance criteria (`2–3 HTML layout variations` string present in output rules; structural distinctness rule documented); Task 2 Step 4 (real output reviewed, variations confirmed distinct) |
| EXPL-03 — Every Bluefish value annotated with token path inline | Task 1 acceptance criteria (`/* color-roles/primary/main */` example present; exact format from D-05 documented; `:root` substitution explicitly rejected); Task 2 Step 4 (spot-check 10 var() usages in real output) |
| EXPL-04 — Skill fires correctly on layout exploration requests | Task 1 acceptance criteria (`HTML layout variations` discriminating phrase in description); Task 2 Step 3 (real auto-trigger test on a layout exploration phrase) |

## Source Coverage Audit

| Source | Item | Plan | Status |
|--------|------|------|--------|
| GOAL (ROADMAP) | Team can type `/bf-explore` and receive 2–3 Bluefish-annotated HTML layout variations | 02-01 | COVERED (Tasks 1+2) |
| REQ (REQUIREMENTS.md) | EXPL-01: SKILL.md created at canonical path | 02-01 | COVERED (Task 1) |
| REQ | EXPL-02: 2–3 meaningfully distinct variations | 02-01 | COVERED (Task 1 Output Rules section, Task 2 Step 4) |
| REQ | EXPL-03: Inline per-usage token annotation | 02-01 | COVERED (Task 1 Output Rules section, Task 2 Step 4) |
| REQ | EXPL-04: Auto-fires on layout exploration language | 02-01 | COVERED (Task 1 frontmatter description, Task 2 Step 3) |
| CONTEXT | D-01: Description anchors on layout exploration language only | 02-01 | COVERED (Task 1 frontmatter, Anti-Patterns section) |
| CONTEXT | D-02: "HTML layout variations" (plural) in description | 02-01 | COVERED (Task 1 frontmatter — verbatim grep) |
| CONTEXT | D-03: Hybrid intake (context → generate; bare → ask one) | 02-01 | COVERED (Task 1 Intake section) |
| CONTEXT | D-04: Single intake question, exact wording | 02-01 | COVERED (Task 1 Intake section — verbatim grep) |
| CONTEXT | D-05: Inline annotation format per usage | 02-01 | COVERED (Task 1 Output Rules section — verbatim grep) |
| CONTEXT | D-06: ⚠️ flag format for missing tokens | 02-01 | COVERED (Task 1 Output Rules + Anti-Patterns) |
| CONTEXT | D-07: Opportunistic Figma MCP | 02-01 | COVERED (Task 1 Figma Context section) |
| CONTEXT | D-08: Code Connect prompt — graceful continue | 02-01 | COVERED (Task 1 Figma Context — verbatim grep) |
| CONTEXT | Claude's Discretion: distinctness bar = explorations/variation-{a,b,c}.html | 02-01 | COVERED (Task 1 Style Reference section + Output Rules #6) |
| RESEARCH | Pattern 1: Frontmatter trigger description | 02-01 | COVERED (Task 1 frontmatter) |
| RESEARCH | Pattern 2: @include + on-demand reads | 02-01 | COVERED (Task 1 @include directive + Support Files section) |
| RESEARCH | Pattern 3: Inline annotation per usage | 02-01 | COVERED (Task 1 Output Rules section) |
| RESEARCH | Pattern 4: CSS var naming convention | 02-01 | COVERED (Task 1 Output Rules #8) |
| RESEARCH | Pattern 5: Variation distinctness bar | 02-01 | COVERED (Task 1 Style Reference + Output Rules #6) |
| RESEARCH | Pitfall 1: Reference files mistaken as annotation model | 02-01 | COVERED (Task 1 Style Reference "important" note + Anti-Patterns) |
| RESEARCH | Pitfall 2: :root substitutes for inline annotation | 02-01 | COVERED (Task 1 Output Rules #3 + Anti-Patterns) |
| RESEARCH | Pitfall 3: Trigger description too broad/narrow | 02-01 | COVERED (Task 1 frontmatter pattern) |
| RESEARCH | Pitfall 4: Hardcoded hex without ⚠️ flag | 02-01 | COVERED (Task 1 Output Rules #5 + Anti-Patterns) |
| RESEARCH | Pitfall 5: Intake exceeds one question | 02-01 | COVERED (Task 1 Intake section + Anti-Patterns) |
| RESEARCH | Known gap: elevation/box-shadow flagging | 02-01 | COVERED (Task 1 Output Rules #5 explicit known-gap list) |
| RESEARCH | Known gap: --page-bg / --text-emphasis | 02-01 | COVERED (Task 1 Output Rules #5 explicit known-gap list) |

**No gaps identified.** All four phase requirement IDs are addressed in this single plan, all eight locked decisions (D-01 through D-08) are implemented, and all five research patterns + five pitfalls are codified in the skill body.

## Vertical Slice Rationale

This phase delivers ONE file. The vertical slice (per MVP_MODE rule) is the entire SKILL.md — splitting it horizontally (e.g., "write frontmatter first, then output rules") would create non-functional intermediate states (a frontmatter-only skill cannot satisfy any requirement). The two tasks here ARE the vertical slice + verification: Task 1 delivers the complete invokable skill; Task 2 confirms real users get real output.

</verification>

<success_criteria>

Plan is complete when:

1. `~/.claude/skills/bf-explore/SKILL.md` exists, is ≥ 80 lines, and passes all grep assertions in Task 1 acceptance criteria
2. Bare `/bf-explore` invocation produces exactly one intake question (Task 2 Step 1)
3. Contextual `/bf-explore` invocation generates without asking the intake question (Task 2 Step 2)
4. Phrases like "Show me 3 HTML layout variations for X" auto-fire the bf-explore skill, not the foundation (Task 2 Step 3)
5. Real generated output contains 2–3 structurally distinct variations with inline `/* token/path */` annotation on every `var()` (Task 2 Step 4)
6. Real generated output contains at least one `/* ⚠️ no token */` gap flag (Task 2 Step 5)
7. User has explicitly approved via Task 2 resume signal

</success_criteria>

<output>
After both tasks complete, create `.planning/phases/02-bf-explore/02-01-SUMMARY.md` documenting:
- Final file contents (path + line count)
- Real /bf-explore invocation result observed during Task 2 verification
- Any deviations from the plan (none expected)
- Confirmation that EXPL-01 through EXPL-04 are satisfied with verification evidence
</output>
