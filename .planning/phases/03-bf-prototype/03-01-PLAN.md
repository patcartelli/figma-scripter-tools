---
phase: 03-bf-prototype
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/bf-prototype/SKILL.md
autonomous: false
requirements: [PROT-01, PROT-02, PROT-03, PROT-04, PROT-05]

must_haves:
  truths:
    - "User can type /bf-prototype and the skill fires immediately"
    - "Skill always asks mode question first (HTML or Vite+MUI) before any generation"
    - "HTML prototype mode delivers a single full-page prototype with inline token annotations on every var()"
    - "Vite+MUI scaffold mode delivers App.tsx + wiring notes with useState interaction states, wrapping pattern, and DATA-03 dual-path output"
    - "Zero hardcoded hex or px literals in any output — gaps are flagged with ⚠️"
    - "Trigger fires on 'prototype this screen', 'build a prototype of X', 'I need a working prototype' — does NOT fire on generic 'build' or 'implement'"
  artifacts:
    - path: "~/.claude/skills/bf-prototype/SKILL.md"
      provides: "bf-prototype command skill"
      contains: "name: bf-prototype"
    - path: "~/.claude/skills/bf-prototype/SKILL.md"
      provides: "@include directive"
      contains: "@~/.claude/skills/bluefish-design-system/SKILL.md"
    - path: "~/.claude/skills/bf-prototype/SKILL.md"
      provides: "Three-path intake gate"
      contains: "Always ask the mode question first"
    - path: "~/.claude/skills/bf-prototype/SKILL.md"
      provides: "HTML prototype output rules"
      contains: "HTML Prototype Mode"
    - path: "~/.claude/skills/bf-prototype/SKILL.md"
      provides: "Vite+MUI scaffold output rules"
      contains: "Vite+MUI Scaffold Mode"
  key_links:
    - from: "description: field"
      to: "Claude Code skill router"
      via: "discriminating phrase 'prototype' / 'working prototype'"
      pattern: "description.*prototype"
    - from: "Intake section"
      to: "generation"
      via: "mode question always first, then optional screen question"
      pattern: "Always ask the mode question first"
    - from: "Figma Context section"
      to: "token values"
      via: "get_variable_defs (authoritative) + get_figma_data (structure)"
      pattern: "get_variable_defs"
---

## Phase Goal

**As a** designer on the Bluefish team, **I want to** type `/bf-prototype` and receive a working prototype — either a quick HTML file or a runnable Vite+MUI app — **so that** I can validate interactions and layout decisions without manual token lookup or build setup.

<objective>
Create `~/.claude/skills/bf-prototype/SKILL.md` — the prototype generation command skill that fires on `/bf-prototype` (or "prototype this screen", "working prototype" language) and delivers either a complete HTML prototype or a runnable Vite+MUI scaffold, both with full Bluefish token compliance.

Purpose: Complete v1 of the Bluefish skill system. Pair with /bf-explore to cover the full design-to-prototype workflow for the team.
Output: `~/.claude/skills/bf-prototype/SKILL.md` (new file, new directory)
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Prior phase analog — read this first to understand the exact structural template -->
@/Users/pcartelli/.claude/skills/bf-explore/SKILL.md

<!-- Foundation — inherited via @include; understand what is already injected so you don't duplicate -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/SKILL.md

<interfaces>
<!-- Key structural facts the executor needs. Extracted from existing files. -->
<!-- DO NOT re-read to "check" — use these directly. -->

From ~/.claude/skills/bf-explore/SKILL.md (structural template):
```
frontmatter: name + description (YAML)
@~/.claude/skills/bluefish-design-system/SKILL.md  ← bare line, immediately after ---
# bf-explore                                        ← skill heading + one paragraph summary
## Support Files — Read On Demand                   ← 4 bullet list, no upfront reads
## Intake                                           ← context-aware gate
## Figma Context (Opportunistic)                    ← MCP tools section
## Style Reference                                  ← reference file pointers
## Output Rules                                     ← numbered generation rules
## Anti-Patterns — Do Not Do These                  ← bulleted pitfalls
```

bf-prototype section order (differs from bf-explore after Figma Context):
1. YAML frontmatter
2. @~/.claude/skills/bluefish-design-system/SKILL.md (bare @include)
3. # bf-prototype heading + one-paragraph summary
4. ## Support Files — Read On Demand
5. ## Intake  (three-path gate — ALWAYS mode question first)
6. ## Figma Context (Opportunistic)  (BOTH get_variable_defs AND get_figma_data)
7. ## HTML Prototype Mode — Output Rules
8. ## Vite+MUI Scaffold Mode — Output Rules
9. ## Anti-Patterns — Do Not Do These

From ~/.claude/skills/bluefish-design-system/SKILL.md (already injected via @include):
- 8-point pre-return checklist (lines 154–168) — DO NOT duplicate in skill body; reference it
- Wrapping pattern: BluefishButton wrapping <Button> — DO NOT re-specify; reference it
- DATA-03 dual-path pattern — DO NOT re-specify; reference it
- alignSelf: 'flex-start' rule — DO NOT re-specify; note it is required and inherited
- Token rules, a11y, Figma MCP setup — all injected; DO NOT duplicate

bf-prototype frontmatter description (exact content per D-07/D-08):
  name: bf-prototype
  description: >
    Generates a Bluefish prototype — either a quick HTML prototype or a runnable Vite+MUI
    single-screen scaffold. Use when the user asks to prototype a screen or interaction, or
    requests a working prototype of a Bluefish component. Not for layout explorations
    (use bf-explore) or production code (use bf-build).

bf-prototype intro paragraph (what to write):
  "Generates a Bluefish prototype in one of two modes: quick HTML prototype (no build step)
  or runnable Vite+MUI single-screen scaffold. Inherits all Bluefish foundation context
  (token rules, accessibility standards, Figma MCP setup, code output standards) from
  `bluefish-design-system` via the `@include` above. This skill owns the prototype
  generation workflow only — intake gate, sub-mode output rules, and interaction state
  requirements."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bf-prototype SKILL.md</name>
  <files>~/.claude/skills/bf-prototype/SKILL.md</files>

  <read_first>
    - ~/.claude/skills/bf-explore/SKILL.md — structural template; read the whole file before writing a single line
    - ~/.claude/skills/bluefish-design-system/SKILL.md — understand what @include already injects so you don't duplicate
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/03-bf-prototype/03-CONTEXT.md — all locked decisions D-01 through D-11
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/03-bf-prototype/03-RESEARCH.md — Pattern 3 through Pattern 7 + Pitfall 1 through Pitfall 5; all code examples
    - /Users/pcartelli/Desktop/figma-scripter-tools/explorations/variation-a.html — nav shell HTML structure and CSS var naming convention (skim :root block + nav markup)
  </read_first>

  <action>
Create directory `~/.claude/skills/bf-prototype/` and write `SKILL.md` following the exact section order below. Each section's required content is specified — do not add sections, do not re-order, do not omit.

---

**Section 1: YAML frontmatter**

Use this exact content (per D-07, D-08):

```yaml
---
name: bf-prototype
description: >
  Generates a Bluefish prototype — either a quick HTML prototype or a runnable Vite+MUI
  single-screen scaffold. Use when the user asks to prototype a screen or interaction, or
  requests a working prototype of a Bluefish component. Not for layout explorations
  (use bf-explore) or production code (use bf-build).
---
```

---

**Section 2: @include (one blank line before and after)**

```
@~/.claude/skills/bluefish-design-system/SKILL.md
```

No XML wrapper. No prefix. Bare line only.

---

**Section 3: Skill heading + summary paragraph**

```markdown
# bf-prototype

Generates a Bluefish prototype in one of two modes: quick HTML prototype (no build step) or runnable Vite+MUI single-screen scaffold. Inherits all Bluefish foundation context (token rules, accessibility standards, Figma MCP setup, code output standards) from `bluefish-design-system` via the `@include` above. This skill owns the prototype generation workflow only — intake gate, sub-mode output rules, and interaction state requirements.
```

---

**Section 4: Support Files — Read On Demand**

Port verbatim from bf-explore SKILL.md (same four files, same "do not read at start" rule).

---

**Section 5: Intake** (per D-01, D-02, D-03)

Write it exactly as follows — the three-path gate MUST be explicit. This is the highest-risk section: do NOT copy the bf-explore intake pattern (which skips intake when context is present). bf-prototype ALWAYS asks the mode question first.

```markdown
## Intake

**Always ask the mode question first** — regardless of whether the user's message included a screen description or component context:

> "Quick HTML prototype or runnable Vite+MUI app?"

**If the user's `/bf-prototype` message included a screen or component description:** After receiving the mode answer, generate immediately. Do not ask what screen.

**If invoked bare** (`/bf-prototype` with no accompanying context): After receiving the mode answer, ask exactly one follow-up:

> "What screen or component are we prototyping?"

Then generate. Do not ask any further questions. Maximum two intake questions total.
```

---

**Section 6: Figma Context (Opportunistic)** (per D-09, D-10, D-11)

bf-prototype differs from bf-explore here — it calls BOTH tools. Write it as follows:

```markdown
## Figma Context (Opportunistic)

If the user has a Figma screen open in Figma Desktop or provides a Figma URL:

1. Call `get_variable_defs` to pull live token values — these are authoritative for the session and take precedence over `tokens.md` for color and scale values
2. Call `get_figma_data` to read component/frame structure
3. If either call triggers a Code Connect prompt: note it inline with `⚠️ Code Connect not configured — proceeding from conversation context` and continue with whatever data was returned before the prompt

If no Figma context is available: generate from conversation description; use `tokens.md` for token values and flag with `/* ⚠️ live token data unavailable — using tokens.md */`.
```

---

**Section 7: HTML Prototype Mode — Output Rules** (per D-04, D-05; PROT-02, PROT-04)

Write a numbered list with these rules. Do not add rules not listed here; do not omit any:

1. **Single prototype**: Deliver one complete, self-contained HTML document. NOT 2-3 variations (that is bf-explore's job). Wrap in a fenced ```html code block. Prefix with `### HTML Prototype: [screen name]`.

2. **Scope — full page with nav shell (per D-04)**: The prototype contains a complete page structure: top header (`<header class="top-header" role="banner">`), body row (`<div class="body">`), nav rail or side nav, and content area. The component being prototyped sits inside this chrome.

3. **`:root` block**: Include a `:root` block defining all CSS custom properties used, with hex fallbacks. This is for standalone rendering — it does NOT substitute for inline token annotation.

4. **Inline token annotation (required on every `var()` usage)**: Every `var()` call in every CSS rule must be followed immediately by an inline comment containing the canonical token path. Exact format: `var(--color-roles-primary-main); /* color-roles/primary/main */` — see Output Rules from bf-explore SKILL.md for the full pattern.

5. **Interaction states — default + hover + selected (per D-05)**: Implement all three states. Active/focus/disabled may be added if relevant to the component. Implementation:
   - `hover` state: CSS `:hover` pseudo-class
   - `selected` state: CSS class (e.g., `.active`) toggled by a minimal inline `<script>` block at the bottom of `<body>`. No external JS dependencies. Pattern:
     ```html
     <script>
       document.querySelectorAll('.kpi-cell').forEach(cell => {
         cell.addEventListener('click', () => {
           document.querySelectorAll('.kpi-cell').forEach(c => c.classList.remove('active'));
           cell.classList.add('active');
         });
       });
     </script>
     ```

6. **Selected-item background sizing**: Apply `align-self: flex-start` on any element with a selected-state background fill (nav items, tabs, KPI cells with active states). Never `width: 100%` on a selected background. This is inherited from the foundation — it is required.

7. **Gap flagging**: Any value without a clean token path must be flagged inline. Known automatic gaps:
   - All `box-shadow` values → `/* ⚠️ no token — elevation undefined */`
   - Hover state if using raw opacity (e.g., `rgba(41,41,41,0.04)`) → use `color-roles/action/hover` token instead; if unavailable, flag: `/* ⚠️ no token — action/hover state layer TBD */`
   - Reference-file vars without token paths: use `color-roles/background/default` instead of `--page-bg`; use `color-roles/text/primary` instead of `--text-emphasis`

8. **CSS variable naming convention**: Use shortened CSS var names matching the reference files: `--radius-sm`, `--radius-md`, `--radius-full` (not `--scale-radius-*`). The canonical token path in the inline comment still uses the full `scale/radius/sm` form.

9. **Accessibility**: Apply ARIA roles, labels, and `aria-current="page"` patterns. Required minimum: `role="banner"`, `role="navigation"` with `aria-label`, `aria-current="page"` on the active nav item, `aria-label` on all icon-only buttons.

10. **Token compliance pre-return check**: Before returning output, run the foundation's 8-point pre-return checklist internally. Items 7 (type imports) and 8 (screenshot comparison) are N/A for HTML mode — apply items 1–6.

---

**Section 8: Vite+MUI Scaffold Mode — Output Rules** (per D-05, D-06; PROT-03, PROT-04)

Write a numbered list with these rules. Do not add rules not listed here; do not omit any:

1. **Deliverable — App.tsx first**: The primary output is the complete `App.tsx` file content as a fenced ```tsx code block. This is the full single-screen component tree.

2. **Scope — single screen, no routing (per D-06)**: One `App.tsx` with the full screen. No React Router, no multi-page setup. Routing is out of scope for prototypes.

3. **Additional wiring output**: After the `App.tsx` block, include two additional fenced blocks with the minimum required wiring for `npm run dev` to work:
   - `main.tsx`: ThemeProvider + CssBaseline wrapping App. Include `import CssBaseline from '@mui/material/CssBaseline'` and `<CssBaseline />` inside the ThemeProvider.
   - `index.html` entry point excerpt: show the DM Sans font link tag. Exact tags:
     ```html
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&display=swap" rel="stylesheet">
     ```
   Label each block clearly: `// main.tsx` and `<!-- index.html (font wiring only) -->`.

4. **MUI wrapping pattern (from foundation)**: Create named custom components that wrap MUI components internally. Example: `function BluefishButton(props) { return <Button {...props} /> }`. Do not use raw MUI components unwrapped. Do not apply Bluefish tokens directly as sx props on raw MUI components.

5. **useState for interaction states (per D-05)**: Use `useState` for all interactive states — nav selection, KPI cell selection, tab selection. Default + hover + selected required. Pattern:
   ```tsx
   const [selectedNav, setSelectedNav] = useState<string>('ai-insights');
   // In render:
   <ListItemButton
     selected={selectedNav === 'ai-insights'}
     onClick={() => setSelectedNav('ai-insights')}
     sx={{ alignSelf: 'flex-start', borderRadius: '9999px' }}
   >
   ```

6. **alignSelf: 'flex-start' on selected backgrounds**: Required on any component whose selected state renders a background fill. This is a foundation-inherited rule — do not omit it.

7. **DATA-03 dual-path token output**: For every token path reference in sx props, output both forms with the ⚠️ flag:
   ```tsx
   // If tokens inject as MUI theme extensions:
   sx={{ color: theme.palette.primary.main }} // color-roles/primary/main
   // If tokens inject as CSS custom properties:
   sx={{ color: 'var(--color-roles-primary-main)' }} // color-roles/primary/main
   /* ⚠️ token injection method unconfirmed — verify with eng */
   ```

8. **Type-only MUI imports**: MUI type exports MUST use `import type {}`. Vite throws a runtime SyntaxError if they are imported as values. Required pattern:
   - CORRECT: `import Select, { type SelectChangeEvent } from '@mui/material/Select'`
   - INCORRECT: `import Select, { SelectChangeEvent } from '@mui/material/Select'`

9. **DM Sans font**: Must be loaded. Include the Google Fonts link in `index.html` (shown in rule 3) or via CSS `@import`. Apply to `body` via MUI theme typography: `fontFamily: "'DM Sans', system-ui, sans-serif"`.

10. **MUI reset styles**: Include `<CssBaseline />` inside the ThemeProvider in `main.tsx`. This is required — do not omit.

11. **Gap flagging**: Apply the same ⚠️ gap flagging rules as HTML mode. Additionally: when no clear MUI equivalent exists for a Figma component, flag inline: `/* ⚠️ no MUI equivalent for [name] — custom implementation required */`

12. **Token compliance pre-return check**: Before returning output, run the full 8-point pre-return checklist from the foundation. All 8 items apply to Vite+MUI mode.

---

**Section 9: Anti-Patterns — Do Not Do These**

Write a bulleted list covering ALL of these pitfalls (derived from 03-RESEARCH.md anti-patterns section). Each bullet must be specific — name the wrong behavior and why:

- **Mode question skipped when context is provided**: bf-prototype ALWAYS asks the mode question first, even when the user's message includes a screen description. Do not copy the bf-explore pattern (which skips intake when context is present). Skipping the mode question collapses D-01.
- **2–3 variations instead of one**: HTML mode output is a SINGLE prototype. Multiple variations are the job of bf-explore. Generating 2-3 is the wrong skill behavior.
- **`get_figma_data` only in Figma Context**: bf-prototype requires BOTH `get_variable_defs` (live token values — authoritative) AND `get_figma_data` (component structure). Using only `get_figma_data` omits the authoritative token source.
- **Missing `alignSelf: 'flex-start'` on selected backgrounds**: Any nav item, tab, or KPI cell with a selected-state background fill must use `alignSelf: 'flex-start'`. Omitting it causes the background to stretch to fill the container.
- **MUI type-only exports imported as values**: Using `import { SelectChangeEvent }` instead of `import { type SelectChangeEvent }` causes a Vite runtime SyntaxError. Always use `import type {}` for MUI type exports.
- **DATA-03 single-path output**: Outputting only the MUI theme extension form OR only the CSS custom property form is wrong. Both forms are required with the `⚠️ token injection method unconfirmed` flag.
- **Trigger over-broadening on "build" / "implement" / "create"**: These phrases collide with Phase 5 `/bf-build`. The discriminating trigger anchors are "prototype" and "working prototype" — not generic task verbs.
- **Hardcoded hex without `⚠️` flag**: Any hex value in output without a token must carry a `/* ⚠️ no token — [reason] */` comment. Zero bare hex literals allowed.
- **Discarding partial MCP results**: If `get_variable_defs` or `get_figma_data` returns data before a Code Connect prompt, use what was returned — do not abandon the response.
  </action>

  <verify>
    <automated>grep -c 'name: bf-prototype' ~/.claude/skills/bf-prototype/SKILL.md && grep -c '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-prototype/SKILL.md && grep -c 'Always ask the mode question first' ~/.claude/skills/bf-prototype/SKILL.md && grep -c 'get_variable_defs' ~/.claude/skills/bf-prototype/SKILL.md && grep -c 'HTML Prototype Mode' ~/.claude/skills/bf-prototype/SKILL.md && grep -c 'Vite+MUI Scaffold Mode' ~/.claude/skills/bf-prototype/SKILL.md && grep -c 'prototype' ~/.claude/skills/bf-prototype/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `~/.claude/skills/bf-prototype/SKILL.md`
    - `grep -c 'name: bf-prototype' ~/.claude/skills/bf-prototype/SKILL.md` returns 1
    - `grep -c '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-prototype/SKILL.md` returns 1
    - `grep -c 'Always ask the mode question first' ~/.claude/skills/bf-prototype/SKILL.md` returns 1
    - `grep -c 'get_variable_defs' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'get_figma_data' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'HTML Prototype Mode' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'Vite+MUI Scaffold Mode' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'align-self: flex-start\|alignSelf.*flex-start' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'import type' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'DATA-03\|token injection method unconfirmed' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'build\|implement' ~/.claude/skills/bf-prototype/SKILL.md` — any match must be INSIDE the "do not use" anti-pattern or negative-scoping context (check manually)
    - description field does NOT contain "build" or "implement" as standalone trigger phrases: `grep -A5 'description:' ~/.claude/skills/bf-prototype/SKILL.md | grep -v 'bf-build\|Not for\|use bf-build' | grep -c '\bbuild\b\|\bimplement\b'` returns 0
    - `grep -c 'CssBaseline' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c 'DM Sans' ~/.claude/skills/bf-prototype/SKILL.md` returns at least 1
  </acceptance_criteria>

  <done>
    `~/.claude/skills/bf-prototype/SKILL.md` exists with all nine sections in correct order:
    frontmatter → @include → heading → support files → intake (three-path gate) → figma context (both MCP tools) → HTML output rules → Vite+MUI output rules → anti-patterns.
    All 15 acceptance criteria pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify bf-prototype skill</name>

  <read_first>
    - ~/.claude/skills/bf-prototype/SKILL.md — read the completed file before presenting verification steps
  </read_first>

  <what-built>
    `~/.claude/skills/bf-prototype/SKILL.md` — the /bf-prototype command skill with two output modes (HTML prototype and Vite+MUI scaffold), a three-path intake gate, and full Bluefish token compliance rules.
  </what-built>

  <how-to-verify>
    Test these five scenarios in Claude Code (open a new conversation to pick up the updated skill file):

    1. **Bare invocation**: Type `/bf-prototype` with nothing else. Expected: Claude asks "Quick HTML prototype or runnable Vite+MUI app?" and nothing else. After answering the mode question: Claude asks "What screen or component are we prototyping?" — exactly one follow-up, then generates.

    2. **Invocation with context**: Type `/bf-prototype the AI Insights overview screen with KPI strip and chart`. Expected: Claude asks only "Quick HTML prototype or runnable Vite+MUI app?" — no second question. After answering, generates immediately.

    3. **Auto-trigger (no slash command)**: Type "I need a working prototype of the campaign list screen". Expected: bf-prototype skill fires (check the system reminder shows `bf-prototype` in active skills, or observe that Claude follows the bf-prototype workflow including the mode question).

    4. **HTML mode output quality**: Answer "HTML prototype" and provide a simple screen description. Verify: (a) output is a SINGLE prototype, not 2-3 variations; (b) every `var()` in CSS has an inline `/* token/path */` comment; (c) nav shell present with header + nav + content area; (d) at least one interaction state (hover or selected) is present; (e) no bare hex values outside ⚠️-flagged gaps.

    5. **Vite+MUI mode output**: Answer "Vite+MUI app" and provide a simple screen description. Verify: (a) App.tsx block present as primary output; (b) main.tsx and index.html wiring blocks present; (c) DATA-03 dual-path comment present on at least one token reference; (d) `import type` used for any MUI type exports if present; (e) `alignSelf: 'flex-start'` present on any selected-state nav/tab element.
  </how-to-verify>

  <acceptance_criteria>
    - Bare invocation: mode question asked first, screen question asked second, no additional questions
    - Context invocation: mode question only (no screen question), generation follows immediately
    - Auto-trigger: bf-prototype workflow fires without explicit /bf-prototype slash command
    - HTML mode: single prototype output, all var() annotated, nav shell present, interaction states present
    - Vite+MUI mode: App.tsx + main.tsx + index.html blocks, DATA-03 flag present, import type used correctly
  </acceptance_criteria>

  <resume-signal>Type "approved" to complete the phase, or describe any issues so they can be fixed before sign-off.</resume-signal>

  <done>User types "approved" after all five verification steps pass. PROT-01 through PROT-05 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This phase creates a SKILL.md file only. No authentication, secrets, API calls, or user data. No trust boundaries cross this artifact. |

## STRIDE Threat Register

Security domain: N/A for this phase. Creating a skill instruction file introduces no authentication, data persistence, or network surface. ASVS L1 categories V4 (access control) and V5 (input validation) are not applicable. No threats to register.
</threat_model>

<verification>
After task 2 approval:

```bash
# Confirm skill file exists and contains all required sections
grep -c 'name: bf-prototype' ~/.claude/skills/bf-prototype/SKILL.md
grep -c '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-prototype/SKILL.md
grep -c 'Always ask the mode question first' ~/.claude/skills/bf-prototype/SKILL.md
grep -c 'HTML Prototype Mode' ~/.claude/skills/bf-prototype/SKILL.md
grep -c 'Vite+MUI Scaffold Mode' ~/.claude/skills/bf-prototype/SKILL.md
grep -c 'get_variable_defs' ~/.claude/skills/bf-prototype/SKILL.md
grep -c 'Anti-Patterns' ~/.claude/skills/bf-prototype/SKILL.md
```

All commands should return 1 or more.

Phase success criteria (from ROADMAP.md):
1. `/bf-prototype` skill exists and is invocable via slash command
2. HTML prototype mode delivers a working, styled prototype in one session with correct token usage
3. Vite+MUI mode delivers a runnable prototype (`npm run dev`) with interaction states and MUI wrapping pattern applied
4. Token compliance check: grep output for hex patterns — zero matches outside of `⚠️` flagged exceptions
</verification>

<success_criteria>
- `~/.claude/skills/bf-prototype/SKILL.md` exists with all nine sections
- All 15 acceptance criteria from Task 1 pass via grep
- Human verification step approves all five test scenarios
- PROT-01 through PROT-05 satisfied
- Phase 3 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-bf-prototype/03-01-SUMMARY.md` using the template at `@/Users/pcartelli/.claude/get-shit-done/templates/summary.md`.
</output>
