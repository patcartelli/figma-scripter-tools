---
phase: 04-bf-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/bluefish-design-system/SKILL.md
  - ~/.claude/skills/bf-spec/
autonomous: true
requirements: [SPEC-02]

must_haves:
  truths:
    - "The bf-spec/ directory exists and is writable"
    - "The foundation skill's description field does not advertise 'spec' as a primary trigger anchor"
    - "After this plan, typing /bf-spec will not be intercepted by bluefish-design-system foundation"
  artifacts:
    - path: "~/.claude/skills/bf-spec/"
      provides: "Output directory for the bf-spec command skill"
    - path: "~/.claude/skills/bluefish-design-system/SKILL.md"
      provides: "Foundation skill with description trigger anchors that exclude /bf-spec routing"
      contains: "description:"
  key_links:
    - from: "bluefish-design-system/SKILL.md description field"
      to: "Claude Code skill router"
      via: "trigger phrase exclusions (explore, prototype, spec, build)"
      pattern: "not a specific explore, prototype, spec, or build task"
    - from: "~/.claude/skills/bf-spec/ directory"
      to: "Plan 02 (writes SKILL.md into this directory)"
      via: "filesystem location"
      pattern: "skills/bf-spec/"
---

<objective>
Pre-work for the bf-spec skill: confirm the foundation `bluefish-design-system/SKILL.md` description trigger phrasing does not intercept `/bf-spec` routing, and create the empty `~/.claude/skills/bf-spec/` directory so Plan 02 can write the SKILL.md file.

Purpose: ROADMAP Phase 4 lists a pre-work item — "Update `bluefish-design-system` foundation description to exclude 'spec' and 'build' triggers before Phase 4 testing begins". The 04-PATTERNS.md analysis found the current description already includes "not a specific explore, prototype, spec, or build task" exclusion language. This plan verifies that exclusion is intact (or fixes it if drift occurred since 04-PATTERNS.md was generated) and creates the new skill directory.

Output: `~/.claude/skills/bf-spec/` directory exists; foundation `description:` field verified to contain "not a specific explore, prototype, spec, or build task" or equivalently narrow exclusion.
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 4 inputs the executor MUST read before touching the foundation file -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-RESEARCH.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-PATTERNS.md

<!-- Foundation file being inspected/edited -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/SKILL.md

<interfaces>
<!-- Current foundation description (lines 2-8) — extracted from bluefish-design-system/SKILL.md -->
<!-- The executor must NOT broaden this description, only confirm or narrow it. -->

The current `description:` field reads (verbatim, from bluefish-design-system/SKILL.md lines 2-8):

```
description: >
  Bluefish design system reference. Use for token lookups, accessibility rules,
  Figma variable reading, component conventions, and code output standards.
  Activates for any question about the Bluefish design system that is not a
  specific explore, prototype, spec, or build task.
```

Key facts:
- The phrase "not a specific explore, prototype, spec, or build task" is the exclusion clause that routes /bf-spec and /bf-build away from this foundation skill.
- bf-explore and bf-prototype skills already exist alongside this foundation and routing has worked.
- 04-PATTERNS.md confirms: "No content change required if the routing test passes; if /bf-spec intercepts, narrow the exclusion language."
- The bf-spec skill directory (`~/.claude/skills/bf-spec/`) does NOT exist yet — RESEARCH.md Environment Availability table confirms this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm foundation description excludes spec trigger and create bf-spec directory</name>
  <files>
    ~/.claude/skills/bluefish-design-system/SKILL.md,
    ~/.claude/skills/bf-spec/
  </files>

  <read_first>
    - /Users/pcartelli/.claude/skills/bluefish-design-system/SKILL.md — read lines 1-15 to confirm current `description:` field text before deciding whether to edit
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-PATTERNS.md — section "`~/.claude/skills/bluefish-design-system/SKILL.md` (pre-work edit, description field only)" for guidance on whether content change is required
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md — `<canonical_refs>` section confirms which file is the foundation
  </read_first>

  <action>
Step 1 — Confirm foundation description exclusion clause.

Read `~/.claude/skills/bluefish-design-system/SKILL.md` lines 1-15. The `description:` YAML field MUST contain the exact exclusion clause `not a specific explore, prototype, spec, or build task` (case-sensitive, exact wording, exact comma placement).

If the exclusion clause is present verbatim: do NOT edit the file. No content change is required (per 04-PATTERNS.md: "No content change required if the routing test passes").

If the exclusion clause has drifted (any word changed, comma removed, or "spec" or "build" missing from the list): edit ONLY the description field to restore exactly this clause: `Activates for any question about the Bluefish design system that is not a specific explore, prototype, spec, or build task.` Do not modify any other line in the file. Do not add new lines. Do not change the frontmatter `name:` field.

Step 2 — Create the bf-spec skill directory.

Run `mkdir -p ~/.claude/skills/bf-spec/` to create the empty directory. The bf-spec directory is a symlink target in the live skills directory; Plan 02 will write `SKILL.md` into this directory. If the directory already exists (because of a prior aborted Phase 4 attempt), `mkdir -p` is a no-op and that is acceptable.

Do NOT create any files inside `~/.claude/skills/bf-spec/` in this plan. Plan 02 is responsible for writing `SKILL.md`.

Step 3 — Verify the result.

The foundation file `bluefish-design-system/SKILL.md` ends with the exclusion clause intact and the bf-spec directory exists. Do not introduce any other changes anywhere on the filesystem.
  </action>

  <verify>
    <automated>grep -F 'not a specific explore, prototype, spec, or build task' ~/.claude/skills/bluefish-design-system/SKILL.md && test -d ~/.claude/skills/bf-spec/ && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - Source: `grep -F 'not a specific explore, prototype, spec, or build task' ~/.claude/skills/bluefish-design-system/SKILL.md` returns exactly 1 matching line
    - Source: `grep -c '^name: bluefish-design-system' ~/.claude/skills/bluefish-design-system/SKILL.md` returns 1 (foundation name field intact)
    - Source: `grep -cE '^description: >' ~/.claude/skills/bluefish-design-system/SKILL.md` returns 1 (single description field)
    - Filesystem: `test -d ~/.claude/skills/bf-spec/` exits 0
    - Filesystem: `ls ~/.claude/skills/bf-spec/ | wc -l` returns 0 (directory empty — no files written in this plan)
    - Behavior: `git diff --stat ~/.claude/skills/bluefish-design-system/SKILL.md` shows either zero changes (clause already correct) OR a change touching ONLY the description field lines (no edits to other sections)
    - Source: `wc -l ~/.claude/skills/bluefish-design-system/SKILL.md` reports the same line count before and after (or +/- 0 if no edit was made; the exclusion clause edit is a single-line replacement)
  </acceptance_criteria>

  <done>
    Foundation description verified to contain the exclusion clause `not a specific explore, prototype, spec, or build task` exactly; `~/.claude/skills/bf-spec/` directory exists and is empty. No other files touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan reads a Markdown skill file and creates an empty directory. No authentication, network, secrets, or user input handling. No trust boundaries cross this artifact. |

## STRIDE Threat Register

Security domain: N/A for this plan. The deliverable is (a) a no-op or single-line edit to a local Markdown file, and (b) `mkdir -p` of a new empty directory. ASVS L1 categories V4 (access control) and V5 (input validation) are not applicable. No threats to register.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | bluefish-design-system/SKILL.md content drift from broader edit | mitigate | Acceptance criteria require `git diff --stat` to show changes ONLY to description lines; line count must match |
</threat_model>

<verification>
After task 1 completes:

```bash
# Confirm exclusion clause intact
grep -F 'not a specific explore, prototype, spec, or build task' ~/.claude/skills/bluefish-design-system/SKILL.md

# Confirm directory created
test -d ~/.claude/skills/bf-spec/ && echo "directory exists"

# Confirm directory empty (Plan 02 will populate)
ls ~/.claude/skills/bf-spec/

# Confirm no unintended foundation edits
git diff --stat ~/.claude/skills/bluefish-design-system/SKILL.md
```

ROADMAP Phase 4 pre-work item: "Update `bluefish-design-system` foundation description to exclude 'spec' and 'build' triggers before Phase 4 testing begins" — addressed by this plan.
</verification>

<success_criteria>
- Foundation `description:` field exclusion clause confirmed (or restored to exact wording)
- `~/.claude/skills/bf-spec/` directory exists and is empty
- No unintended edits to other files
- Plan 02 can proceed to write `SKILL.md` into the new directory
</success_criteria>

<output>
After completion, create `.planning/phases/04-bf-spec/04-01-SUMMARY.md` using the template at `@/Users/pcartelli/.claude/get-shit-done/templates/summary.md`.
</output>
