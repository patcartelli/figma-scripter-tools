---
phase: 04-bf-spec
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - ~/.claude/skills/bf-spec/SKILL.md
autonomous: true
requirements: [SPEC-01, SPEC-02]

must_haves:
  truths:
    - "User can type /bf-spec and the skill fires immediately"
    - "Skill infers scope (screen vs. component mode) from the Figma selection returned by get_metadata — does NOT ask a mode question"
    - "Skill executes the MCP sequence in exact order: get_metadata first, get_variable_defs second, get_design_context per component third"
    - "Bare invocation (`/bf-spec` with no context) triggers exactly one clarifying question: 'What screen or component are you speccing?'"
    - "Screen mode produces one file `spec-[screen-name].md` containing a `## Component Inventory` table followed by `## [Component Name]` sections"
    - "Component mode produces one file `spec-[component-name].md` matching spec-template.md format exactly"
    - "Spec files are written to current working directory (./), not to ~/.claude/skills/bluefish-design-system/"
    - "Code Connect prompts at any level (top-level or sub-component) emit ⚠️ flag and continue — output is never blocked"
    - "Sub-components matching a figma-reading-guide.md Step 3 category are inlined as `### Sub-component: [Name]` under their parent's `## [Component Name]` section"
    - "Trigger description does NOT advertise 'build' or 'implement' as triggers — these belong to bf-build"
  artifacts:
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "bf-spec command skill file"
      contains: "name: bf-spec"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "@include directive inheriting bluefish-design-system foundation"
      contains: "@~/.claude/skills/bluefish-design-system/SKILL.md"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Support Files — Read On Demand section"
      contains: "Support Files — Read On Demand"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Intake gate with scope inference (no mode question)"
      contains: "Do not ask a mode question"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Figma Context section with three-tool MCP sequence"
      contains: "Figma Context — Always Attempt First"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Screen Mode Output Rules"
      contains: "Screen Mode — Output Rules"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Component Mode Output Rules"
      contains: "Component Mode — Output Rules"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Sub-Component Recursion Rules"
      contains: "Sub-Component Recursion Rules"
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "Anti-Patterns section listing bf-spec-specific pitfalls"
      contains: "Anti-Patterns — Do Not Do These"
  key_links:
    - from: "description: field"
      to: "Claude Code skill router"
      via: "discriminating phrases 'spec', 'handoff doc', 'engineering spec', 'component inventory'"
      pattern: "description.*(spec|handoff|component inventory)"
    - from: "Figma Context section"
      to: "MCP tools"
      via: "ordered sequence: get_metadata → get_variable_defs → get_design_context per component"
      pattern: "get_metadata.*get_variable_defs.*get_design_context"
    - from: "Screen Mode Output Rules"
      to: "spec output file"
      via: "Component Inventory table + per-component sections + spec-template.md Field Mapping"
      pattern: "Component Inventory"
    - from: "Sub-Component Recursion Rules"
      to: "per-component spec sections"
      via: "`### Sub-component: [Name]` heading nested inside parent `## [Component Name]`"
      pattern: "### Sub-component:"
---

## Phase Goal

**As a** designer or engineer on the Bluefish team, **I want to** type `/bf-spec` on any Figma screen or component and receive a structured engineering handoff doc — component inventory, token table, interaction notes, and accessibility section — **so that** I can hand a spec to engineering without manually transcribing tokens, props, or component structure from Figma.

<objective>
Create `~/.claude/skills/bf-spec/SKILL.md` — the spec generation command skill that fires on `/bf-spec` (or "spec this screen", "handoff doc", "engineering spec", "component inventory" language) and delivers either a screen-mode spec doc (component inventory + per-component sections in one file) or a component-mode spec doc (single component spec). The skill infers which mode from the Figma selection rather than asking — bf-spec does NOT use the mode-question pattern from bf-prototype.

Purpose: Deliver requirement SPEC-01 and SPEC-02 of milestone v1.1. The spec output (component mode) is the input format that Phase 5 `/bf-build` Path B reads — so the file shape produced here is a contract Phase 5 depends on. The structural template is `bf-prototype/SKILL.md` (already verified Phase 3); the per-component output rule is the Field Mapping table in `spec-template.md`; the sub-component recursion trigger list is `figma-reading-guide.md` Step 3.

Output: `~/.claude/skills/bf-spec/SKILL.md` (new file, written into directory created by Plan 01).
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 4 source artifacts — read first, in order -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-RESEARCH.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-PATTERNS.md

<!-- Plan 01 summary — confirms pre-work complete -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-01-SUMMARY.md

<!-- Direct structural analog — copy structure, adapt content -->
@/Users/pcartelli/.claude/skills/bf-prototype/SKILL.md

<!-- Foundation — inherited via @include; do not duplicate its content in the new skill -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/SKILL.md

<!-- Output format authority — the Field Mapping table is referenced from the new skill -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-template.md

<!-- Sub-component recursion trigger list (Step 3) -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/figma-reading-guide.md

<!-- Quality bar references for component mode output -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-button.md
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-autocomplete.md

<interfaces>
<!-- Key structural facts the executor needs. Extracted from existing files. -->
<!-- DO NOT re-read these files to "check" — use these directly. -->

bf-prototype section order (lines 1-196, the structural template to adapt):
1. YAML frontmatter (lines 1-8): `name: bf-prototype` + `description: >` multi-line block
2. `@~/.claude/skills/bluefish-design-system/SKILL.md` (line 10, bare line, no XML wrapper)
3. `# bf-prototype` heading + one-paragraph summary (lines 12-14)
4. `## Support Files — Read On Demand` (lines 16-23, four bullets)
5. `## Intake` (lines 25-37, three-path gate — bf-prototype ALWAYS asks mode question first)
6. `## Figma Context — Always Attempt First` (lines 39-67, MCP sequence + fallback diagnostic)
7. `## HTML Prototype Mode — Output Rules` (numbered list)
8. `## Vite+MUI Scaffold Mode — Output Rules` (numbered list)
9. `## Anti-Patterns — Do Not Do These` (bullet list)

bf-spec section order MUST be:
1. YAML frontmatter (different name + different description text — see below)
2. `@~/.claude/skills/bluefish-design-system/SKILL.md` (IDENTICAL bare line)
3. `# bf-spec` heading + adapted summary paragraph
4. `## Support Files — Read On Demand` (same four bullets, one extension noted in 04-PATTERNS.md)
5. `## Intake` (scope-inference version — does NOT ask a mode question; D-01, D-05)
6. `## Figma Context — Always Attempt First` (three-tool sequence: get_metadata → get_variable_defs → get_design_context per component; D-06, D-08, D-09, D-10)
7. `## Screen Mode — Output Rules` (Component Inventory table + per-component sections; D-02)
8. `## Component Mode — Output Rules` (single-component file matching spec-template.md; D-03)
9. `## Sub-Component Recursion Rules` (D-11, D-12, D-13)
10. `## Anti-Patterns — Do Not Do These` (10 items from 04-RESEARCH.md)

bf-spec frontmatter description (verbatim — from 04-RESEARCH.md "Canonical Frontmatter + @include" code example):
  name: bf-spec
  description: >
    Generates a structured engineering handoff spec from a Figma screen or component.
    Use when the user types /bf-spec or asks for a spec, engineering handoff doc, or
    component inventory for a Bluefish screen or component. Not for prototypes (use
    bf-prototype), layout explorations (use bf-explore), or production code (use bf-build).

bf-spec summary paragraph (verbatim from 04-PATTERNS.md "Purpose paragraph pattern"):
  "Generates a structured engineering handoff spec for a Bluefish screen or component. Operates
  in two modes inferred from the Figma selection: screen mode (component inventory + per-component
  sections in one file) or component mode (single component spec file). Inherits all Bluefish
  foundation context (token rules, accessibility standards, Figma MCP setup, component map) from
  `bluefish-design-system` via the `@include` above. This skill owns the spec generation workflow
  only — scope inference, MCP sequence, output rules per mode, and sub-component recursion."

Foundation file (already injected via @include — DO NOT duplicate):
- 8-point pre-return checklist (foundation SKILL.md lines 154-168) — REFERENCE only, do not re-list
- Token rules (rules 1-10) — REFERENCE only
- Live token grounding workflow (`get_variable_defs` precedence over tokens.md) — REFERENCE only
- Warning flag vocabulary — REFERENCE only

spec-template.md Field Mapping table (lines 63-87) — the authoritative spec field → get_design_context source translation. The new skill's Screen Mode and Component Mode sections must REFERENCE this table by file path, not re-derive the mapping.

Sub-component recursion trigger list (from figma-reading-guide.md Step 3 table, lines 32-39):
- Navigation: Nav, Navigation, Nav Rail, Header, Menu, Tab, Search
- Buttons & Capsules: Button, Btn, CTA, Capsule, Badge, Tag, Chip
- Forms & Controls: Input, Select, Dropdown, Checkbox, Radio, Toggle, Switch, Date, Control
- Data & Charts: Chart, Graph, Bar, Line, Pie, Donut, Sparkline
- Feedback: Alert, Toast, Banner, Message, Notification, Tooltip
- Layout: Card, Panel, Container, Modal, Drawer, Sheet

The new skill's recursion section must REFERENCE figma-reading-guide.md Step 3 by file path — do not re-list the entire trigger table inline.

Warning flag vocabulary — exact strings (from 04-PATTERNS.md "Warning Flag Vocabulary" table, must be used verbatim wherever the situation arises):
- Code Connect (top-level): `⚠️ Code Connect not configured — proceeding from conversation context`
- Code Connect (sub-component): `⚠️ Code Connect not configured for [sub-component] — proceeding from conversation context`
- MCP unavailable: `⚠️ Figma MCP unavailable — tokens from ~/.claude/skills/bluefish-design-system/{tokens.md, type-styles.md, tokens-dataviz.md}; live values not verified`
- Custom layout sub-component: `⚠️ custom layout — no Bluefish component match`
- Missing category: `⚠️ component type unclear — [name] — needs categorization`
- Token drift: `⚠️ token drift — using live Figma value`
- No token: `⚠️ no token for [value] — needs token`

MCP unavailable diagnostic for spec output (Markdown, not HTML/TSX):
The bf-prototype skill emits the MCP-unavailable diagnostic as `<!-- -->` (HTML) or `{/* */}` (JSX) — bf-spec output is Markdown, so the diagnostic is emitted as a blockquote at the top of the spec file:

  > ⚠️ Figma MCP unavailable — tokens from ~/.claude/skills/bluefish-design-system/{tokens.md, type-styles.md, tokens-dataviz.md}; live values not verified
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write bf-spec SKILL.md (10 sections, exact order)</name>
  <files>~/.claude/skills/bf-spec/SKILL.md</files>

  <read_first>
    - ~/.claude/skills/bf-prototype/SKILL.md — read the WHOLE file before writing a single line; this is the structural template
    - ~/.claude/skills/bluefish-design-system/SKILL.md — understand what @include already injects so you don't duplicate
    - ~/.claude/skills/bluefish-design-system/spec-template.md — the Field Mapping table is what the new skill's output rules reference
    - ~/.claude/skills/bluefish-design-system/figma-reading-guide.md — Step 3 governs sub-component recursion triggers
    - ~/.claude/skills/bluefish-design-system/spec-button.md — quality bar for component mode output
    - ~/.claude/skills/bluefish-design-system/spec-autocomplete.md — composite component quality bar (shows sub-component pattern)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md — all locked decisions D-01 through D-13
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-RESEARCH.md — Architecture Patterns 1-8, Pitfalls 1-6, Anti-Patterns to Avoid section
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-PATTERNS.md — exact verbatim copy targets for frontmatter, summary paragraph, intake section, and Figma Context section
  </read_first>

  <action>
Write `~/.claude/skills/bf-spec/SKILL.md` containing exactly 10 sections in the order specified below. The directory was created by Plan 01 — write the file directly into it. Do not create any other files. Do not modify any other file.

Use these verbatim strings where indicated. Where structure is described, follow the structure of `bf-prototype/SKILL.md` but substitute spec-appropriate content.

---

**Section 1: YAML frontmatter (verbatim)**

The frontmatter MUST contain exactly the `name:` and `description:` text from the interfaces block above. Use the verbatim 4-line `description: >` multi-line value. Do not change any wording. The closing `---` ends the frontmatter.

---

**Section 2: @include line (verbatim, one blank line before and after)**

Insert the line `@~/.claude/skills/bluefish-design-system/SKILL.md` exactly once, immediately after the frontmatter's closing `---`, with no XML wrapper and no prefix. This is identical to bf-prototype line 10 and bf-explore line 10.

---

**Section 3: `# bf-spec` heading + summary paragraph (verbatim)**

Use the heading `# bf-spec` followed by a blank line and then the verbatim summary paragraph from the interfaces block above. Do not add additional sentences.

---

**Section 4: `## Support Files — Read On Demand`**

Copy the structure of bf-prototype lines 16-23 (the heading line, the "Do not read these at skill start. Read them only when the task requires it:" instruction, the four bullets). Use the same four files (tokens.md, type-styles.md, tokens-dataviz.md, figma-reading-guide.md), but adjust the trigger phrases for spec context:
- `tokens.md` trigger: "when resolving color, spacing, or radius token paths"
- `type-styles.md` trigger: "when populating the Type Styles section of a spec"
- `tokens-dataviz.md` trigger: "when speccing dataviz components"
- `figma-reading-guide.md` trigger: "when interpreting Figma MCP output or inferring component categories" (the "or inferring component categories" extension is the bf-spec-specific addition — see 04-PATTERNS.md Support Files section)

---

**Section 5: `## Intake` (scope-inference version — does NOT ask a mode question)**

This section MUST encode D-01 (scope inferred from Figma selection, not asked) and D-05 (bare-invoke triggers exactly one clarifying question). Write it as four blocks:

Block 1 (must appear first as a strong directive):
"**Do not ask a mode question.** bf-spec infers screen mode vs. component mode from the Figma selection returned by `get_metadata` — not from user input."

Block 2 (bare-invoke gate, per D-05):
"**If invoked bare** (`/bf-spec` with no accompanying context): ask exactly one question before proceeding to MCP calls:" followed by a blockquote `> "What screen or component are you speccing?"`

Block 3 (bare get_metadata gate, per RESEARCH.md Pitfall 3):
"**If `get_metadata` returns empty or nil AND no prior conversation context:** ask the same one question before proceeding. Do not attempt to generate a spec for an empty selection."

Block 4 (context-present, per D-06):
"**If the user's message included a screen or component description AND `get_metadata` returns meaningful context:** proceed to MCP sequence immediately. Do not ask any questions."

End the section with: "Maximum one intake question total."

Do NOT include any wording resembling bf-prototype's "Always ask the mode question first" — that is the explicit anti-pattern for bf-spec.

---

**Section 6: `## Figma Context — Always Attempt First` (three-tool MCP sequence)**

This section encodes D-06, D-08, D-09, D-10 and matches 04-RESEARCH.md "MCP Sequence — bf-spec Order" code example.

Open with a strong directive: "**Before generating any output**, always attempt Figma MCP calls in this exact order. Do not skip this step even when the user's message already includes a screen description."

Then a numbered list of four steps:

1. "Call `get_metadata` — identifies the selected frame: frame name, page, and node context. Use the frame name for the output filename (`spec-[frame-name].md`) and the page name for the Figma Reference field in each component section. **Scope inference:** If the response indicates a screen/frame node → screen mode. If the response indicates a component instance → component mode. If scope is ambiguous → ask the one clarifying question from Intake."
2. "Call `get_variable_defs` — pulls live token values. These are authoritative for the session and take precedence over `tokens.md`. Shared across all components in the screen — do NOT re-call per component."
3. "For each component in the frame structure, call `get_design_context` — one call per component. Use the Field Mapping table in `~/.claude/skills/bluefish-design-system/spec-template.md` to populate spec fields."
4. "If `get_design_context` triggers a Code Connect prompt at any level (top-level or sub-component): flag inline with `⚠️ Code Connect not configured — proceeding from conversation context` (top-level) or `⚠️ Code Connect not configured for [sub-component] — proceeding from conversation context` (sub-component) and continue with whatever data was returned before the prompt. Never block output."

After the numbered list, add:

**Token drift subsection** — one paragraph explaining: if a value from `get_variable_defs` differs from the same path in `tokens.md`, use the live Figma value and flag inline with `⚠️ token drift — using live Figma value`. Include one short example showing the flag inline in a spec Tokens Used table row.

**Fallback subsection** — one paragraph: "Use when MCP calls fail, return empty, or Figma Desktop is not open: generate from conversation description; read `tokens.md` for token values, `type-styles.md` for typography, `tokens-dataviz.md` for chart colors. Flag every token reference with `⚠️ live token data unavailable — using tokens.md`."

**MCP unavailable diagnostic subsection** — encode the Markdown blockquote variant (bf-prototype uses HTML/JSX comments; bf-spec output is Markdown so the diagnostic is a blockquote at the top of the spec file):

"Required when no MCP call succeeded. Insert as the first line of the output spec file, before any spec content:" followed by a Markdown example showing the blockquote with the exact flag text `⚠️ Figma MCP unavailable — tokens from ~/.claude/skills/bluefish-design-system/{tokens.md, type-styles.md, tokens-dataviz.md}; live values not verified`. End with: "If at least one MCP call returned data, do not emit this diagnostic."

---

**Section 7: `## Screen Mode — Output Rules` (D-02, D-04)**

This section is unique to bf-spec — no direct analog in bf-prototype. Encode the screen-mode document structure from 04-RESEARCH.md Pattern 4 and 04-PATTERNS.md "Screen Mode Output Rules section".

Write a numbered list with these rules. Do not add rules not listed; do not omit any:

1. "**Output file**: One file written to `./` (current working directory of the user's project). Filename: `spec-[screen-name].md` where `[screen-name]` is the frame name from `get_metadata` response, lowercased with spaces replaced by hyphens. Do NOT write to `~/.claude/skills/bluefish-design-system/` — the existing spec files there are reference examples only (per D-04)."
2. "**Document header**: First content lines of the spec file are: `# Spec: [Screen Name]`, then a blank line, then `**Generated:** [ISO date]`, then `**Source:** [Figma page from get_metadata]`. If MCP was unavailable, the MCP-unavailable diagnostic blockquote precedes the heading."
3. "**Component Inventory section**: Immediately after the header, include a `## Component Inventory` heading followed by a Markdown table with columns: `Component | Category | MUI Equivalent | Section`. One row per component found in the frame. The `Section` column links to the `## [Component Name]` heading later in the doc. If `get_design_context` does not return enough data to populate `MUI Equivalent`, infer from `figma-reading-guide.md` Step 3 categories; if no match, write `⚠️ component type unclear — [name] — needs categorization`."
4. "**Per-component sections**: After Component Inventory, add a `---` horizontal rule, then one `## [Component Name]` section per component listed in the inventory. Each section follows `~/.claude/skills/bluefish-design-system/spec-template.md` format. Use the Field Mapping table in `spec-template.md` (lines 63-87) to populate every field. The `### Accessibility` subsection is REQUIRED and never blank — `spec-template.md` line 43 marks it `[Required — non-optional]`."
5. "**Sub-component sections**: Sub-components are nested inside their parent's section as `### Sub-component: [Name]` subsections. They are NOT hoisted to top-level `##` sections. See Sub-Component Recursion Rules below."
6. "**Section separators**: Use `---` horizontal rules between top-level `## [Component Name]` sections for readability."
7. "**`get_variable_defs` reuse**: Token data from `get_variable_defs` is shared across all components. Do NOT re-call it per component. Re-calling is the explicit anti-pattern (per D-10 and Anti-Patterns section below)."
8. "**Pre-return checklist**: Before returning the output, run the foundation's 8-point pre-return checklist internally. Specifically verify every `## [Component Name]` section has a non-empty `### Accessibility` subsection."

---

**Section 8: `## Component Mode — Output Rules` (D-03, D-04)**

Write a numbered list with these rules. Do not add rules not listed; do not omit any:

1. "**Output file**: One file written to `./` (current working directory). Filename: `spec-[component-name].md` where `[component-name]` is the master component name from `get_design_context` (per `figma-reading-guide.md` Step 1), lowercased with spaces replaced by hyphens. Do NOT write to `~/.claude/skills/bluefish-design-system/` (per D-04)."
2. "**Document structure**: The file matches `~/.claude/skills/bluefish-design-system/spec-template.md` exactly. Start with `## [Component Name]` as the first heading — there is NO `# Spec: [Name]` outer heading and NO Component Inventory table in component mode. Reference examples for the quality bar: `~/.claude/skills/bluefish-design-system/spec-button.md` and `~/.claude/skills/bluefish-design-system/spec-chip.md`."
3. "**Field population**: Use the Field Mapping table in `spec-template.md` (lines 63-87) to populate every field from `get_design_context` response. Component name is the ONLY field that triggers a clarifying question; all other missing fields use the fill-and-flag pattern silently — do not ask follow-up questions for category, status, variants, props, tokens, type styles, or Figma reference."
4. "**Accessibility section**: `### Accessibility` is REQUIRED and never blank, even for non-interactive components. `spec-template.md` line 43 marks it non-optional."
5. "**Status default**: `**Status:**` always defaults to `Needs Review`. Never populated from MCP."
6. "**Sub-component handling**: If the component is composite (e.g., Autocomplete contains a TextField and a Listbox), sub-components are inlined as `### Sub-component: [Name]` subsections within the same file — same rule as screen mode. See Sub-Component Recursion Rules below. Quality bar reference: `~/.claude/skills/bluefish-design-system/spec-autocomplete.md`."
7. "**MCP-unavailable diagnostic**: Emit as a blockquote at the very top of the file if no MCP call succeeded, before the `## [Component Name]` heading."
8. "**Pre-return checklist**: Run the foundation's 8-point pre-return checklist internally before returning. Specifically verify the `### Accessibility` subsection is non-empty."

---

**Section 9: `## Sub-Component Recursion Rules` (D-11, D-12, D-13)**

This section is unique to bf-spec — no analog in any prior skill. Encode:

Block 1 (the recursion trigger rule, per D-11): "Recurse into a sub-component's `get_design_context` only when its name matches a component category listed in `~/.claude/skills/bluefish-design-system/figma-reading-guide.md` Step 3 (Navigation, Buttons & Capsules, Forms & Controls, Data & Charts, Feedback, Layout). Reference the figma-reading-guide.md Step 3 table for the authoritative trigger list — do not re-list it here."

Block 2 (non-matching sub-components, per D-11): "Custom layout containers and unnamed sub-layouts that do not match a Step 3 category are treated as atomic: do NOT recurse, do NOT call `get_design_context` on them, and note them inline with the flag `⚠️ custom layout — no Bluefish component match`."

Block 3 (inline heading format, per D-12): "Matching sub-components are inlined under their parent component's `## [Component Name]` section as `### Sub-component: [Name]` subsections. Do NOT hoist them to top-level `##` sections. A composite component's sub-components stay nested in the same spec file as the parent."

Block 4 (Code Connect on sub-components, per D-13): "If a sub-component's `get_design_context` call triggers a Code Connect prompt, emit the flag `⚠️ Code Connect not configured for [sub-component] — proceeding from conversation context` and continue with whatever data was returned before the prompt. Sub-component Code Connect prompts NEVER block output, same as top-level."

Block 5 (depth note): "Recurse one level by default. If a sub-component is itself composite, treat it as a candidate for its own future `/bf-spec` invocation rather than deep-recursing — note this in the parent spec's `### Notes` subsection."

---

**Section 10: `## Anti-Patterns — Do Not Do These`**

Bulleted list with all 10 items below. Each bullet must be specific — name the wrong behavior and the locked decision or pitfall it violates. Use the bf-prototype Anti-Patterns section as the structural template (heading, hyphen-bullet format, bold lead phrase). Do not add bullets not listed here; do not omit any:

- "**Asking a mode question instead of inferring scope**: bf-spec does NOT ask 'screen or component mode?' — it reads the Figma selection via `get_metadata` to infer (per D-01). Asking a mode question is the bf-prototype intake pattern and is wrong for bf-spec."
- "**Calling `get_variable_defs` before `get_metadata`**: The MCP sequence for bf-spec starts with `get_metadata` (per D-08, D-09). Calling `get_variable_defs` first (as in bf-prototype) is the wrong order — `get_metadata` provides the frame identity needed for the output filename and document header."
- "**Re-calling `get_variable_defs` per component**: Token data from `get_variable_defs` is fetched once for the screen and shared across all components (per D-10). Per-component re-fetch is wasteful and wrong."
- "**Hoisting sub-component specs to top-level `##` sections**: Sub-component specs are nested under `### Sub-component: [Name]` inside the parent's `## [Component Name]` section (per D-12). Hoisting collapses the recursion structure."
- "**Blocking output on Code Connect**: At no level (top-level or sub-component) does a Code Connect intercept block spec generation. Flag and continue (per D-07, D-13)."
- "**Discarding partial MCP results**: If `get_design_context` returns data before a Code Connect prompt, use what was returned — do not abandon the response (same rule as bf-prototype)."
- "**Writing specs to `~/.claude/skills/bluefish-design-system/`**: User-generated specs are written to `./` (current working directory of the user's project), not the foundation skills directory (per D-04). The `spec-button.md`, `spec-chip.md`, `spec-text-field.md`, and `spec-autocomplete.md` files in the foundation directory are reference examples only."
- "**Triggering on 'build' or 'implement'**: These phrases belong to Phase 5 `/bf-build`. The bf-spec trigger anchors are 'spec', 'handoff doc', 'engineering spec', 'component inventory'. The frontmatter `description:` field MUST exclude 'build' and 'implement' as standalone triggers."
- "**Omitting the Accessibility section**: `spec-template.md` marks `### Accessibility` as `[Required — non-optional. Every spec must include this section, even for non-interactive components]`. It is NEVER blank. The pre-return checklist verifies presence in every `## [Component Name]` section."
- "**Asking follow-up questions beyond component name**: The Field Mapping table in `spec-template.md` is explicit — component name is the ONLY field that triggers a clarifying question. All other missing fields (category, status, variants, props, tokens, type styles, Figma reference) use fill-and-flag silently."

---

**Final check before returning**:
- File path is exactly `~/.claude/skills/bf-spec/SKILL.md`
- The 10 sections appear in the exact order above
- No section is duplicated, omitted, or re-ordered
- The verbatim strings from the interfaces block (frontmatter, summary paragraph, intake directive blocks) appear exactly as specified
- The file references `spec-template.md` and `figma-reading-guide.md` by file path rather than re-listing their contents
- No section duplicates foundation content that is already injected via `@include`
  </action>

  <verify>
    <automated>grep -c '^name: bf-spec$' ~/.claude/skills/bf-spec/SKILL.md && grep -cF '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Support Files — Read On Demand$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Intake$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Figma Context — Always Attempt First$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Screen Mode — Output Rules$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Component Mode — Output Rules$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Sub-Component Recursion Rules$' ~/.claude/skills/bf-spec/SKILL.md && grep -c '^## Anti-Patterns — Do Not Do These$' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'Do not ask a mode question' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'get_metadata' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'get_variable_defs' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'get_design_context' ~/.claude/skills/bf-spec/SKILL.md && grep -cF '### Sub-component:' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'Component Inventory' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'spec-template.md' ~/.claude/skills/bf-spec/SKILL.md && grep -cF 'figma-reading-guide.md' ~/.claude/skills/bf-spec/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - Source: `test -f ~/.claude/skills/bf-spec/SKILL.md` exits 0
    - Source: `grep -c '^name: bf-spec$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -cF '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^# bf-spec$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Support Files — Read On Demand$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Intake$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Figma Context — Always Attempt First$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Screen Mode — Output Rules$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Component Mode — Output Rules$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Sub-Component Recursion Rules$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source: `grep -c '^## Anti-Patterns — Do Not Do These$' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Source (intake anti-mode-question directive): `grep -cF 'Do not ask a mode question' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (bare-invoke question): `grep -cF 'What screen or component are you speccing?' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (MCP sequence): `grep -cF 'get_metadata' ~/.claude/skills/bf-spec/SKILL.md` returns at least 3 (Figma Context section steps + scope inference + reference in screen mode)
    - Source: `grep -cF 'get_variable_defs' ~/.claude/skills/bf-spec/SKILL.md` returns at least 2
    - Source: `grep -cF 'get_design_context' ~/.claude/skills/bf-spec/SKILL.md` returns at least 3
    - Source (MCP sequence order — get_metadata appears before get_variable_defs textually): `awk '/get_metadata/{m=NR} /get_variable_defs/{if(m>0 && NR>m){print "OK"; exit}}' ~/.claude/skills/bf-spec/SKILL.md | grep -c OK` returns at least 1
    - Source (MCP sequence order — get_variable_defs appears before get_design_context): `awk '/get_variable_defs/{v=NR} /get_design_context/{if(v>0 && NR>v){print "OK"; exit}}' ~/.claude/skills/bf-spec/SKILL.md | grep -c OK` returns at least 1
    - Source (sub-component heading format): `grep -cF '### Sub-component:' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (Component Inventory): `grep -cF 'Component Inventory' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (output destination): `grep -cF 'current working directory' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (Code Connect graceful fallback string): `grep -cF '⚠️ Code Connect not configured' ~/.claude/skills/bf-spec/SKILL.md` returns at least 2 (top-level + sub-component variants)
    - Source (custom layout flag): `grep -cF '⚠️ custom layout — no Bluefish component match' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (MCP unavailable flag exact string): `grep -cF '⚠️ Figma MCP unavailable' ~/.claude/skills/bf-spec/SKILL.md` returns at least 1
    - Source (references spec-template.md by path rather than re-listing): `grep -cF 'spec-template.md' ~/.claude/skills/bf-spec/SKILL.md` returns at least 2
    - Source (references figma-reading-guide.md Step 3 rather than re-listing): `grep -cF 'figma-reading-guide.md' ~/.claude/skills/bf-spec/SKILL.md` returns at least 2
    - Source (description excludes build/implement as triggers): `grep -A6 '^description: >' ~/.claude/skills/bf-spec/SKILL.md | grep -v 'bf-build\|use bf-build\|production code' | grep -cE '\b(build|implement)\b'` returns 0
    - Source (no broken @include syntax): `grep -cE '^@~/.claude/skills/' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1 (only the foundation @include)
    - Source (no duplicate Anti-Patterns): `grep -c '^## Anti-Patterns' ~/.claude/skills/bf-spec/SKILL.md` returns exactly 1
    - Behavior gate (section ordering): the line numbers of section headings, in this order — Intake, Figma Context, Screen Mode, Component Mode, Sub-Component Recursion Rules, Anti-Patterns — must be strictly increasing. Run `grep -nE '^## (Intake|Figma Context — Always Attempt First|Screen Mode — Output Rules|Component Mode — Output Rules|Sub-Component Recursion Rules|Anti-Patterns — Do Not Do These)$' ~/.claude/skills/bf-spec/SKILL.md` and confirm line numbers are increasing in the listed order.
  </acceptance_criteria>

  <done>
    `~/.claude/skills/bf-spec/SKILL.md` exists with all 10 sections in the correct order:
    frontmatter → @include → heading + summary → Support Files → Intake (scope-inference, no mode question) → Figma Context (three-tool MCP sequence) → Screen Mode Output Rules → Component Mode Output Rules → Sub-Component Recursion Rules → Anti-Patterns. All acceptance criteria pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan writes a Markdown skill instruction file. No authentication, secrets, API calls, network surface, or user input handling beyond Figma MCP tool invocations that occur at runtime (not at write time). No trust boundaries cross this artifact. |

## STRIDE Threat Register

Security domain: N/A for this plan. The deliverable is a single Markdown file containing skill behavior instructions. ASVS L1 categories V4 (access control), V5 (input validation), V7 (cryptography), V11 (business logic) are not applicable to skill authoring. No code is executed; no data is persisted; no network calls are made by the act of creating the file.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | bf-spec/SKILL.md description trigger collision with /bf-build | mitigate | Acceptance criteria assert frontmatter `description:` field does not advertise `build` or `implement` as standalone triggers (only as exclusion clauses pointing to bf-build) |
| T-04-03 | Information Disclosure | Spec files written to wrong directory exposing user project paths via skill bundle | mitigate | Skill instructs writing to `./` (user's current working directory); never to the global skills directory. Anti-Patterns section explicitly forbids writing to `~/.claude/skills/bluefish-design-system/` |
| T-04-04 | Denial of Service | Code Connect prompt blocks all spec generation | accept | Skill instructions handle Code Connect prompts gracefully via `⚠️` flags and continue (D-07, D-13). Not a security threat — degradation pattern. |
</threat_model>

<verification>
After task 1 completes:

```bash
# Section presence and order
grep -nE '^## (Support Files — Read On Demand|Intake|Figma Context — Always Attempt First|Screen Mode — Output Rules|Component Mode — Output Rules|Sub-Component Recursion Rules|Anti-Patterns — Do Not Do These)$' ~/.claude/skills/bf-spec/SKILL.md

# Frontmatter integrity
head -10 ~/.claude/skills/bf-spec/SKILL.md

# MCP sequence references
grep -nF 'get_metadata' ~/.claude/skills/bf-spec/SKILL.md
grep -nF 'get_variable_defs' ~/.claude/skills/bf-spec/SKILL.md
grep -nF 'get_design_context' ~/.claude/skills/bf-spec/SKILL.md

# Sub-component pattern
grep -nF '### Sub-component:' ~/.claude/skills/bf-spec/SKILL.md

# Foundation @include
grep -nF '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/.claude/skills/bf-spec/SKILL.md
```

Phase success criteria (from ROADMAP Phase 4):
1. `bf-spec/SKILL.md` exists at `~/.claude/skills/bf-spec/SKILL.md` and the skill is invocable via `/bf-spec` — verified by file existence + frontmatter name field
2. Skill executes the correct MCP sequence — verified by section ordering + sequence enforcement in acceptance criteria
3. Code Connect prompts trigger graceful flag-and-continue — verified by `⚠️ Code Connect not configured` string presence in two variants
4. Skill fires correctly on `/bf-spec` or spec/handoff requests — verified by description frontmatter content (final invocation verification happens in Plan 03 human checkpoint)
</verification>

<success_criteria>
- `~/.claude/skills/bf-spec/SKILL.md` exists with all 10 sections in correct order
- All acceptance criteria from Task 1 pass via grep
- SPEC-01 and SPEC-02 satisfied at the source-artifact level (final behavioral validation occurs in Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-bf-spec/04-02-SUMMARY.md` using the template at `@/Users/pcartelli/.claude/get-shit-done/templates/summary.md`.
</output>
