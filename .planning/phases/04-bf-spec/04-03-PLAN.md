---
phase: 04-bf-spec
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: []
autonomous: false
requirements: [SPEC-01, SPEC-02]

must_haves:
  truths:
    - "Typing `/bf-spec` in Claude Code activates the bf-spec skill (not the foundation, not bf-prototype)"
    - "Bare invocation triggers exactly one clarifying question: 'What screen or component are you speccing?'"
    - "Asking for a 'spec' or 'handoff doc' auto-triggers bf-spec without typing the slash command"
    - "Skill executes the MCP sequence in the order get_metadata → get_variable_defs → get_design_context"
    - "Screen mode produces a doc with `## Component Inventory` table + per-component sections in one file"
    - "Component mode produces a doc matching spec-template.md format exactly"
    - "Code Connect prompts emit ⚠️ flag and the skill continues — output is not blocked"
    - "Sub-components appear as `### Sub-component: [Name]` under their parent section, NOT as top-level `##` headings"
    - "Spec files are written to the user's current working directory, not to ~/.claude/skills/bluefish-design-system/"
  artifacts:
    - path: "~/.claude/skills/bf-spec/SKILL.md"
      provides: "The skill file under behavioral test"
  key_links:
    - from: "User invocation"
      to: "bf-spec skill activation"
      via: "Claude Code skill router matching description trigger phrases"
      pattern: "skill activation observable in conversation"
    - from: "Skill workflow"
      to: "Output spec file"
      via: "MCP sequence + spec-template.md field population"
      pattern: "spec-[name].md in cwd"
---

<objective>
Human-verify that `~/.claude/skills/bf-spec/SKILL.md` behaves correctly across the five test scenarios that exercise SPEC-01 and SPEC-02 acceptance criteria: bare invocation, context invocation, auto-trigger via description match, screen mode behavior, and component mode behavior.

Purpose: This is the gate that closes Phase 4. The Plan 02 acceptance criteria verified the skill file's textual correctness; this plan verifies the skill actually fires and produces the correct output shape in Claude Code. Without this checkpoint, SPEC-02 ("Skill fires correctly when user types /bf-spec or asks for a spec or handoff doc for a Bluefish screen") cannot be confirmed.

Output: User approval (or actionable issues list) — no files written by this plan.
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 4 inputs needed to formulate verification steps -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-02-SUMMARY.md

<!-- The skill being verified -->
@/Users/pcartelli/.claude/skills/bf-spec/SKILL.md

<interfaces>
<!-- Acceptance scenarios mapped to ROADMAP Phase 4 success criteria -->

ROADMAP Phase 4 success criteria 1: "`bf-spec/SKILL.md` exists at `~/.claude/skills/bf-spec/SKILL.md` and the skill is invocable via `/bf-spec`"
→ Verified by scenarios 1 (bare invoke) and 2 (context invoke)

ROADMAP Phase 4 success criteria 2: "Skill executes the correct MCP sequence — `get_metadata` first, then `get_variable_defs`, then `get_design_context` per sub-component — without reversing order or skipping tools"
→ Verified by scenarios 4 (screen mode) and 5 (component mode) — observe order of tool calls

ROADMAP Phase 4 success criteria 3: "When `get_design_context` returns a Code Connect prompt instead of component data, the skill emits a `⚠️ Code Connect not configured` flag and continues — output is never blocked"
→ Verified opportunistically during scenarios 4 and 5 if Code Connect intercept occurs; explicitly noted as expected behavior in verification steps

ROADMAP Phase 4 success criteria 4: "Skill fires correctly when user types `/bf-spec` or asks for a spec or handoff doc for a Bluefish screen"
→ Verified by scenarios 1, 2, and 3 (auto-trigger via natural language)

SPEC-01 (requirement): file exists with the three-tool MCP sequence as core mechanic
→ Confirmed by Plan 02 grep gates + scenarios 4/5 behavioral observation

SPEC-02 (requirement): skill fires correctly via slash command or natural language request
→ Confirmed by scenarios 1, 2, 3
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify bf-spec skill across five scenarios</name>

  <read_first>
    - ~/.claude/skills/bf-spec/SKILL.md — read the completed skill file before presenting verification steps so the executor can quote exact wording in the scenario descriptions
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-02-SUMMARY.md — confirms Plan 02 completed and all acceptance criteria passed before this checkpoint runs
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/04-bf-spec/04-CONTEXT.md — decisions D-01, D-02, D-03, D-05, D-08, D-11, D-12 govern expected behavior per scenario
  </read_first>

  <files>
    (no files written by this plan — checkpoint observes behavior of ~/.claude/skills/bf-spec/SKILL.md authored in Plan 02)
  </files>

  <action>
    Present the five verification scenarios described in `<how-to-verify>` to the user, in order, and pause for the user to run each. For each scenario, restate the exact invocation text (e.g., the literal string `/bf-spec` for scenario 1; `/bf-spec the Campaign Detail page nav rail` for scenario 2; `Can you write a handoff doc for the AI Insights overview screen?` for scenario 3). Wait for the user's observed result per scenario before moving on. Map each observed result against the corresponding bullet in `<acceptance_criteria>`. If a scenario fails, capture the specific failure (e.g., "mode question asked in scenario 1", "get_design_context called before get_metadata in scenario 4") and surface it to the user verbatim. Do not modify the skill file during this plan — failures are reported back to the orchestrator for revision via /gsd:plan-phase. The plan completes only when the user types `approved`.
  </action>

  <verify>
    <human-check>User runs five scenarios in a fresh Claude Code conversation and reports observed behavior. Acceptance criteria bullets are checked against observations. User signals `approved` or describes failed scenarios.</human-check>
  </verify>

  <what-built>
    `~/.claude/skills/bf-spec/SKILL.md` — the `/bf-spec` command skill that:
    1. Infers scope (screen vs. component mode) from the Figma selection via `get_metadata` — does NOT ask a mode question
    2. Executes the three-tool MCP sequence: `get_metadata` first, `get_variable_defs` second, `get_design_context` per component third
    3. Produces screen mode output (`spec-[screen-name].md`) with `## Component Inventory` table + per-component sections, OR component mode output (`spec-[component-name].md`) matching spec-template.md format
    4. Handles Code Connect prompts gracefully with `⚠️ Code Connect not configured` flags and continues — output is never blocked
    5. Recurses into sub-components only when their names match `figma-reading-guide.md` Step 3 categories; inlines matches as `### Sub-component: [Name]` under the parent section
    6. Writes spec files to `./` (user's current working directory), not to the global skills directory

    Inherits all Bluefish foundation context (token rules, accessibility standards, Figma MCP setup, component map) from `bluefish-design-system` via `@include`.
  </what-built>

  <how-to-verify>
    **Setup**: Open a fresh Claude Code conversation (start a new chat so the updated skill file is picked up). Have Figma Desktop open with the Bluefish pattern library available for scenarios 4 and 5. If Figma Desktop is not available, scenarios 4 and 5 verify the MCP-unavailable fallback path instead — both paths must work.

    Run all five scenarios. Each scenario is independent — start a fresh conversation between scenarios if behavior bleeds across.

    **Scenario 1 — Bare invocation (verifies D-05, SPEC-02)**
    Type exactly: `/bf-spec`
    Expected behavior:
    - Claude activates the bf-spec skill (observable in conversation context: skill response patterns, no bf-prototype mode question)
    - Claude asks exactly one question: `What screen or component are you speccing?` (or close paraphrase containing both "screen" and "component")
    - Claude does NOT ask any mode question (HTML vs. Vite+MUI is bf-prototype's pattern, not bf-spec's)
    - After you answer the one question, Claude proceeds to attempt MCP calls

    **Scenario 2 — Context invocation (verifies D-06, SPEC-02)**
    Type exactly: `/bf-spec the Campaign Detail page nav rail`
    Expected behavior:
    - Claude does NOT ask the bare-invoke question (because context was provided)
    - Claude proceeds directly to MCP sequence calls (`get_metadata` first)
    - No mode question is asked

    **Scenario 3 — Auto-trigger via natural language (verifies SPEC-02, description routing)**
    Type exactly: `Can you write a handoff doc for the AI Insights overview screen?`
    Expected behavior:
    - The bf-spec skill auto-activates (not bf-prototype, not bf-explore, not bluefish-design-system foundation)
    - Confirmation signal: Claude follows the bf-spec workflow (attempts MCP sequence including `get_metadata`); a foundation response would lookup tokens without calling MCP, a bf-prototype response would ask the HTML/Vite mode question, a bf-explore response would generate layout variations

    **Scenario 4 — Screen mode behavior (verifies D-01, D-02, D-10, ROADMAP success #2)**
    Open a screen frame (e.g., AI Insights overview, Campaign Detail, or any multi-component screen) in Figma Desktop and select it. Then in Claude Code type: `/bf-spec`
    Answer the clarifying question if asked. Then observe:
    - Claude calls `get_metadata` FIRST (visible in tool call output)
    - Claude calls `get_variable_defs` SECOND
    - Claude calls `get_design_context` once per component found in the frame structure
    - `get_variable_defs` is NOT re-called per component
    - Output spec file is written to the current working directory (e.g., `spec-ai-insights-overview.md`), NOT to `~/.claude/skills/bluefish-design-system/`
    - Output file structure: `# Spec: [Screen Name]` heading → `**Generated:**` line → `**Source:**` line → `## Component Inventory` table → `---` rule → one `## [Component Name]` section per component
    - Each `## [Component Name]` section contains the spec-template.md fields including a non-empty `### Accessibility` subsection
    - If any component is composite (has named sub-components matching figma-reading-guide.md Step 3 categories), the sub-components appear as `### Sub-component: [Name]` nested inside the parent's `##` section — NOT as additional top-level `##` headings
    - If Code Connect intercepts any `get_design_context` call, the output contains the `⚠️ Code Connect not configured` flag and the spec is still produced

    **Scenario 5 — Component mode behavior (verifies D-01, D-03, ROADMAP success #2)**
    Open a single component instance (e.g., a Button, Chip, or TextField) in Figma Desktop and select it. Then in Claude Code type: `/bf-spec`
    Answer the clarifying question if asked. Then observe:
    - MCP sequence executes in correct order (same as scenario 4)
    - Output spec file is written to current working directory (e.g., `spec-button.md`), NOT to the global skills directory
    - Output file matches `~/.claude/skills/bluefish-design-system/spec-template.md` format exactly
    - First content heading is `## [Component Name]` — there is NO outer `# Spec: [Name]` heading and NO Component Inventory table in component mode
    - `**Status:**` is `Needs Review`
    - `### Accessibility` subsection is present and non-empty
    - If the component is composite, sub-components appear as `### Sub-component: [Name]` subsections within the same file

    **Fallback path (if Figma Desktop is unavailable for scenarios 4 or 5)**:
    The MCP calls will fail or return empty. Expected: Claude emits the MCP-unavailable diagnostic blockquote at the top of the output file with exact text `> ⚠️ Figma MCP unavailable — tokens from ~/.claude/skills/bluefish-design-system/{tokens.md, type-styles.md, tokens-dataviz.md}; live values not verified` and proceeds with conversation-context-based spec generation, flagging every token with `⚠️ live token data unavailable — using tokens.md`. Output is never blocked.
  </how-to-verify>

  <acceptance_criteria>
    - Behavior (scenario 1): bare `/bf-spec` invocation asks the question `What screen or component are you speccing?` (exact or near-exact wording with both "screen" and "component") and asks no mode question
    - Behavior (scenario 2): `/bf-spec` with context skips the bare-invoke question and proceeds to MCP calls
    - Behavior (scenario 3): natural-language request `Can you write a handoff doc for the AI Insights overview screen?` activates bf-spec (Claude follows the bf-spec workflow including `get_metadata` call attempt)
    - Behavior (scenario 4): screen mode output file lives in the project's current working directory, filename starts with `spec-`, contains `## Component Inventory` table and at least one `## [Component Name]` section with a non-empty `### Accessibility` subsection
    - Behavior (scenario 4 sequence): `get_metadata` tool call occurs BEFORE `get_variable_defs` tool call, which occurs BEFORE the first `get_design_context` tool call
    - Behavior (scenario 4 dedup): `get_variable_defs` is called at most once per invocation (not once per component)
    - Behavior (scenario 4 nesting): if composite components exist, sub-component spec sections appear as `### Sub-component:` subsections, not as additional `##` top-level sections; the count of `##` headings in the output (excluding `## Component Inventory`) matches the row count of the Component Inventory table
    - Behavior (scenario 5): component mode output file lives in the project's current working directory, filename starts with `spec-`, first content heading is `## [Component Name]` (no outer `# Spec:` heading, no Component Inventory table)
    - Behavior (scenario 5 fields): output contains `**Category:**`, `**Status:** Needs Review`, `### Variants & States`, `### Props`, `### Tokens Used`, `### Accessibility` subsections; `### Accessibility` is non-empty
    - Behavior (Code Connect, when applicable): if any `get_design_context` call triggers a Code Connect prompt during scenarios 4 or 5, the output contains the literal string `⚠️ Code Connect not configured` and the spec is still produced (not blocked)
    - Behavior (fallback path, when applicable): if Figma Desktop is unavailable, output starts with the blockquote `> ⚠️ Figma MCP unavailable` and proceeds with conversation-context fallback
    - User signal: the user types `approved` after all applicable scenarios pass, or describes specific issues for each failed scenario
  </acceptance_criteria>

  <resume-signal>Type "approved" to complete Phase 4, or describe any failed scenarios so they can be addressed before sign-off.</resume-signal>

  <done>
    User types "approved" after all five scenarios pass (or scenarios 1-3 + fallback path for 4-5 if Figma Desktop unavailable). SPEC-01 and SPEC-02 confirmed at behavioral level. Phase 4 complete; ROADMAP Phase 4 success criteria 1-4 verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Claude Code | Verification scenario inputs cross from user to Claude Code; outputs are observed but not persisted as security-sensitive data |
| Figma MCP → Claude | MCP tool responses cross into the skill workflow; bf-spec treats them as untrusted in the sense that empty/Code-Connect responses must not crash the skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | Code Connect prompt blocks scenario 4/5 from completing | mitigate | Scenario 4 and 5 expected behavior explicitly includes the graceful Code Connect fallback path; user is instructed to verify continuation rather than blockage |
| T-04-06 | Repudiation | Verification result not recorded | mitigate | User signal is "approved" or specific failure description; SUMMARY.md captures result for audit trail |

Security domain: N/A for the artifact itself (Markdown skill instructions, no code execution, no network, no secrets). Threats above relate to verification process integrity, not application security.
</threat_model>

<verification>
Plan 03 verification IS the human-verify checkpoint — the task itself.

After user approval:

```bash
# Confirm skill file integrity unchanged during behavioral verification
test -f ~/.claude/skills/bf-spec/SKILL.md && echo "skill intact"

# Confirm any spec files generated during verification land in cwd, not in foundation
ls ~/.claude/skills/bluefish-design-system/spec-*.md
# Expected: only the four reference specs (spec-button.md, spec-chip.md, spec-text-field.md, spec-autocomplete.md). No new files.
```

Phase 4 ROADMAP success criteria 1-4 are verified by the five scenarios. SPEC-01 and SPEC-02 are confirmed at the behavioral level.
</verification>

<success_criteria>
- All five verification scenarios pass (or scenarios 1-3 + fallback path for 4-5 if Figma Desktop unavailable)
- User types `approved`
- SPEC-01 and SPEC-02 verified
- ROADMAP Phase 4 success criteria 1-4 confirmed
- Phase 4 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-bf-spec/04-03-SUMMARY.md` using the template at `@/Users/pcartelli/.claude/get-shit-done/templates/summary.md`.
</output>
