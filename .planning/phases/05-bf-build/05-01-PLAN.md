---
phase: 05-bf-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md
  - ~/dev/bluefish-ai-skills/bf-spec/SKILL.md
  - ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md
  - ~/dev/bluefish-ai-skills/README.md
autonomous: true
requirements: [BUILD-02]

must_haves:
  truths:
    - "No skill file under ~/dev/bluefish-ai-skills/ still claims 'bf-build — planned, not yet available' in any frontmatter description"
    - "Foundation (bluefish-design-system) description no longer claims to handle build/implement requests on behalf of the planned bf-build skill"
    - "Foundation Known Gaps section no longer contains a bf-build placeholder bullet"
    - "bf-prototype and bf-spec anti-pattern bullets now redirect build/implement requests to /bf-build instead of saying 'until bf-build ships, the foundation handles it'"
    - "README.md known-limitations entry about bf-build being unbuilt is replaced with a bf-build entry that reflects shipped status"
    - "README.md skills table contains a bf-build row positioned after the bf-spec row"
    - "README.md usage section contains a /bf-build usage example positioned after the /bf-spec example"
    - "README.md install section contains a cp -r bf-build ~/.claude/skills/ line positioned after the bf-spec install line"
  artifacts:
    - path: "~/dev/bluefish-ai-skills/bf-prototype/SKILL.md"
      provides: "bf-prototype skill with cleaned trigger description and updated anti-pattern bullet"
      contains: "use bf-build"
    - path: "~/dev/bluefish-ai-skills/bf-spec/SKILL.md"
      provides: "bf-spec skill with cleaned trigger description and updated anti-pattern bullet"
      contains: "use bf-build"
    - path: "~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md"
      provides: "Foundation skill description and Known Gaps with bf-build interim language removed"
      contains: "explore, prototype, spec, or build task"
    - path: "~/dev/bluefish-ai-skills/README.md"
      provides: "README updated with bf-build in skills table, usage example, install step, and shipped-status known-limitation note"
      contains: "bf-build"
  key_links:
    - from: "Every existing command skill description"
      to: "Phase 5 bf-build trigger anchors (build / implement / generate production code)"
      via: "Removal of 'planned, not yet available' interim language so bf-build alone claims those triggers"
      pattern: "no occurrence of 'planned, not yet available' in any skill description"
    - from: "Foundation skill description + Known Gaps"
      to: "bf-build skill (about to be authored in Plan 02)"
      via: "Removal of the 'until bf-build ships' clauses that route build requests to foundation"
      pattern: "no occurrence of 'until.*bf-build.*ships' in foundation SKILL.md"
    - from: "README.md"
      to: "Five-skill ecosystem (bf-explore, bf-prototype, bf-spec, bf-build, bluefish-design-system)"
      via: "Skills table, usage section, and install section all reflect five skills, not four"
      pattern: "README skills table contains bf-build row"
---

## Phase Goal

**As a** designer or engineer on the Bluefish team, **I want to** type `/bf-build` and receive a production-ready React/MUI component generated from a Figma frame or an existing `/bf-spec` output file, **so that** I can ship component code without manually transcribing tokens, props, or accessibility behavior from Figma.

<objective>
Execute the seven concrete routing-reclaim edits enumerated in `05-RESEARCH.md` (Routing Reclaim — Enumerated Edit Targets) across four existing files in `~/dev/bluefish-ai-skills/`, plus the three additive README.md changes (table row, usage example, install line). This plan does NOT create the bf-build skill itself — that is Plan 05-02. This plan removes the interim language that currently routes build/implement requests to the foundation skill, so that when Plan 05-02 ships `bf-build`, no two skills compete for the same trigger.

Purpose: Implement D-13 from `05-CONTEXT.md`. Required pre-work before authoring bf-build, because if interim language remains, both the foundation skill and bf-build will claim build/implement triggers and the Phase 5 human verification (Plan 05-03) cannot validate that bf-build fires correctly on natural-language build requests. Closes RESEARCH Pitfall 6 (Routing Reclaim Partial Completion).

Output: Four files modified in place. Zero new files created. After this plan: `grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/` returns no results, and `grep -r "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/` returns no results.
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 5 source of truth — all 13 locked decisions and routing reclaim enumeration -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-HANDOFF.md

<interfaces>
<!-- Edit targets are pre-mapped in 05-RESEARCH.md "Routing Reclaim — Enumerated Edit Targets" and 05-PATTERNS.md "Routing Reclaim Edits (4 files)". -->
<!-- Each edit has an exact current text string (grep-verified) and an exact replacement string. -->
<!-- Executor MUST read the four target files before editing to confirm the current text strings are still present — if Phase 4 review-pass commits changed wording, surface the drift and stop. -->

Seven edits total, distributed:
  bf-prototype/SKILL.md     — 2 edits (frontmatter description line 8; anti-pattern bullet line 198)
  bf-spec/SKILL.md          — 2 edits (frontmatter description line 8; anti-pattern bullet lines 257-258)
  bluefish-design-system/SKILL.md — 2 edits (frontmatter description lines 7-8; Known Gaps bullet line 194)
  README.md                 — 1 known-limitations replacement + 3 additive changes (table row, usage example, install line)

Verification gates after edits:
  - grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/ → 0 results
  - grep -r "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/ → 0 results
  - grep "bf-build" ~/dev/bluefish-ai-skills/README.md → at least 3 hits (table row, usage example, install line)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply six SKILL.md routing reclaim edits across three files</name>

  <read_first>
    - ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md — read in full before editing; confirm the current text strings on line 8 and line 198 (or nearest match) still match the strings cited in 05-RESEARCH.md
    - ~/dev/bluefish-ai-skills/bf-spec/SKILL.md — read in full before editing; confirm the current text strings on line 8 and lines 257-258 still match the strings cited in 05-RESEARCH.md
    - ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md — read in full before editing; confirm the description clause (lines 7-8) and Known Gaps bullet (line 194) still match the strings cited in 05-RESEARCH.md
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md — section "Routing Reclaim — Enumerated Edit Targets" provides every current-text/replacement pair
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-PATTERNS.md — section "Routing Reclaim Edits (4 files)" provides surrounding-bullet context for each anti-pattern edit so the replacement reads cleanly in context
  </read_first>

  <files>
    ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md
    ~/dev/bluefish-ai-skills/bf-spec/SKILL.md
    ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md
  </files>

  <action>
    Apply six find-and-replace edits using the Edit tool. Each pair is specified exactly in 05-RESEARCH.md "Routing Reclaim — Enumerated Edit Targets". Execute in this order:

    Edit 1 — bf-prototype/SKILL.md frontmatter description: replace the substring "(bf-build — planned, not yet available)." with "(use bf-build)." Preserve all surrounding whitespace and the closing period exactly as written.

    Edit 2 — bf-prototype/SKILL.md anti-pattern bullet (final bullet of the Anti-Patterns section, the "Trigger over-broadening on build / implement / create" bullet): replace the sentence "Until `/bf-build` ships, the foundation skill handles build requests; offer this skill explicitly if the user wants something prototype-grade now." with "Use `/bf-build` for production component output." The surrounding bullet text remains untouched.

    Edit 3 — bf-spec/SKILL.md frontmatter description: replace the substring "planned, not yet available)." with "use bf-build)." Preserve indentation. Full line context per 05-PATTERNS.md is "  bf-build — planned, not yet available)." which becomes "  bf-build — use bf-build)." — confirm this is the literal resulting line (note the awkwardness: the result references bf-build twice in one phrase; this is intentional per RESEARCH Pattern 2 and PATTERNS Edit 1, and matches the bf-prototype description shape).

    Edit 4 — bf-spec/SKILL.md anti-pattern bullet (final bullet of the "Triggering on build or implement" entry): replace "Until `/bf-build` ships, the foundation skill handles build requests." with "Use `/bf-build` for production component output."

    Edit 5 — bluefish-design-system/SKILL.md frontmatter description: locate the multi-line description block and replace the clause "— including build and implementation requests until the dedicated bf-build skill ships." entirely (including the em-dash and trailing period that follows "code output standards"). The resulting description block must end exactly as: "Activates for any question about the Bluefish design system that is not a specific explore, prototype, spec, or build task." Add the words ", spec, or build" to the trailing enumeration as shown in 05-PATTERNS.md Edit 1 replacement block — the previous enumeration only listed "explore, prototype, or spec task".

    Edit 6 — bluefish-design-system/SKILL.md Known Gaps section: delete the entire line "- [ ] No `/bf-build` skill yet — build/implement requests are handled by this foundation (apply token + accessibility rules directly; offer bf-prototype for scaffolding) until Phase 5 ships." Do NOT disturb the Code Connect gap above or the get_variable_defs gap below.

    After all six edits, run the grep verification gates listed in `<acceptance_criteria>`.

    Per D-13. If any current-text string fails to match the file contents (e.g., Phase 4 review pass renumbered lines or reworded a bullet), STOP and report the drift to the user with a quote of the actual current text — do not guess at a replacement.
  </action>

  <verify>
    <automated>grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/ | grep -v '^Binary' ; test $(grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/ 2>/dev/null | grep -v '^Binary' | wc -l) -eq 0 &amp;&amp; grep -rE "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/ | grep -v '^Binary' ; test $(grep -rE "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/ 2>/dev/null | grep -v '^Binary' | wc -l) -eq 0 &amp;&amp; grep -c "explore, prototype, spec, or build task" ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md | grep -qv '^0$' &amp;&amp; ! grep -q "No \`/bf-build\` skill yet" ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md &amp;&amp; grep -c "Use \`/bf-build\` for production component output" ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md | grep -qv '^0$' &amp;&amp; grep -c "Use \`/bf-build\` for production component output" ~/dev/bluefish-ai-skills/bf-spec/SKILL.md | grep -qv '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/` returns 0 matches
    - `grep -rE "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/` returns 0 matches
    - `grep -c "Use \`/bf-build\` for production component output" ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md` returns at least 1
    - `grep -c "Use \`/bf-build\` for production component output" ~/dev/bluefish-ai-skills/bf-spec/SKILL.md` returns at least 1
    - `grep -c "explore, prototype, spec, or build task" ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md` returns at least 1 (confirms the trailing enumeration includes the new "spec, or build" tokens)
    - `grep "No \`/bf-build\` skill yet" ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md` returns 0 matches (the Known Gaps bullet is fully removed)
    - All three files remain syntactically valid YAML+Markdown — `head -10 ~/dev/bluefish-ai-skills/bf-prototype/SKILL.md` shows frontmatter block intact (opening `---`, name/description, closing `---`)
    - bf-prototype/SKILL.md, bf-spec/SKILL.md, and bluefish-design-system/SKILL.md each remain a single complete file (line count change of at most one line for the Known Gaps deletion in foundation; description and anti-pattern edits are in-place text substitutions, not line additions or removals)
  </acceptance_criteria>

  <done>
    Six edits applied across three files. Verification greps confirm no interim "planned, not yet available" or "until bf-build ships" language remains anywhere under ~/dev/bluefish-ai-skills/. The three skills now point build/implement requests at /bf-build and the foundation description no longer claims to catch those requests. Files remain valid Markdown with frontmatter intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md — replace known-limitation entry, add table row, usage example, install line</name>

  <read_first>
    - ~/dev/bluefish-ai-skills/README.md — read in full before editing; confirm current line 74 known-limitations bullet, current Skills table structure (column headers and existing four rows), current Usage section structure (existing examples for each skill), current Install section structure (existing cp -r lines)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md — section "Routing Reclaim — Enumerated Edit Targets" specifies the README replacement text and the three additive blocks verbatim
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-PATTERNS.md — section "~/dev/bluefish-ai-skills/README.md — 1 block replacement" provides additive change details (table row, usage example, install step)
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md — D-13 confirms README is part of the routing reclaim task scope (not deferred); per Open Question 1 in 05-RESEARCH.md, README install instructions stay as `cp -r` form (external-team-facing) — do NOT change to `ln -s`
  </read_first>

  <files>
    ~/dev/bluefish-ai-skills/README.md
  </files>

  <action>
    Apply one in-place replacement and three additive insertions to README.md, in this order:

    Replacement 1 — Known limitations section: locate the bullet "- **No `/bf-build` yet** — production React/MUI component generation is planned. Until it ships, build requests are handled by the foundation skill's token and accessibility rules, with `bf-prototype` available for prototype-grade scaffolding." Replace it with: "- **`/bf-build` output is a component file only** — no scaffold, no App.tsx, no main.tsx. Drop the generated `.tsx` file into your Vite+MUI project."

    Insertion 1 — Skills table: locate the existing Skills table (4 rows: bf-explore, bf-prototype, bf-spec, bluefish-design-system, in whatever current order). Insert a new row immediately after the bf-spec row with the cells: `| \`bf-build\` | \`/bf-build\` | Generates a production-ready TypeScript React/MUI component file from a Figma frame or spec file |` — match the column count and pipe spacing of the existing rows exactly. Do NOT reorder existing rows.

    Insertion 2 — Usage section: locate the existing usage examples (one per command skill). Insert a new example after the `/bf-spec` example with this block (preserving the markdown code-fence style used by surrounding examples):

    Heading line: `**Build a production component:**`
    Code-fence block: a single fenced block containing only the literal text `/bf-build the Button component`
    Trailing description paragraph: "Claude reads the Figma frame via MCP (or parses `spec-button.md` if present) and writes `BluefishButton.tsx` to your working directory."

    Insertion 3 — Install section: locate the existing `cp -r` install lines (one per command skill). Insert immediately after the line referencing bf-spec a new line: `cp -r bf-build ~/.claude/skills/` — match the code-fence or list style of the surrounding install lines exactly.

    Do NOT change the install form from `cp -r` to `ln -s`. Per 05-RESEARCH.md Open Question 1, the README is the external-facing install doc and stays in `cp -r` form even though internal install uses symlinks.

    Per D-13. If the README structure differs from what is described (e.g., no separate Install or Usage section), STOP and surface the discrepancy with quoted current text — do not invent placement.
  </action>

  <verify>
    <automated>test $(grep -c "bf-build" ~/dev/bluefish-ai-skills/README.md) -ge 3 &amp;&amp; ! grep -q "No \`/bf-build\` yet" ~/dev/bluefish-ai-skills/README.md &amp;&amp; grep -q "output is a component file only" ~/dev/bluefish-ai-skills/README.md &amp;&amp; grep -q "/bf-build the Button component" ~/dev/bluefish-ai-skills/README.md &amp;&amp; grep -q "cp -r bf-build ~/.claude/skills/" ~/dev/bluefish-ai-skills/README.md &amp;&amp; grep -q "BluefishButton.tsx" ~/dev/bluefish-ai-skills/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "bf-build" ~/dev/bluefish-ai-skills/README.md` returns at least 3 (covers table row, usage example, install line — plus the new known-limitation entry referencing bf-build)
    - `grep "No \`/bf-build\` yet" ~/dev/bluefish-ai-skills/README.md` returns 0 matches
    - `grep "output is a component file only" ~/dev/bluefish-ai-skills/README.md` returns 1 match (the replacement known-limitation bullet)
    - `grep "/bf-build the Button component" ~/dev/bluefish-ai-skills/README.md` returns 1 match (the usage-section invocation example)
    - `grep "cp -r bf-build ~/.claude/skills/" ~/dev/bluefish-ai-skills/README.md` returns 1 match (the install-section new line)
    - `grep "BluefishButton.tsx" ~/dev/bluefish-ai-skills/README.md` returns 1 match (the usage-section trailing description)
    - The new bf-build table row appears AFTER the existing bf-spec row in the Skills table — verifiable by `awk '/\| \`bf-spec\`/{found=1} found &amp;&amp; /\| \`bf-build\`/{print "ok"; exit}' ~/dev/bluefish-ai-skills/README.md` outputs "ok"
    - `awk '/^cp -r bf-spec/{found=1} found &amp;&amp; /^cp -r bf-build/{print "ok"; exit}' ~/dev/bluefish-ai-skills/README.md` outputs "ok" (install line ordered correctly)
    - The README remains valid Markdown — no broken table syntax, no unclosed code fences
  </acceptance_criteria>

  <done>
    README.md known-limitation entry replaced; bf-build row added to Skills table after bf-spec; /bf-build usage example added to Usage section after /bf-spec example; cp -r bf-build install line added after bf-spec install line. README still reads cleanly as the external-facing project doc. No remaining "No /bf-build yet" or "planned" language anywhere in the file.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

```bash
grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/ | grep -v '^Binary'
# expected: no output (exit 1)

grep -rE "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/ | grep -v '^Binary'
# expected: no output (exit 1)

grep -c "bf-build" ~/dev/bluefish-ai-skills/README.md
# expected: 3 or more

head -12 ~/dev/bluefish-ai-skills/bluefish-design-system/SKILL.md
# expected: description ends with "explore, prototype, spec, or build task." — no "build and implementation requests" clause
```

Files touched should be valid Markdown — frontmatter intact in all three SKILL.md files, table and code-fence structure preserved in README.md.
</verification>

<success_criteria>
1. Six SKILL.md edits applied across three files in `~/dev/bluefish-ai-skills/` (bf-prototype, bf-spec, bluefish-design-system)
2. README.md updated with replacement known-limitation entry + bf-build table row + /bf-build usage example + cp -r bf-build install line
3. `grep -r "planned, not yet available" ~/dev/bluefish-ai-skills/` and `grep -rE "until.*bf-build.*ships" ~/dev/bluefish-ai-skills/` both return zero results
4. All four touched files remain syntactically valid Markdown with frontmatter blocks intact
5. Foundation `bluefish-design-system/SKILL.md` no longer claims to catch build/implement requests in either its description or Known Gaps section
</success_criteria>

<output>
Create `.planning/phases/05-bf-build/05-01-SUMMARY.md` when done — record the seven edits applied (six SKILL + one README replacement + three README additions), verification grep results, and any drift encountered between cited current-text and actual file contents.
</output>
