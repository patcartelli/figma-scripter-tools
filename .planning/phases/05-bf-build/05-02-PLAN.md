---
phase: 05-bf-build
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - ~/dev/bluefish-ai-skills/bf-build/SKILL.md
  - ~/.claude/skills/bf-build
autonomous: true
requirements: [BUILD-01, BUILD-02]

must_haves:
  truths:
    - "~/dev/bluefish-ai-skills/bf-build/ exists as a directory with SKILL.md inside it"
    - "~/.claude/skills/bf-build is a symlink pointing to /Users/pcartelli/dev/bluefish-ai-skills/bf-build (absolute path, no trailing slash)"
    - "Frontmatter description claims 'build', 'implement', and 'generate production code' as triggers and excludes 'prototype', 'explore', and 'spec' as triggers"
    - "Frontmatter description never contains the phrase 'planned, not yet available'"
    - "Skill file inherits foundation via @include with the verbatim fallback blockquote from bf-spec/bf-prototype"
    - "Support Files — Read On Demand section names the same four foundation files used by bf-spec, with bf-build-specific triggering conditions"
    - "Intake section handles bare invocation by asking exactly: 'What component should I build?' (only when no description AND no Figma context)"
    - "Figma Context section declares exactly two MCP calls: get_variable_defs and get_design_context — get_metadata is NEVER mentioned as part of the bf-build sequence"
    - "Anti-Patterns section contains an explicit bullet stating that calling get_metadata is the bf-spec sequence and bf-build does not call it"
    - "Path B — Spec File Parsing section documents the H2/H3 spec structure, cross-spec auto-read behavior, and screen-level inventory gate question 'Which component should I build?'"
    - "Output Rules specify single .tsx file output to ./, filename pattern Bluefish[ComponentName].tsx (PascalCase, Bluefish prefix), named export, TypeScript interface extending MUI props, import type for MUI type exports, dual-path DATA-03 token output, ARIA props required"
    - "Output Rules contain NO scaffold instructions — no main.tsx, no App.tsx, no index.html, no npm create vite command"
    - "Dataviz token format documented as color-roles/dataviz/[NN]/[property] sourced from tokens.md; tokens-dataviz.md is described as raw palette for gradients/tints only"
    - "Typography annotation format is /* type: H1 */ inline (top-level style names only, never nested /-paths)"
    - "Dual-path token output is flagged with /* ⚠️ token injection method unconfirmed — verify with eng */ (NOT 'DATA-03')"
    - "MCP-unavailable diagnostic uses the exact phrasing 'tokens from cached Bluefish reference (tokens.md, type-styles.md, tokens-dataviz.md)' as a // comment at the top of the generated .tsx file"
    - "Pre-return checklist references the foundation 8-point checklist by name without restating its bullets (avoid drift)"
    - "~/.claude/skills/bf-build symlink resolves to /Users/pcartelli/dev/bluefish-ai-skills/bf-build with no trailing slash"
  artifacts:
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "bf-build command skill file"
      contains: "name: bf-build"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "@include inheriting foundation"
      contains: "@~/.claude/skills/bluefish-design-system/SKILL.md"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Foundation @include fallback blockquote"
      contains: "did not load above"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Support Files — Read On Demand section"
      contains: "Support Files — Read On Demand"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Intake section with bare-invoke gate"
      contains: "What component should I build"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Figma Context section with two-tool MCP sequence (Path A)"
      contains: "Figma Context — Always Attempt First"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Path B Spec File Parsing section (new — no analog in bf-spec/bf-prototype)"
      contains: "Path B — Spec File Parsing"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Screen-level inventory gate"
      contains: "Which component should I build"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Output Rules section"
      contains: "Output Rules"
    - path: "~/dev/bluefish-ai-skills/bf-build/SKILL.md"
      provides: "Anti-Patterns section"
      contains: "Anti-Patterns — Do Not Do These"
    - path: "~/.claude/skills/bf-build"
      provides: "Symlink so Claude Code discovers the skill"
      contains: "symlink to /Users/pcartelli/dev/bluefish-ai-skills/bf-build"
  key_links:
    - from: "description: field in frontmatter"
      to: "Claude Code skill router"
      via: "discriminating phrases 'build', 'implement', 'generate production code', 'production component'"
      pattern: "description.*(build|implement|production component)"
    - from: "Figma Context section"
      to: "Figma MCP tools"
      via: "ordered sequence: get_variable_defs then get_design_context (NO get_metadata)"
      pattern: "get_variable_defs.*get_design_context"
    - from: "Path B section"
      to: "User-side spec file in ./"
      via: "H2 component heading parsed for name + sub-section parsing for props/tokens/a11y; cross-spec auto-read when ### Sub-component references another spec-*.md"
      pattern: "### Sub-component"
    - from: "Output Rules"
      to: "Generated .tsx file in user's CWD"
      via: "Bluefish[ComponentName].tsx written to ./ — single file, no scaffold"
      pattern: "Bluefish\\[ComponentName\\]\\.tsx"
    - from: "~/.claude/skills/bf-build symlink"
      to: "~/dev/bluefish-ai-skills/bf-build/ source directory"
      via: "ln -s with absolute source path (matches existing bf-spec/bf-prototype/bf-explore/bluefish-design-system symlinks)"
      pattern: "symlink resolves to /Users/pcartelli/dev/bluefish-ai-skills/bf-build"
---

## Phase Goal

**As a** designer or engineer on the Bluefish team, **I want to** type `/bf-build` and receive a production-ready React/MUI component file generated from either an open Figma frame or an existing `/bf-spec` output file, **so that** I can ship component code without manually transcribing tokens, props, accessibility behavior, or MUI wrapping conventions from Figma or a spec doc.

<objective>
Create `~/dev/bluefish-ai-skills/bf-build/SKILL.md` — the `/bf-build` production component generation skill — and symlink it into `~/.claude/skills/bf-build` so Claude Code discovers it. The skill MUST have ten sections in the exact order specified below, MUST follow every locked decision D-01 through D-13 from `05-CONTEXT.md`, and MUST adapt structural patterns directly from `bf-spec/SKILL.md` (primary analog) and `bf-prototype/SKILL.md` (code output secondary analog) — reading those files in full before writing.

Purpose: Deliver BUILD-01 and BUILD-02. This plan produces the skill file itself; Plan 05-03 verifies it fires correctly via five human-run scenarios. The output of this skill (`Bluefish[ComponentName].tsx` written to `./`) is the final artifact in the v1.1 spec → build pipeline.

Output: One new SKILL.md file (similar size to bf-spec/SKILL.md, which is roughly 270 lines) and one symlink. No other files touched.
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 5 sources of truth — all 13 locked decisions, full research, full pattern map, handoff binding decisions -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-PATTERNS.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-HANDOFF.md

<!-- Plan 01 cleared the routing-reclaim ground; bf-build now claims build/implement triggers alone -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-01-SUMMARY.md

<!-- Primary structural analog: copy section order, @include line, fallback blockquote, Support Files structure, Anti-Patterns shape -->
@/Users/pcartelli/.claude/skills/bf-spec/SKILL.md

<!-- Secondary code-output analog: MUI wrapping pattern, import type rule, dual-path token output, MCP-unavailable diagnostic phrasing, pre-return checklist reference, two-tool MCP sequence (get_variable_defs + get_design_context) -->
@/Users/pcartelli/.claude/skills/bf-prototype/SKILL.md

<!-- Foundation (read for Code Output Standards section, 8-point pre-return checklist, and to verify Plan 01 edits landed cleanly) -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/SKILL.md

<!-- Reference spec examples — show the H2/H3 structure Path B parses -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-button.md
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-autocomplete.md

<interfaces>
<!-- bf-build assembles its file by adapting proven sections from analogs. The exact section order is fixed by 05-RESEARCH.md Pattern 1. -->

Section order (10 sections, exact):
  1. Frontmatter (name: bf-build, description block scalar, closing ---)
  2. @include line (verbatim from bf-spec) + blank line + fallback blockquote (verbatim from bf-spec)
  3. # bf-build heading + one-paragraph summary
  4. ## Support Files — Read On Demand
  5. ## Intake
  6. ## Figma Context — Always Attempt First   ← Path A (two-tool MCP sequence)
  7. ## Path B — Spec File Parsing             ← NEW (no analog; structure per 05-PATTERNS.md)
  8. ## Output Rules
  9. ## Anti-Patterns — Do Not Do These

Note: 05-RESEARCH.md Pattern 1 lists 10 sections by labeling #2 as "@include" and #3 as "fallback blockquote" separately. They are written as one contiguous block in bf-spec — treat them as a single section in document structure but check both elements are present.

Frontmatter description (verbatim per 05-RESEARCH.md Pattern 2 / 05-PATTERNS.md frontmatter pattern):
  - name: bf-build
  - description uses `>` block scalar form
  - Must claim triggers: "build", "implement", "generate production code", "production component"
  - Must include explicit "Not for..." exclusions naming: prototypes (bf-prototype), layout explorations (bf-explore), specs (bf-spec)
  - Must NEVER contain "planned, not yet available"
  - Output statement: "Output: a single .tsx file written to the working directory."

MCP sequence (Path A — Figma Context section):
  CORRECT: get_variable_defs, then get_design_context. Two calls only.
  WRONG: any mention of get_metadata as part of the bf-build sequence.
  Bf-build derives the output filename from the component name (conversation context or get_design_context response), NOT from a Figma frame name.

Path B parser structure (documented per 05-PATTERNS.md Path B section):
  Component name source: H2 heading `## [Component Name]`
  Sub-sections to expect: `### Variants & States`, `### Props`, `### Tokens Used`, `### Type Styles`, `### Accessibility`, `### Figma Reference`, `### Sub-component: [Name]`, `### Notes`
  Cross-spec auto-read trigger: a `### Sub-component` section mentioning "see spec-text-field.md" (or similar) — read that file silently from ./
  Screen-level gate: when `## Component Inventory` is present at top of file, present the inventory and ask "Which component should I build?" — generate ONE component only

Output Rules requirements (per D-07, D-08, D-09, D-10, D-11, D-12):
  - Single `.tsx` file written to `./` (user CWD), filename Bluefish[ComponentName].tsx (PascalCase, Bluefish prefix)
  - Named export: `export function Bluefish[Name](props: Bluefish[Name]Props)` — NOT default export
  - TypeScript interface extending the appropriate MUI props type (Claude's discretion which type per component)
  - `import type {}` for MUI type-only exports (Vite SyntaxError otherwise)
  - MUI wrapping pattern (named custom component wrapping MUI internally)
  - Dual-path tokens (both theme.palette.* and var(--*) form) with /* ⚠️ token injection method unconfirmed — verify with eng */ flag
  - Dataviz tokens: color-roles/dataviz/[NN]/[property] from tokens.md (NEVER color-dataviz/categorical/*)
  - Typography: /* type: H1 */ inline, top-level style names only
  - ARIA props required (never optional)
  - MCP-unavailable diagnostic as a // comment at top of the file, before imports
  - No scaffold files (no App.tsx, no main.tsx, no index.html, no `npm create vite` command in output)
  - React 19 target: do NOT mandate React.forwardRef wrappers (D-01)
  - Pre-return: reference foundation's 8-point checklist by name; do not restate the bullets (avoid drift)

Anti-Patterns to include (per 05-RESEARCH.md Anti-Patterns + Pitfalls):
  - "Asking the user to choose Path A or Path B" — path is inferred automatically
  - "Calling get_metadata in bf-build" — that is bf-spec's sequence
  - "Generating scaffold files (main.tsx, App.tsx, index.html)" — component file only
  - "Writing the output file to ~/.claude/skills/" — component files go to user's `./`
  - "Single-path token output" — DATA-03 unresolved, both forms required
  - "Calling get_variable_defs multiple times per session" — call once, reuse for sub-components
  - "Inventing token paths like color-dataviz/categorical/*" — read tokens.md before generating dataviz code

Symlink:
  Command: ln -s /Users/pcartelli/dev/bluefish-ai-skills/bf-build ~/.claude/skills/bf-build
  Pattern matches existing symlinks (verified in 05-RESEARCH.md): absolute source path, no trailing slash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bf-build/ directory and author SKILL.md with all 10 sections</name>

  <read_first>
    - ~/.claude/skills/bf-spec/SKILL.md — primary structural analog. Read in full. Extract verbatim: the @include line, the fallback blockquote, the Support Files section opener ("Do not read these at skill start..."), the Anti-Patterns section heading and bullet shape, the section heading style ("## Figma Context — Always Attempt First", "## Anti-Patterns — Do Not Do These").
    - ~/.claude/skills/bf-prototype/SKILL.md — secondary analog for code output. Read in full. Extract: the two-tool MCP sequence (get_variable_defs + get_design_context — bf-build's Path A sequence, NOT bf-spec's three-tool sequence), the Code Connect inline-flag rule, the token drift rule, the MCP-unavailable diagnostic phrasing, the MUI wrapping pattern wording, the `import type {}` rule wording, the dual-path token output block, the anti-pattern bullet style.
    - ~/.claude/skills/bluefish-design-system/SKILL.md — foundation. Read the Code Output Standards section and the 8-point pre-return checklist. Confirm Plan 01 edits landed (description no longer claims build/implement requests; Known Gaps no longer mentions bf-build).
    - ~/.claude/skills/bluefish-design-system/spec-button.md — reference spec example, simple component (no sub-components). Confirms Path B parse structure on a flat spec.
    - ~/.claude/skills/bluefish-design-system/spec-autocomplete.md — reference spec example, composite component. Confirms the `### Sub-component: [Name]` pattern and the "see spec-text-field.md" cross-spec reference style that triggers the auto-read rule.
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md — every D-XX decision is binding. Read the full Decisions block, especially D-01 (React 19), D-03 (no get_metadata), D-05 (cross-spec auto-read), D-06 (screen-level inventory gate), D-07, D-08 (output shape), D-09 through D-12 (token + diagnostic format), D-13 (already implemented by Plan 01).
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md — read sections: Summary, Standard Stack, Architecture Patterns (Pattern 1 through Pattern 6), Common Pitfalls (1-6), Code Examples. These specify exact wording, exact section order, and the complete TypeScript output template.
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-PATTERNS.md — read all of bf-build SKILL.md pattern assignments. Frontmatter pattern, @include pattern, Summary pattern, Support Files pattern, Intake pattern, Figma Context pattern, Path B pattern (NEW), Output Rules pattern, Anti-Patterns pattern. Verbatim sample text is provided for sections that copy from analogs.
  </read_first>

  <files>
    ~/dev/bluefish-ai-skills/bf-build/SKILL.md
  </files>

  <action>
    Step 1 — Create the directory: `mkdir -p /Users/pcartelli/dev/bluefish-ai-skills/bf-build` (no-op if it already exists; do not create a nested `bf-build/bf-build/`).

    Step 2 — Author `~/dev/bluefish-ai-skills/bf-build/SKILL.md` with the following ten sections in this exact order. After writing, the file MUST satisfy every must_haves truth.

    Section 1 — Frontmatter: `---` opener, `name: bf-build`, `description: >` followed by a block scalar that claims "build", "implement", and "generate production code" as triggers, references "Output: a single .tsx file written to the working directory.", and ends with "Not for prototypes (use bf-prototype), layout explorations (use bf-explore), or specs (use bf-spec)." Verbatim sample text appears in 05-RESEARCH.md Pattern 2 and 05-PATTERNS.md frontmatter pattern. Close with `---`. The description MUST NOT contain "planned, not yet available".

    Section 2 — @include + fallback blockquote: immediately after the closing `---`, insert the verbatim two-line @include and blockquote block from 05-PATTERNS.md "@include + fallback blockquote pattern". This is identical to bf-spec lines 11-14. Do NOT paraphrase.

    Section 3 — `# bf-build` heading + summary paragraph: per 05-PATTERNS.md "Summary paragraph pattern". One paragraph stating bf-build generates a production TypeScript React/MUI component file, naming the two intake paths (Path A: Figma MCP; Path B: spec file), inheriting foundation context via the @include above, and declaring what bf-build owns specifically (intake detection, MCP sequence, Path B spec parsing, output rules, anti-patterns).

    Section 4 — `## Support Files — Read On Demand`: copy the section heading and opener line from bf-spec verbatim ("Do not read these at skill start. Read them only when the task requires it:"). Four bullets, each starting "Read `~/.claude/skills/bluefish-design-system/[file]` when [condition]." Adapt triggering conditions for bf-build (e.g., "when generating color or spacing token references in sx props" instead of "when populating the Type Styles section of a spec"). The four files: tokens.md, type-styles.md, the color-roles/dataviz table within tokens.md (with the note that tokens-dataviz.md is raw source palette only — for gradients/tints — never for chart series tokens, per D-09), figma-reading-guide.md.

    Section 5 — `## Intake`: define bf-build's intake gate per 05-PATTERNS.md Intake section. Rules: if invoked bare with no component description AND no prior conversation context AND no Figma selection, ask exactly one question (verbatim): "What component should I build?" If invoked with a description, proceed immediately to MCP sequence. Never ask the user to choose Path A or Path B. Screen-level spec gate question lives in the Path B section, not here.

    Section 6 — `## Figma Context — Always Attempt First`: per 05-PATTERNS.md Figma Context section. Preamble: "Before generating any output, always attempt Figma MCP calls..." Numbered list of TWO calls (only): (1) Call `get_variable_defs` — pulls live token values; live values take precedence over tokens.md. (2) Call `get_design_context` — reads component/frame structure. (3) If either call triggers a Code Connect prompt: note inline with `⚠️ Code Connect not configured — proceeding from conversation context` and continue. Then a Token drift rule (use live value, flag drift inline) and a Fallback block. Fallback block: when both calls return nothing AND a `spec-*.md` is referenced in the user's message, switch to Path B (per D-02). When MCP is unavailable and no spec file is referenced, emit the MCP-unavailable diagnostic as a `//` comment at the top of the generated .tsx file using EXACTLY this phrasing (per D-12 / 05-PATTERNS.md MCP-unavailable diagnostic): `// ⚠️ Figma MCP unavailable — tokens from cached Bluefish reference (tokens.md, type-styles.md, tokens-dataviz.md); live values not verified`. Do NOT mention `get_metadata` anywhere in this section.

    Section 7 — `## Path B — Spec File Parsing`: per 05-PATTERNS.md "Path B — Spec File Parsing pattern (NEW)". Four sub-blocks: (a) Spec file format documentation listing the H2 component heading and the eight `###` sub-sections in the order documented in 05-RESEARCH.md Pattern 5; (b) Cross-spec auto-read rule using the exact wording from 05-PATTERNS.md / 05-RESEARCH.md Pitfall 5 — "When a `### Sub-component` section references another spec file (e.g., 'see spec-text-field.md'), read that file from `./` before generating the sub-component code. This read is silent — do not prompt the user."; (c) Screen-level spec gate — when the spec file contains `## Component Inventory`, present the inventory table and ask exactly: "Which component should I build?" Generate ONE .tsx file for the selected component only (per D-06); (d) Component name derivation — extract from the `## [Component Name]` H2 heading; this drives the output filename Bluefish[ComponentName].tsx.

    Section 8 — `## Output Rules`: state the single-file, no-scaffold rule per D-07/D-08. Specify: filename Bluefish[ComponentName].tsx written to ./; named export `export function Bluefish[Name]`; TypeScript interface extending the appropriate MUI props type (e.g., `interface BluefishButtonProps extends ButtonProps`); MUI wrapping pattern (named custom component wrapping MUI internally, per 05-PATTERNS.md Output Rules — copy bf-prototype's wording for this rule); `import type {}` rule with CORRECT/INCORRECT examples (verbatim from bf-prototype lines 178-180 per 05-PATTERNS.md); dual-path token output block (per D-11 / 05-PATTERNS.md) showing both the theme.palette form and the CSS custom property form with `/* ⚠️ token injection method unconfirmed — verify with eng */` flag on every token reference; dataviz token format `color-roles/dataviz/[NN]/[property]` sourced from tokens.md, with explicit note that tokens-dataviz.md is the raw palette for gradients/tints only (per D-09); typography annotation format `/* type: H1 */` inline, top-level style names only (per D-10); ARIA props required (never optional); MCP-unavailable diagnostic as a `//` comment at the top of the file before imports; React 19 note — `forwardRef` is NOT required unless the component explicitly needs ref forwarding for a specific API reason (per D-01); pre-return: "Apply the foundation 8-point pre-return checklist (see `bluefish-design-system/SKILL.md` Code Output Standards) before returning the file." Do NOT restate the checklist bullets. Conclude with a complete TypeScript example matching 05-RESEARCH.md Code Examples "Output File — Complete Pattern" — the BluefishButton.tsx example — adapted to whatever wording flows in the section.

    Section 9 — `## Anti-Patterns — Do Not Do These`: section heading exact. Bullet list of bold-titled anti-patterns. Required bullets (with sources): "Asking the user to choose Path A or Path B" (new); "Calling `get_metadata` in bf-build" — that is bf-spec's sequence (per Pitfall 2); "Generating scaffold files (main.tsx, App.tsx, index.html)" — bf-build output is the component file only (per Pitfall 3); "Writing the output file to `~/.claude/skills/`" — adapted from bf-spec; "Single-path token output" — DATA-03 unresolved, dual-path required (adapted from bf-prototype); "Calling `get_variable_defs` multiple times per session" — call once, reuse for sub-components; "Inventing token paths like `color-dataviz/categorical/*` for dataviz" — read `tokens.md` color-roles/dataviz table before generating any chart code (per Pitfall 1). Do NOT include any bullet referencing "until /bf-build ships" — this skill IS bf-build.

    Throughout the file, use the ⚠️ flagging vocabulary verbatim per 05-PATTERNS.md "Shared Patterns — ⚠️ flagging vocabulary". Strip internal planning IDs (D-XX, BUILD-XX, DATA-XX) from team-facing text per the portability pass — do NOT write "DATA-03" in the skill file; instead use the human-readable flag phrasing "token injection method unconfirmed — verify with eng".

    Per D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12. D-13 already implemented in Plan 01.
  </action>

  <verify>
    <automated>test -f ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^name: bf-build' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '@~/.claude/skills/bluefish-design-system/SKILL.md' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'did not load above' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Support Files — Read On Demand' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Intake' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Figma Context — Always Attempt First' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Path B — Spec File Parsing' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Output Rules' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '^## Anti-Patterns — Do Not Do These' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'get_variable_defs' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'get_design_context' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; ! grep -q 'planned, not yet available' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; ! grep -q 'DATA-03' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'token injection method unconfirmed' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'What component should I build' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'Which component should I build' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'Bluefish\[ComponentName\]\.tsx' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'color-roles/dataviz' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; ! grep -q 'color-dataviz/categorical' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q '/\* type: ' ~/dev/bluefish-ai-skills/bf-build/SKILL.md &amp;&amp; grep -q 'import type' ~/dev/bluefish-ai-skills/bf-build/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `~/dev/bluefish-ai-skills/bf-build/SKILL.md` exists as a regular file
    - Frontmatter contains `name: bf-build`
    - Frontmatter description contains the words "build" and "implement" (case-insensitive trigger anchors)
    - Frontmatter description contains "Not for prototypes (use bf-prototype)" or equivalent explicit exclusions naming bf-prototype, bf-explore, and bf-spec
    - File contains the @include line `@~/.claude/skills/bluefish-design-system/SKILL.md`
    - File contains the fallback blockquote referencing "did not load above"
    - File contains heading `## Support Files — Read On Demand` (exact, em-dash not hyphen)
    - File contains heading `## Intake`
    - File contains heading `## Figma Context — Always Attempt First` (exact, em-dash not hyphen)
    - File contains heading `## Path B — Spec File Parsing` (exact, em-dash not hyphen)
    - File contains heading `## Output Rules`
    - File contains heading `## Anti-Patterns — Do Not Do These` (exact, em-dash not hyphen)
    - File mentions `get_variable_defs` and `get_design_context`
    - File does NOT instruct Claude to call `get_metadata` as part of the bf-build sequence — `get_metadata` appears in the file at most once, and only inside the Anti-Patterns section as a "do not call" bullet
    - File does NOT contain the phrase "planned, not yet available" anywhere
    - File does NOT contain the internal planning ID "DATA-03" anywhere
    - File contains the human-readable flag phrasing "token injection method unconfirmed" (the user-facing replacement for DATA-03)
    - Intake section bare-invoke question is "What component should I build?" — verifiable by `grep "What component should I build" ~/dev/bluefish-ai-skills/bf-build/SKILL.md`
    - Path B screen-level inventory gate question is "Which component should I build?" — verifiable by `grep "Which component should I build" ~/dev/bluefish-ai-skills/bf-build/SKILL.md`
    - Output Rules reference the filename pattern `Bluefish[ComponentName].tsx`
    - Output Rules reference the dataviz path `color-roles/dataviz` — and the file does NOT contain the invalid path `color-dataviz/categorical` anywhere
    - Output Rules document the typography annotation format with `/* type: ` opening comment style
    - Output Rules document the `import type` rule for MUI type-only exports
    - Output Rules reference the foundation 8-point pre-return checklist by name (e.g., the section name "Code Output Standards" or the phrase "8-point pre-return checklist") rather than restating its bullets
    - Anti-Patterns section contains an explicit bullet that calling `get_metadata` is the bf-spec sequence
    - File starts with valid YAML frontmatter (line 1 is `---`, the next `---` closes the frontmatter, and `name: bf-build` appears between them) — verifiable by `head -3 ~/dev/bluefish-ai-skills/bf-build/SKILL.md` showing `---` on line 1 and `name: bf-build` on a subsequent line
  </acceptance_criteria>

  <done>
    `~/dev/bluefish-ai-skills/bf-build/SKILL.md` exists with all 10 sections in the correct order. Every locked decision D-01 through D-12 is represented in the file's wording. The skill never references `get_metadata` except in the anti-pattern bullet. Token output is dual-path with the human-readable flag (not "DATA-03"). The file reads like a sibling to `bf-spec/SKILL.md` in structure and tone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create symlink ~/.claude/skills/bf-build pointing to /Users/pcartelli/dev/bluefish-ai-skills/bf-build</name>

  <read_first>
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-RESEARCH.md — section "Routing Reclaim — Enumerated Edit Targets" includes the exact symlink command and notes that the pattern was verified against existing symlinks via `ls -la ~/.claude/skills/`
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-HANDOFF.md — "File layout changed" section explains why ~/dev/bluefish-ai-skills/ is the canonical source and ~/.claude/skills/ holds symlinks
    - Output of `ls -la ~/.claude/skills/ | grep '^l'` — confirms the four existing skill symlinks all use absolute source paths with no trailing slash (bf-spec, bf-prototype, bf-explore, bluefish-design-system); bf-build symlink must match the same pattern
  </read_first>

  <files>
    ~/.claude/skills/bf-build
  </files>

  <action>
    Run `ls -la ~/.claude/skills/bf-build` first to confirm it does NOT already exist (would indicate a stale entry from a prior run). If it exists and is NOT a symlink, STOP and surface the conflict — do not overwrite a real directory or file. If it exists as a symlink already pointing to /Users/pcartelli/dev/bluefish-ai-skills/bf-build, the task is already done — record the no-op in the SUMMARY.

    Otherwise, run exactly: `ln -s /Users/pcartelli/dev/bluefish-ai-skills/bf-build ~/.claude/skills/bf-build`

    Verify by running `ls -la ~/.claude/skills/bf-build` and confirming the output shows a symlink (leading `l` in the file-type field) with target `/Users/pcartelli/dev/bluefish-ai-skills/bf-build` (absolute path, NO trailing slash) — matches the pattern of the four existing skill symlinks. Then run `ls ~/.claude/skills/bf-build/SKILL.md` to confirm the symlink resolves and the SKILL.md file from Task 1 is reachable via the symlinked path.
  </action>

  <verify>
    <automated>test -L ~/.claude/skills/bf-build &amp;&amp; test "$(readlink ~/.claude/skills/bf-build)" = "/Users/pcartelli/dev/bluefish-ai-skills/bf-build" &amp;&amp; test -f ~/.claude/skills/bf-build/SKILL.md &amp;&amp; grep -q '^name: bf-build' ~/.claude/skills/bf-build/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `~/.claude/skills/bf-build` exists as a symbolic link (verifiable by `test -L ~/.claude/skills/bf-build`)
    - `readlink ~/.claude/skills/bf-build` outputs exactly `/Users/pcartelli/dev/bluefish-ai-skills/bf-build` (absolute path, no trailing slash) — matches the pattern of existing symlinks like `bf-spec -> /Users/pcartelli/dev/bluefish-ai-skills/bf-spec`
    - `ls -la ~/.claude/skills/bf-build/SKILL.md` resolves and shows the SKILL.md file authored in Task 1
    - `grep -c '^name: bf-build' ~/.claude/skills/bf-build/SKILL.md` returns 1 (confirms the skill file is reachable via the symlinked path)
    - Output of `ls -la ~/.claude/skills/ | grep bf-` shows five entries (bf-build, bf-explore, bf-prototype, bf-spec, plus bluefish-design-system separately) all with the same `l` file-type prefix and absolute source paths
  </acceptance_criteria>

  <done>
    Symlink `~/.claude/skills/bf-build` exists, points to `/Users/pcartelli/dev/bluefish-ai-skills/bf-build`, and resolves to the SKILL.md authored in Task 1. The five skill symlinks under `~/.claude/skills/` all follow identical conventions (absolute path, no trailing slash). Claude Code can discover and load the bf-build skill.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

```bash
# Skill file exists at canonical source location and is reachable via symlink
test -f ~/dev/bluefish-ai-skills/bf-build/SKILL.md && echo "source OK"
test -L ~/.claude/skills/bf-build && echo "symlink OK"
test -f ~/.claude/skills/bf-build/SKILL.md && echo "resolves OK"

# Section structure is intact
grep -E '^(## (Support Files — Read On Demand|Intake|Figma Context — Always Attempt First|Path B — Spec File Parsing|Output Rules|Anti-Patterns — Do Not Do These))' ~/dev/bluefish-ai-skills/bf-build/SKILL.md
# expected: six matches (one per major section heading)

# Locked-decision compliance
! grep -q 'planned, not yet available' ~/dev/bluefish-ai-skills/bf-build/SKILL.md && echo "no interim language"
! grep -q 'DATA-03' ~/dev/bluefish-ai-skills/bf-build/SKILL.md && echo "no internal IDs"
! grep -q 'color-dataviz/categorical' ~/dev/bluefish-ai-skills/bf-build/SKILL.md && echo "no invented dataviz paths"

# get_metadata appears at most once and only in the Anti-Patterns section
grep -n 'get_metadata' ~/dev/bluefish-ai-skills/bf-build/SKILL.md
# expected: at most one match, located after the Anti-Patterns heading
```
</verification>

<success_criteria>
1. `~/dev/bluefish-ai-skills/bf-build/SKILL.md` exists with frontmatter, @include, fallback blockquote, summary, and all six `## ...` content sections in order
2. The skill declares the two-tool Path A MCP sequence (`get_variable_defs` then `get_design_context`) — never calls `get_metadata`
3. Path B section documents H2/H3 spec parsing, cross-spec auto-read, and the screen-level "Which component should I build?" gate
4. Output Rules specify the single-file production output shape (Bluefish[ComponentName].tsx to ./, named export, MUI props extension, dual-path tokens with human-readable flag, ARIA required, no scaffold, React 19 no-forwardRef rule)
5. Anti-Patterns section includes the seven required bullets, including the "Calling get_metadata in bf-build" entry
6. `~/.claude/skills/bf-build` is a symlink to `/Users/pcartelli/dev/bluefish-ai-skills/bf-build` (absolute path) and resolves to the SKILL.md authored in Task 1
7. No internal planning IDs (D-XX, DATA-XX) and no interim language ("planned, not yet available") anywhere in the skill file
</success_criteria>

<output>
Create `.planning/phases/05-bf-build/05-02-SUMMARY.md` when done — record the section breakdown of the authored SKILL.md (line count and what each major section contains), the symlink command result, all grep-verification outcomes, and any deviation from the planned section order or wording with rationale.
</output>
