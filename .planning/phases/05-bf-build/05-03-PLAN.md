---
phase: 05-bf-build
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: []
autonomous: false
requirements: [BUILD-01, BUILD-02]

must_haves:
  truths:
    - "Typing `/bf-build` in Claude Code activates the bf-build skill (not the foundation, not bf-prototype, not bf-spec)"
    - "Bare invocation triggers exactly one clarifying question: 'What component should I build?' — and only when no Figma context and no component description are present"
    - "Asking to 'build' or 'implement' a component auto-triggers bf-build without typing the slash command"
    - "Path A (Figma frame open, MCP available): skill calls get_variable_defs and get_design_context (in that order or in parallel), NEVER calls get_metadata, and produces a Bluefish[ComponentName].tsx file in the user's CWD"
    - "Path B (spec file present, MCP returns nothing): skill reads the spec-[component].md file from ./, parses the H2 component name + ### sub-sections, and produces a Bluefish[ComponentName].tsx file in the user's CWD"
    - "Screen-level spec input (Component Inventory table at top): skill presents the inventory and asks exactly 'Which component should I build?' before generating anything; produces ONE .tsx file"
    - "Cross-referenced spec input (sub-component references another spec-*.md file): skill silently reads the referenced spec from ./ before generating the sub-component code — does not prompt the user about the cross-reference"
    - "Generated .tsx files contain dual-path token output (theme.palette form AND var(--*) form) flagged with /* ⚠️ token injection method unconfirmed — verify with eng */"
    - "Generated .tsx files contain ZERO scaffold artifacts — no App.tsx, no main.tsx, no index.html, no `npm create vite` command in the response"
    - "Generated .tsx files use named export (export function Bluefish[Name]), TypeScript interface extending MUI props, import type for MUI type exports, ARIA props on interactive elements"
    - "Generated .tsx filenames use Bluefish[ComponentName].tsx (PascalCase, Bluefish prefix) and land in the user's CWD"
  artifacts:
    - path: "~/.claude/skills/bf-build/SKILL.md"
      provides: "The skill file under behavioral test (symlinked from ~/dev/bluefish-ai-skills/bf-build/SKILL.md)"
  key_links:
    - from: "User invocation (slash command or natural language)"
      to: "bf-build skill activation"
      via: "Claude Code skill router matching the description triggers from Plan 02"
      pattern: "skill activation observable in conversation"
    - from: "Path A — Figma frame open"
      to: "Bluefish[ComponentName].tsx file in ./"
      via: "get_variable_defs + get_design_context (NOT get_metadata) → TypeScript output"
      pattern: "tsx file in cwd; no get_metadata call observed in conversation"
    - from: "Path B — spec file in ./"
      to: "Bluefish[ComponentName].tsx file in ./"
      via: "Read of spec-[component].md → H2/H3 parse → TypeScript output"
      pattern: "tsx file in cwd; spec file Read tool call observed in conversation"
    - from: "Screen-level spec input"
      to: "Single-component output after user selection"
      via: "Skill presents Component Inventory + asks 'Which component should I build?' → user names one → skill builds that one only"
      pattern: "one tsx file, named exactly the component the user selected"
    - from: "Cross-referenced spec input"
      to: "Sub-component code derived from referenced spec"
      via: "Silent Read of spec-text-field.md (or similar) before generating sub-component"
      pattern: "Read tool call on the referenced spec observable in conversation; no user prompt about cross-reference"
---

## Phase Goal

**As a** designer or engineer on the Bluefish team, **I want to** type `/bf-build` and receive a production-ready React/MUI component file generated from either an open Figma frame or an existing `/bf-spec` output file, **so that** I can ship component code without manually transcribing tokens, props, accessibility behavior, or MUI wrapping conventions from Figma or a spec doc.

<objective>
Human-verify that `~/.claude/skills/bf-build/SKILL.md` (the symlinked file authored in Plan 02) behaves correctly across five test scenarios that exercise BUILD-01 and BUILD-02 acceptance criteria: bare invocation, Path A (Figma frame), Path B with a component-level spec, Path B with a screen-level spec (inventory → pick), and Path B with a cross-referenced composite spec.

Purpose: This is the gate that closes Phase 5 and milestone v1.1. Plan 02's grep gates verified the skill file's textual correctness; this plan verifies the skill actually fires in Claude Code AND produces the correct output shape per intake path. Without this checkpoint, BUILD-02 ("Skill fires correctly when user types /bf-build or asks to build or implement a Bluefish component from a spec or Figma frame") cannot be confirmed. The five scenarios collectively prove both paths (Path A + Path B), all three intake conditions (bare, component-level, screen-level), and the cross-spec auto-read behavior — closing the remaining must_haves truths that grep cannot test.

Output: User approval (or actionable issues list) — no files written by this plan. If issues are reported, the orchestrator will revise Plan 05-02 via /gsd:plan-phase.
</objective>

<execution_context>
@/Users/pcartelli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/PROJECT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/ROADMAP.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/STATE.md

<!-- Phase 5 inputs needed to formulate verification steps -->
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-01-SUMMARY.md
@/Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-02-SUMMARY.md

<!-- The skill being verified — symlinked file -->
@/Users/pcartelli/.claude/skills/bf-build/SKILL.md

<!-- Reference spec files for Path B scenarios — already in the foundation skill, available at the user-facing paths -->
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-button.md
@/Users/pcartelli/.claude/skills/bluefish-design-system/spec-autocomplete.md

<interfaces>
<!-- Acceptance scenarios mapped to ROADMAP Phase 5 success criteria -->

ROADMAP Phase 5 success criteria 1: "`bf-build/SKILL.md` exists at `~/.claude/skills/bf-build/SKILL.md` and the skill is invocable via `/bf-build`"
→ Verified by scenarios 1 (bare invoke), 2 (Path A), and 3 (Path B component-level)

ROADMAP Phase 5 success criteria 2: "Path A works: Figma frame open → skill reads frame via MCP (`get_variable_defs` + `get_design_context`) and produces a TypeScript React component"
→ Verified by scenario 2; observe MCP tool calls (must be get_variable_defs + get_design_context, NOT get_metadata)

ROADMAP Phase 5 success criteria 3: "Path B works: existing `spec-[component].md` file provided → skill reads spec file and produces a TypeScript React component without requiring a Figma frame"
→ Verified by scenarios 3, 4, and 5; observe Read tool call on the spec file

ROADMAP Phase 5 success criteria 4: "Skill fires correctly when user types `/bf-build` or asks to build or implement a Bluefish component from a spec or Figma frame"
→ Verified by scenarios 1 (slash command bare), 2 (slash command + frame), and the natural-language sub-tests inside scenarios 2-3

BUILD-01 (requirement): file exists supporting Path A and Path B
→ Confirmed by Plan 02 grep gates + scenarios 2 (Path A) and 3, 4, 5 (Path B)

BUILD-02 (requirement): skill fires correctly via slash command or natural-language build/implement request
→ Confirmed by scenarios 1, 2, and the natural-language sub-tests in scenario 2

Scenario coverage matrix:
  Scenario 1 — Bare /bf-build         → bare-invoke gate ("What component should I build?")
  Scenario 2 — Path A: Figma frame     → MCP sequence (get_variable_defs + get_design_context), output file shape
  Scenario 3 — Path B: component spec  → spec parsing, component-level output, named export, dual-path tokens
  Scenario 4 — Path B: screen spec     → screen-level inventory gate ("Which component should I build?"), one file per invocation
  Scenario 5 — Path B: composite spec  → cross-spec auto-read (silent Read of referenced spec file), sub-component handling
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify bf-build skill across five scenarios</name>

  <read_first>
    - ~/.claude/skills/bf-build/SKILL.md — read the completed skill file before presenting verification steps so the executor can quote exact wording (the bare-invoke question, the screen-level gate question, the MCP call list, the output filename pattern) in the scenario descriptions
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-02-SUMMARY.md — confirms Plan 02 completed (skill file authored + symlinked) and all acceptance criteria passed before this checkpoint runs
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-01-SUMMARY.md — confirms routing reclaim completed; interim "planned, not yet available" language no longer competes with bf-build for build/implement triggers
    - /Users/pcartelli/Desktop/figma-scripter-tools/.planning/phases/05-bf-build/05-CONTEXT.md — decisions D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-11, D-12 govern expected behavior per scenario
    - ~/.claude/skills/bluefish-design-system/spec-button.md — flat component spec used in Scenario 3; executor must know its exact filename and component name before presenting the scenario
    - ~/.claude/skills/bluefish-design-system/spec-autocomplete.md — composite spec used in Scenario 5; executor must verify it contains a `### Sub-component` section that references another spec file, so the cross-spec auto-read behavior can be observed
  </read_first>

  <files>
    (no files written by this plan — checkpoint observes behavior of ~/.claude/skills/bf-build/SKILL.md authored in Plan 02)
  </files>

  <action>
    Present the five verification scenarios described in `<how-to-verify>` to the user, in order, and pause for the user to run each. For each scenario, restate the exact invocation text the user should type (e.g., literal `/bf-build` for scenario 1; literal `/bf-build` after opening a Figma frame for scenario 2; literal `/bf-build using spec-button.md` for scenario 3; literal `/bf-build using spec-[screen].md` for scenario 4 — use whatever screen-level spec the user has handy from Phase 4 verification; literal `/bf-build using spec-autocomplete.md` for scenario 5).

    Before each scenario, restate the SPECIFIC observable signals from `<acceptance_criteria>` that map to that scenario — for example, for scenario 2 the executor must remind the user to watch for `get_variable_defs` and `get_design_context` tool calls in the conversation and to confirm `get_metadata` is NOT called. The executor must also remind the user to run each scenario in a fresh Claude Code conversation (start a new chat) so skill caching does not mask a regression.

    Wait for the user's observed result per scenario before moving on. Map each observed result against the corresponding scenario's acceptance bullets. If a scenario fails, capture the specific failure verbatim — e.g., "scenario 2 observed: skill called get_metadata as first MCP call instead of get_variable_defs" — and surface it to the user. Do NOT modify the skill file during this plan. Failed scenarios are reported back to the orchestrator for revision via `/gsd:plan-phase --revision`. The plan completes only when the user types `approved` (all five scenarios pass).

    Per D-02 (Path A first, fallback to Path B), D-03 (no get_metadata in bf-build), D-05 (cross-spec auto-read), D-06 (screen-level inventory gate), D-07 (output shape), D-08 (no scaffold), D-11 (dual-path tokens), D-12 (MCP-unavailable diagnostic phrasing).
  </action>

  <verify>
    <human-check>User runs five scenarios in fresh Claude Code conversations and reports observed behavior. Acceptance criteria bullets are checked against observations. User signals `approved` (all five pass) or describes failed scenarios with enough detail for the orchestrator to revise Plan 05-02.</human-check>
  </verify>

  <acceptance_criteria>
    **Scenario 1 — Bare /bf-build (BUILD-02 slash-command fire + bare-invoke gate):**
    - Typing `/bf-build` in a fresh Claude Code conversation activates the bf-build skill (NOT bluefish-design-system, NOT bf-prototype, NOT bf-spec)
    - With no Figma selection open and no prior component description in the conversation, the skill asks exactly one question: "What component should I build?" (verbatim, no variation)
    - The skill does NOT ask the user to choose between Path A and Path B
    - The skill does NOT proceed to MCP calls or file generation before the user names a component

    **Scenario 2 — Path A: Figma frame open (ROADMAP success criteria 2 + BUILD-01 Path A):**
    - With a Bluefish Figma frame selected, typing `/bf-build` causes the skill to call `get_variable_defs` and `get_design_context` (observable as tool calls in the conversation)
    - The skill does NOT call `get_metadata` at any point in this scenario
    - If a Code Connect prompt interrupts either call, the skill flags `⚠️ Code Connect not configured — proceeding from conversation context` inline and continues; output is not blocked
    - The skill writes exactly one `.tsx` file to the user's current working directory (verifiable by `ls *.tsx` after the conversation)
    - The output filename follows `Bluefish[ComponentName].tsx` — PascalCase, `Bluefish` prefix (e.g., `BluefishButton.tsx`, not `bluefish-button.tsx`, not `Button.tsx`)
    - The output file contains a named export `export function Bluefish[ComponentName]` — NOT `export default`
    - The output file contains a TypeScript interface extending an MUI props type (e.g., `interface BluefishButtonProps extends ButtonProps`)
    - The output file uses `import type` for MUI type-only imports
    - The output file contains dual-path token references (both `theme.palette.*` form AND `var(--*)` form) with the flag `/* ⚠️ token injection method unconfirmed — verify with eng */` on at least one token reference
    - The skill does NOT write `App.tsx`, `main.tsx`, `index.html`, or any other scaffold file; the response does NOT contain an `npm create vite` command

    **Scenario 3 — Path B with component-level spec (BUILD-01 Path B):**
    - With a `spec-button.md` file present in the working directory (e.g., copy from `~/.claude/skills/bluefish-design-system/spec-button.md` to a temp working dir), and no Figma frame open, typing `/bf-build using spec-button.md` (or asking "build the Button component from spec-button.md") causes the skill to read the spec file (observable as a Read tool call in the conversation)
    - The skill extracts the component name "Button" from the `## Button` H2 heading
    - The skill writes exactly one `.tsx` file named `BluefishButton.tsx` to the working directory
    - The output file's TypeScript interface includes the props enumerated in the spec's `### Props` section
    - The output file includes the ARIA props enumerated in the spec's `### Accessibility` section (ARIA props are present, never omitted)
    - The output file contains dual-path token references for every color/spacing/radius token from the spec's `### Tokens Used` section, each flagged with `/* ⚠️ token injection method unconfirmed — verify with eng */`
    - The output file does NOT include `App.tsx`, `main.tsx`, `index.html`, or scaffold instructions

    **Scenario 4 — Path B with screen-level spec (D-06 inventory gate):**
    - With a screen-level spec file in the working directory (one containing `## Component Inventory` at the top followed by per-component `## [Component Name]` sections — the user supplies an actual Phase 4 screen-mode output file or a hand-crafted equivalent), typing `/bf-build using spec-[screen].md` causes the skill to present the Component Inventory table (or list) to the user
    - The skill asks exactly: "Which component should I build?" (verbatim, no variation; matches the wording in the SKILL.md Path B section)
    - The skill does NOT generate any `.tsx` files before the user names a component
    - After the user names one component, the skill writes exactly ONE `.tsx` file (`Bluefish[ChosenComponent].tsx`) — does NOT generate files for every component in the inventory

    **Scenario 5 — Path B with cross-referenced composite spec (D-05 cross-spec auto-read):**
    - With `spec-autocomplete.md` in the working directory AND `spec-text-field.md` in the same directory (both copied from `~/.claude/skills/bluefish-design-system/`), typing `/bf-build using spec-autocomplete.md` causes the skill to read `spec-autocomplete.md` first, then silently read `spec-text-field.md` (observable as a second Read tool call) when it encounters the `### Sub-component` section referencing the text field spec
    - The skill does NOT prompt the user with a question about the cross-reference (no "Should I read spec-text-field.md?" message)
    - The skill writes exactly one `.tsx` file (`BluefishAutocomplete.tsx`) to the working directory
    - The output file's sub-component handling reflects the token paths and props from `spec-text-field.md` (not invented from training data) — sanity-check by spot-comparing one or two prop names or token paths against `spec-text-field.md` contents

    **Overall:**
    - Each scenario is run in a FRESH Claude Code conversation (not chained in one long session) so skill activation is reproducible from cold start
    - All five scenarios pass for the user to type `approved`; any failure is captured verbatim so the orchestrator can revise Plan 05-02
  </acceptance_criteria>

  <what-built>
    `~/.claude/skills/bf-build/SKILL.md` — the `/bf-build` command skill that:
    1. Activates on `/bf-build` slash command OR natural-language build/implement requests (BUILD-02)
    2. Bare invocation asks exactly: "What component should I build?" — only when no description and no Figma context (D-02)
    3. Path A: with Figma frame open, calls `get_variable_defs` + `get_design_context` (NEVER `get_metadata`, D-03)
    4. Path B: with spec file in `./`, reads the spec and parses H2 component heading + `###` sub-sections (D-04)
    5. Screen-level spec gate: presents Component Inventory and asks "Which component should I build?" before generating (D-06)
    6. Cross-referenced spec: silently reads the referenced spec file (D-05)
    7. Output: single `Bluefish[ComponentName].tsx` file written to `./` (D-07) — no scaffold files (D-08)
    8. Generated file: named export, TypeScript interface extending MUI props, `import type` for MUI types, dual-path tokens with `⚠️ token injection method unconfirmed` flag (D-11), ARIA required, dataviz tokens use `color-roles/dataviz/*` paths from tokens.md (D-09), typography uses `/* type: H1 */` inline (D-10)
    9. MCP-unavailable diagnostic: `// ⚠️ Figma MCP unavailable — tokens from cached Bluefish reference (tokens.md, type-styles.md, tokens-dataviz.md); live values not verified` at top of file (D-12)
    10. React 19 target: no `React.forwardRef` wrappers unless the component explicitly needs ref forwarding (D-01)

    Inherits all Bluefish foundation context (token rules, accessibility standards, Figma MCP setup, component map, 8-point pre-return checklist) from `bluefish-design-system` via `@include`.
  </what-built>

  <how-to-verify>
    **Setup**: Open a fresh Claude Code conversation for each scenario (start a new chat each time so the updated skill file is freshly discovered). Have Figma Desktop open with the Bluefish pattern library available for scenario 2. For scenarios 3, 4, 5: prepare a scratch working directory with the listed spec files copied in.

    **Scenario 1 — Bare invocation**
    1. Open a fresh Claude Code conversation in a directory with NO Figma frame open and NO prior component context
    2. Type literally: `/bf-build`
    3. Observe: skill should ask exactly "What component should I build?" — no more, no less, no offer to choose Path A vs Path B, no MCP calls yet
    4. Report what was actually asked (paste the question verbatim)

    **Scenario 2 — Path A: Figma frame open**
    1. Open a fresh Claude Code conversation
    2. In Figma Desktop, select a Bluefish component (Button or Chip is simplest — pick whatever frame is accessible)
    3. Type literally: `/bf-build`
    4. Observe the tool calls in the conversation: confirm `get_variable_defs` and `get_design_context` are called; confirm `get_metadata` is NOT called
    5. After the skill completes, run `ls *.tsx` in the working directory; confirm exactly one Bluefish[Name].tsx file exists
    6. Open the generated file: confirm named export, MUI props interface extension, `import type` usage, dual-path tokens with `⚠️ token injection method unconfirmed` flag, ARIA props on interactive elements, NO scaffold files generated
    7. Report any deviation (wrong MCP call order, missing flag, extra scaffold files, etc.)

    **Scenario 3 — Path B: component-level spec**
    1. Open a fresh Claude Code conversation in a scratch directory
    2. Copy `~/.claude/skills/bluefish-design-system/spec-button.md` into the scratch directory
    3. Without opening Figma (or with no frame selected), type literally: `/bf-build using spec-button.md`
    4. Observe the conversation: confirm a Read tool call on `spec-button.md` is made
    5. After the skill completes, run `ls *.tsx`; confirm `BluefishButton.tsx` exists in the scratch directory (NOT `~/.claude/skills/`)
    6. Open the generated file: confirm the props interface includes the props from `spec-button.md`'s `### Props` section and ARIA props from its `### Accessibility` section
    7. Confirm dual-path tokens are present with the `⚠️ token injection method unconfirmed — verify with eng` flag

    **Scenario 4 — Path B: screen-level spec (inventory gate)**
    1. Open a fresh Claude Code conversation in a scratch directory
    2. Place a screen-level spec file (one containing `## Component Inventory` table at top + per-component `## [Name]` sections — use a Phase 4 output if one exists, or hand-craft a minimal one with two components like Button and Chip)
    3. Type literally: `/bf-build using spec-[your-screen-name].md`
    4. Observe: skill should present the Component Inventory (quote the table or list it) and ask exactly "Which component should I build?" — and STOP, generating nothing yet
    5. Reply with one component name (e.g., "Button")
    6. Confirm exactly ONE `.tsx` file is generated (`BluefishButton.tsx`), not multiple

    **Scenario 5 — Path B: composite spec with cross-reference**
    1. Open a fresh Claude Code conversation in a scratch directory
    2. Copy both `spec-autocomplete.md` AND `spec-text-field.md` from `~/.claude/skills/bluefish-design-system/` into the scratch directory
    3. Type literally: `/bf-build using spec-autocomplete.md`
    4. Observe the conversation: confirm TWO Read tool calls — first on `spec-autocomplete.md`, then silently on `spec-text-field.md` (when the skill encounters the `### Sub-component` cross-reference). The skill must NOT ask the user about reading the second file.
    5. After completion, confirm `BluefishAutocomplete.tsx` exists; spot-check that one or two TextField-related props or tokens in the output match the values in `spec-text-field.md` (i.e., the skill actually USED the cross-referenced spec rather than improvising from training data)

    **Reporting**: For each scenario, reply with one of:
    - `pass` (acceptance criteria all met)
    - A short description of what failed (e.g., "scenario 2: skill called get_metadata before get_variable_defs" or "scenario 5: skill asked 'Should I read spec-text-field.md?' instead of silently reading it")

    Type `approved` after all five scenarios pass to close Phase 5.
  </how-to-verify>

  <resume-signal>Type "approved" after all five scenarios pass, or describe failures per the reporting format above</resume-signal>

  <done>
    User has run all five scenarios in fresh Claude Code conversations and either typed `approved` (Phase 5 closes; milestone v1.1 complete) or reported specific scenario failures (Plan 05-02 needs revision via /gsd:plan-phase --revision).
  </done>
</task>

</tasks>

<verification>
This plan's verification is the user's `approved` signal across all five scenarios. No automated verification — the checkpoint is the verification.

Cross-references to confirm during the verify session:
- ROADMAP Phase 5 Success Criteria 1 (skill exists + invocable) → Scenarios 1, 2, 3
- ROADMAP Phase 5 Success Criteria 2 (Path A MCP sequence + TypeScript output) → Scenario 2
- ROADMAP Phase 5 Success Criteria 3 (Path B works without Figma frame) → Scenarios 3, 4, 5
- ROADMAP Phase 5 Success Criteria 4 (fires on slash command + natural language) → Scenarios 1, 2
- BUILD-01 (file exists + supports both paths) → Scenarios 2 (Path A) + 3, 4, 5 (Path B)
- BUILD-02 (correct firing) → Scenarios 1, 2 + natural-language sub-tests
</verification>

<success_criteria>
1. User has executed all five scenarios in fresh Claude Code conversations
2. Each scenario's acceptance criteria are evaluated against observed behavior
3. User signals `approved` (closes Phase 5, closes milestone v1.1) OR reports specific failures with verbatim observations for the orchestrator to drive revision
4. No automated test substitutes for this human verification — the checkpoint IS the gate
5. Plan completes only when the user explicitly types `approved`
</success_criteria>

<output>
Create `.planning/phases/05-bf-build/05-03-SUMMARY.md` when done — record each scenario's pass/fail status, the verbatim user-reported observations, the date verified, and (on full pass) the explicit "BUILD-01 and BUILD-02 verified; milestone v1.1 closed" line. If any scenario failed, record the failure verbatim and link to the next action (revision via /gsd:plan-phase).
</output>
